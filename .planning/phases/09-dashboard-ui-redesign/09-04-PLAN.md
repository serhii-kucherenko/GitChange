---
phase: 09-dashboard-ui-redesign
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/dashboard/src/components/IndexStatusCard.tsx
  - packages/dashboard/src/components/EraDetailPanel.tsx
  - packages/dashboard/src/components/CommitFilterBar.tsx
  - packages/dashboard/src/components/RepoFilterBar.tsx
  - packages/dashboard/src/components/FileHistoryScrubber.tsx
  - packages/dashboard/src/components/CommitDetailPanel.tsx
  - packages/dashboard/src/components/FileHunkView.tsx
  - packages/dashboard/src/components/DrillBreadcrumb.tsx
  - packages/dashboard/src/components/OpenThreadsPanel.tsx
  - packages/dashboard/src/components/RepoSnapshot.tsx
  - packages/dashboard/src/components/ConfidenceBadge.tsx
  - packages/dashboard/src/components/OpenWorkBadge.tsx
  - packages/dashboard/src/components/RepoBadge.tsx
  - packages/dashboard/src/components/TourPicker.tsx
  - packages/dashboard/src/components/TourChapterNav.tsx
  - packages/dashboard/src/components/TourPlayer.tsx
  - packages/dashboard/src/components/TourStopCard.tsx
  - packages/dashboard/src/components/TemporalGraphView.tsx
autonomous: true
requirements: [DASH-02, DASH-03]
must_haves:
  truths:
    - "Every card/panel uses the base surface contract (bg-slate-900, border-slate-700, rounded-lg, p-4, Heading-size title) and metadata reads at slate-400, not slate-500"
    - "IndexStatusCard shows index freshness + schema version as a legible rail card (DASH-03)"
    - "All badges (attribution, confidence, open-work, repo) use the pill shape + the 5 semantic state colors; no violet/rose one-offs remain"
    - "Tour prose renders at max-w-3xl; primary CTAs use the contract copy (Next stop / Start tour)"
    - "Graph canvas restyled as a frame with a slim helper strip; TemporalGraphView test stays green; typecheck passes"
  artifacts:
    - path: "packages/dashboard/src/components/IndexStatusCard.tsx"
      provides: "Restyled freshness/schema rail card (DASH-03)"
      contains: "slate-900"
    - path: "packages/dashboard/src/components/ConfidenceBadge.tsx"
      provides: "Restyled ConfidenceBadge + AttributionBadge pills using semantic states"
      contains: "rounded-full"
  key_links:
    - from: "leaf components"
      to: "09-UI-SPEC design system"
      via: "Card/badge/typography/color/spacing contract applied uniformly"
      pattern: "border-slate-700"
---

<objective>
Apply the design-system contract (Card/panel treatment, Color ladder, Typography scale, Spacing, Badge treatment) uniformly across the remaining leaf components, badges, tour components, and the graph helper strip. Retire incidental `violet`/`rose` one-offs onto the 5 semantic state colors, raise readable metadata from `slate-500` to `slate-400`, and apply the contract copy. Presentational only — no logic, props, data-flow, or drill-callback change.

Purpose: Completes DASH-02 (all five views cohesive) and DASH-03 (freshness/schema legible). This is the uniform polish pass over everything NOT owned by Plans 02 (EraTimeline/CSS) and 03 (virtualized lists).

Output: Restyled rail cards, filters, drill panels, badges, tour components, and graph frame per 09-UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-dashboard-ui-redesign/09-UI-SPEC.md
@.planning/phases/09-dashboard-ui-redesign/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle Timeline rail cards, filters, and drill panels</name>
  <files>packages/dashboard/src/components/IndexStatusCard.tsx, packages/dashboard/src/components/EraDetailPanel.tsx, packages/dashboard/src/components/CommitFilterBar.tsx, packages/dashboard/src/components/RepoFilterBar.tsx, packages/dashboard/src/components/FileHistoryScrubber.tsx, packages/dashboard/src/components/CommitDetailPanel.tsx, packages/dashboard/src/components/FileHunkView.tsx, packages/dashboard/src/components/DrillBreadcrumb.tsx, packages/dashboard/src/components/OpenThreadsPanel.tsx, packages/dashboard/src/components/RepoSnapshot.tsx</files>
  <read_first>
    - packages/dashboard/src/components/IndexStatusCard.tsx, EraDetailPanel.tsx, CommitFilterBar.tsx, RepoFilterBar.tsx, FileHistoryScrubber.tsx, CommitDetailPanel.tsx, FileHunkView.tsx, DrillBreadcrumb.tsx, OpenThreadsPanel.tsx, RepoSnapshot.tsx (files being modified — read current class strings before editing)
    - 09-UI-SPEC.md §Component Treatment Contract (Card/panel: `bg-slate-900 border border-slate-700 rounded-lg p-4`, Heading title, `gap-6` between rail cards; Interactive states hover/focus/selected), §Color (raise `slate-500`→`slate-400`), §Typography (Heading `text-lg`/600 `tracking-tight`, Body `text-sm`/400, Meta `text-xs`), §Copywriting (loading verbs, empty-pane copy)
    - 09-RESEARCH.md §"Anti-Patterns to Avoid" (wrap/adjust className; do not re-author JSX logic)
  </read_first>
  <action>
    Apply the base Card contract to each rail card and drill panel: surface `bg-slate-900 border border-slate-700 rounded-lg p-4`; card header (title + optional badge) on its own row with `mb-3`, title at `text-lg font-semibold tracking-tight text-slate-100`; body at `text-sm text-slate-300`; metadata (SHAs/dates/authors, monospace where already mono) at `text-xs text-slate-400` — replace every readable `slate-500` with `slate-400` (keep `slate-500` only for genuinely incidental hints). `IndexStatusCard` must keep rendering freshness + schema version from `manifest` (DASH-03) — restyle only. Filters (`CommitFilterBar`, `RepoFilterBar`): inputs/controls get hit targets ≥32px tall, hover `hover:bg-slate-800`, and the `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-400 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950` ring; selected/active state pairs `ring-1 ring-sky-500 bg-slate-800` (never color alone). Evidence/drill links (`DrillBreadcrumb`, `CommitDetailPanel`, `FileHunkView`) use `text-sky-300` with underline on hover; "View commit" / "Open diff" / "Open file history" copy per §Copywriting. Do NOT touch any data fetch, `useDrillStore` selector, props, or event-handler logic — className/copy edits only.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes.
    - `pnpm --dir packages/dashboard test` stays green.
    - Each listed card/panel uses `bg-slate-900 border border-slate-700 rounded-lg p-4` with a Heading-size title; no readable metadata remains at `slate-500` (uses `slate-400`).
    - `IndexStatusCard` still renders freshness + schema version from `manifest`.
    - Interactive controls carry the `focus-visible:ring-sky-400` ring and ≥32px hit targets; drill links use `text-sky-300`.
    - No diff to data fetching, store selectors, props, or handlers.
  </acceptance_criteria>
  <done>Timeline rail cards, filters, and drill panels match the card/color/typography/state contract; DASH-03 freshness/schema legible; typecheck + tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Standardize all badges onto the semantic state palette</name>
  <files>packages/dashboard/src/components/ConfidenceBadge.tsx, packages/dashboard/src/components/OpenWorkBadge.tsx, packages/dashboard/src/components/RepoBadge.tsx</files>
  <read_first>
    - packages/dashboard/src/components/ConfidenceBadge.tsx (exports ConfidenceBadge AND AttributionBadge), OpenWorkBadge.tsx, RepoBadge.tsx (files being modified — read current color classes)
    - 09-UI-SPEC.md §Component Treatment Contract → "Badges" (pill `rounded-full px-2 py-0.5`, Label size 12px, `border`, `inline-flex items-center gap-1`), §Color → "Semantic state colors" table (Complete/In progress/Open/Stale/Error) and "Retire incidental violet/rose one-offs"
    - 09-RESEARCH.md §Anti-Patterns (no new hues; grep confirmed no violet/rose in components today — keep it)
  </read_first>
  <action>
    Restyle every badge to the pill contract: `inline-flex items-center gap-1 rounded-full border px-2 py-0.5 text-xs`. Map each state to the semantic table — Complete: `text-emerald-200 bg-emerald-950/40 border-emerald-700`; In progress: `text-amber-200 bg-amber-950/40 border-amber-700`; Open/info: `text-sky-200 bg-sky-950/40 border-sky-700`; Stale: `text-slate-300 bg-slate-800/60 border-slate-600`; Error/blocked: `text-red-200 bg-red-950/40 border-red-800`. Retire any incidental `violet`/`rose` fill onto the closest of these five (note in the SUMMARY if a specific badge semantics forced an exception). Badge text must be a real label (never color alone). Keep each badge's existing prop/branching logic (confidence level → state, attribution → Degraded on warnings) unchanged — only swap the class strings.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes.
    - `pnpm --dir packages/dashboard test` stays green.
    - All three badge files use `rounded-full ... px-2 py-0.5 text-xs` pills mapped to the 5 semantic state color triplets; no `violet`/`rose` class remains in any component (`grep -rn "violet\|rose-" packages/dashboard/src/components` returns nothing).
    - Badge state→class mapping preserves existing branching logic (ConfidenceBadge/AttributionBadge Degraded path intact).
    - Every badge carries a text label.
  </acceptance_criteria>
  <done>All badges use the unified semantic-state pill treatment with no stray hues; branching logic unchanged; typecheck + tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle Tours components and the Graph canvas frame</name>
  <files>packages/dashboard/src/components/TourPicker.tsx, packages/dashboard/src/components/TourChapterNav.tsx, packages/dashboard/src/components/TourPlayer.tsx, packages/dashboard/src/components/TourStopCard.tsx, packages/dashboard/src/components/TemporalGraphView.tsx</files>
  <read_first>
    - packages/dashboard/src/components/TourPicker.tsx, TourChapterNav.tsx, TourPlayer.tsx, TourStopCard.tsx, TemporalGraphView.tsx (files being modified) + packages/dashboard/src/components/TemporalGraphView.test.tsx (must stay green — asserts store state/roles/onDrillToTimeline, NOT class names)
    - 09-UI-SPEC.md §Per-view body layouts (Tours prose `max-w-3xl`; Graph full-bleed canvas `1fr` min 32rem + slim helper strip), §Component Treatment Contract, §Color, §Copywriting (Tour CTAs "Next stop"/"Previous stop"/"Start tour")
    - 09-RESEARCH.md §"Tab navigation wiring" — TourPlayer/TemporalGraphView drill callbacks (onDrillToTimeline/onDrillToDecisions) are behavior; preserve exactly
  </read_first>
  <action>
    Tours: apply the Card contract to `TourPicker`/`TourChapterNav` (rail cards, `gap-6`); `TourPlayer`/`TourStopCard` render stop prose at a comfortable measure `max-w-3xl` with Body `text-sm text-slate-300` and Heading titles. Primary CTA button (Next stop / Start tour): `bg-sky-600 text-slate-950` (accent primary action) with the focus ring; secondary "Previous stop" as a lower-emphasis button; copy exactly per §Copywriting. Selected chapter/stop uses `ring-1 ring-sky-500 bg-slate-800` + text, never color alone. Graph: restyle the `TemporalGraphView` canvas as a frame — container `1fr` height `min-h-[32rem]` within the content width, a slim helper/legend strip (`text-xs text-slate-400`) above or below the canvas (not a 22rem sidebar). Keep `onDrillToTimeline` and all `@xyflow/react` wiring, node/edge model, and store calls unchanged — className/copy only. Do NOT edit `TemporalGraphView.test.tsx`.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard test` passes — `TemporalGraphView.test.tsx` green (drill callbacks + roles intact).
    - `pnpm --dir packages/dashboard typecheck` passes.
    - Tour stop prose is constrained to `max-w-3xl`; primary CTA uses `bg-sky-600 text-slate-950` + focus ring with copy "Next stop"/"Start tour"; secondary is "Previous stop".
    - Graph canvas region uses `min-h-[32rem]` `1fr` with a slim `text-xs slate-400` helper strip, no 22rem sidebar.
    - `onDrillToTimeline`/`onDrillToDecisions`, `@xyflow/react` wiring, and store calls unchanged; `TemporalGraphView.test.tsx` not edited.
  </acceptance_criteria>
  <done>Tours read comfortably with contract CTAs and the graph is a full-width framed canvas with a helper strip; drill callbacks and the graph test preserved; typecheck + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → local /api | Existing read-only fetches (tours/graph/eras); unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Leaf/badge/graph/tours restyle | accept | No new threat surface — presentational restyle of read-only components over pre-built artifacts (SCALE-02). No new inputs, network calls, writes, `dangerouslySetInnerHTML`, or drill-callback changes introduced. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (no new dependencies permitted); Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `pnpm --dir packages/dashboard typecheck` green; `pnpm --dir packages/dashboard test` green (incl. TemporalGraphView).
- `grep -rn "violet\|rose-" packages/dashboard/src/components` returns nothing.
- Manual (per 09-VALIDATION.md): cards not cramped; metadata `slate-400`; Tours prose `max-w-3xl` with Next/Previous CTAs; graph canvas fills content width with helper strip.
</verification>

<success_criteria>
- Card/badge/typography/color/spacing contract applied uniformly; no violet/rose; metadata `slate-400`.
- IndexStatusCard freshness/schema legible (DASH-03); Tours + Graph views cohesive (DASH-02).
- No logic/props/drill-callback change; typecheck + existing tests (incl. graph) green.
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-ui-redesign/09-04-SUMMARY.md` when done.
</output>
