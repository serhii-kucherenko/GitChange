---
phase: 06-decisions-status-open-work
plan: 05
type: execute
wave: 4
depends_on: ["06-02"]
files_modified:
  - packages/plugin/skills/gitchange-interview/SKILL.md
  - packages/plugin/agents/interview-synthesizer.md
  - packages/core/src/interviews/store.ts
  - packages/core/src/interviews/store.test.ts
  - packages/core/src/interviews/merge.ts
  - packages/core/src/interviews/merge.test.ts
  - packages/core/src/decisions/decisions-io.ts
  - packages/plugin/scripts/merge-interview.ts
  - packages/plugin/schemas/interview-record.schema.json
  - packages/plugin/scripts/generate-schemas.ts
  - packages/plugin/skills/gitchange/SKILL.md
autonomous: true
requirements: [DEC-03, DEC-04]
user_setup: []

must_haves:
  truths:
    - "Maintainer can run /gitchange-interview to confirm or reject a pending decision"
    - "Interview answers persist under .gitchange/interviews/ as JSON"
    - "Confirmed interviews merge interview evidence into decisions.json and set reviewStatus confirmed"
    - "Rejected decisions set reviewStatus rejected without deleting audit trail"
  artifacts:
    - path: "packages/plugin/skills/gitchange-interview/SKILL.md"
      provides: "DEC-03 in-chat interview loop entry point"
    - path: "packages/core/src/interviews/store.ts"
      provides: "Interview JSON persistence"
      exports: ["writeInterviewRecord", "readInterviewRecord"]
    - path: "packages/core/src/interviews/merge.ts"
      provides: "DEC-04 merge into decisions artifact"
      exports: ["mergeInterviewIntoDecisions"]
    - path: "packages/plugin/agents/interview-synthesizer.md"
      provides: "Host LLM spec for structuring maintainer answers"
  key_links:
    - from: "merge.ts"
      to: "decisions.json"
      via: "interview evidence type + reviewStatus update"
      pattern: "type.*interview"
    - from: "gitchange-interview/SKILL.md"
      to: "merge-interview.ts"
      via: "post-answer merge step"
      pattern: "merge-interview"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Maintainer interview loop (DEC-03): present evidence + question in host chat, capture confirm/reject, persist durable lore (DEC-04), merge back into decisions.json with interview evidence type.

Purpose: Closes the tribal-knowledge gap differentiator from PITFALLS #2.
Output: Interview skill, agent spec, store/merge modules, helper script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@.planning/research/PITFALLS.md
@packages/plugin/skills/gitchange/SKILL.md
@packages/core/src/decisions/decisions-io.ts
@packages/core/src/decisions/threshold.ts

<interfaces>
DecisionRecord fields from 06-01:
```typescript
reviewStatus: "pending" | "confirmed" | "rejected";
miningSource: "deterministic" | "agent" | "interview";
```

Interview evidence (06-01):
```typescript
{ type: "interview", path: string, recordedAt: string, excerpt: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interview record store + schema</name>
  <files>packages/core/src/interviews/store.ts, packages/core/src/interviews/store.test.ts, packages/plugin/schemas/interview-record.schema.json, packages/plugin/scripts/generate-schemas.ts</files>
  <behavior>
    - writeInterviewRecord creates .gitchange/interviews/{ulid}.json
    - Path traversal rejected for interview ids
    - Record contains decisionId, question, answer, verdict confirm|reject, recordedAt, maintainer optional
  </behavior>
  <action>
    Define Zod `InterviewRecord` schema. Implement `writeInterviewRecord(gitchangeDir, record)` writing to `.gitchange/interviews/<id>.json` with mkdir recursive; `readInterviewRecord` resolves path only under interviews dir (reject `..`). Add JSON schema to plugin generate-schemas. Interview excerpt max 2000 chars for maintainer free-text answer.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/interviews/store</automated>
  </verify>
  <done>Interview JSON round-trips; path traversal blocked</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge interview into decisions artifact</name>
  <files>packages/core/src/interviews/merge.ts, packages/core/src/interviews/merge.test.ts, packages/core/src/index.ts, packages/plugin/scripts/merge-interview.ts</files>
  <behavior>
    - Confirm verdict sets reviewStatus confirmed, bumps confidence to min 0.7, adds interview evidence
    - Reject verdict sets reviewStatus rejected, does not delete decision row
    - mergeInterviewIntoDecisions re-validates full DecisionsArtifact before write
  </behavior>
  <action>
    Implement `mergeInterviewIntoDecisions(gitchangeDir, interviewId)` reading interview JSON + decisions.json, locating target decision by decisionId, appending interview evidence ref, updating reviewStatus and confidence per verdict, setting miningSource interview when confirmed. On confirm with maintainer opt-in flag in interview record, also write markdown snippet to `docs/interviews/<id>.md` (create docs/interviews if missing) — file write only when user explicitly set `writeToDocs: true` in interview record per DEC-04; default false. Add `merge-interview.ts` CLI script. Export merge from core index.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/interviews/merge</automated>
  </verify>
  <done>Confirmed interview raises confidence above EVD-03 threshold; rejected stays in artifact as rejected</done>
</task>

<task type="auto">
  <name>Task 3: /gitchange-interview skill + agent spec</name>
  <files>packages/plugin/skills/gitchange-interview/SKILL.md, packages/plugin/agents/interview-synthesizer.md, packages/plugin/skills/gitchange/SKILL.md</files>
  <action>
    Create `gitchange-interview` SKILL.md: trigger when user asks to confirm/reject a decision or when /gitchange surfaces pending reviewStatus decisions. Flow: (1) read decision by id from decisions.json, (2) present evidence list + attribution to maintainer, (3) ask confirm/reject + optional rationale, (4) host structures answer via interview-synthesizer agent spec into InterviewRecord JSON, (5) writeInterviewRecord, (6) run merge-interview.ts. Create `interview-synthesizer.md` — output InterviewRecord only, no LLM SDK. Link from main /gitchange skill when decisions have reviewStatus pending. Document that GitChange never auto-commits doc writeback — maintainer commits manually.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/plugin-schemas</automated>
  </verify>
  <done>Interview skill documented; plugin manifest includes new skill directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer answer → interview JSON | User-provided text; redact via existing secret rules if written to docs |
| Doc writeback | Optional; never auto-git-commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | Interview path traversal | mitigate | Store module rejects .. segments |
| T-06-12 | Information Disclosure | Maintainer answer in docs | mitigate | writeToDocs explicit opt-in only |
| T-06-13 | Repudiation | Rejected decision removed | mitigate | Keep rejected rows with reviewStatus |
</threat_model>

<verification>
- Interview store and merge unit tests pass
- merge-interview script runs on fixture
- plugin-schemas test passes with new skill
</verification>

<success_criteria>
- DEC-03 interview loop documented and implementable end-to-end
- DEC-04 durable lore in .gitchange/interviews/ with optional docs export
- Confirmed decisions exceed EVD-03 threshold
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-05-SUMMARY.md` when done
</output>
