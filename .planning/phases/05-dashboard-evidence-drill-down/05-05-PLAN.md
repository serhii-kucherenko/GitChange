---
phase: 05-dashboard-evidence-drill-down
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-04"]
files_modified:
  - packages/core/src/read/file-history.ts
  - packages/core/src/read/file-history.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/file-history.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/components/FileHistoryScrubber.tsx
  - packages/dashboard/src/components/FileHistoryList.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/store/drill.ts
  - tests/integration/dashboard-file-history.test.ts
autonomous: true
requirements: [TIME-03, DASH-04]
user_setup: []

must_haves:
  truths:
    - "User enters a file path and sees chronological touch events from file_changes"
    - "Selecting a file history event jumps drill-down to that commit+file hunk view"
    - "File history list virtualized for files with many touches"
    - "No blame or live git at read time (P5-D-08)"
  artifacts:
    - path: "packages/core/src/read/file-history.ts"
      provides: "getFileHistory path query"
    - path: "packages/dashboard/src/components/FileHistoryScrubber.tsx"
      provides: "Path input + history scrubber UI"
    - path: "packages/server/src/routes/file-history.ts"
      provides: "GET /api/files/:path/history"
  key_links:
    - from: "FileHistoryScrubber"
      to: "/api/files"
      via: "encoded path segment"
      pattern: "files/.*/history"
    - from: "file-history.ts"
      to: "file_changes"
      via: "WHERE path = ? ORDER BY committedAt"
      pattern: "file_changes"
---

## Phase Goal

**As a** new hire or maintainer, **I want to** explore project history interactively with era→commit→file drill-down on localhost, **so that** I can understand how the codebase evolved with linked evidence without hunting git log.

<objective>
File-centric history scrubber (TIME-03): query indexed file_changes by path, virtualized event list, jump into commit/file hunk drill-down (TIME-02).

Purpose: Alternative entry point to history — start from a file path instead of era or commit list.
Output: GET /api/files/:path/history, FileHistoryScrubber + FileHistoryList components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@.planning/phases/05-dashboard-evidence-drill-down/05-04-SUMMARY.md

<interfaces>
FileHistoryEvent:
- commitSha, committedAt, changeType, summary (from join commits)
- path, oldPath (for renames)

getFileHistory(gitchangeDir, path, { limit?, cursor? }) → { events, nextCursor }

GET /api/files/:path/history — path URL-encoded; reject path containing .. segments.

Optional: doc snapshot excerpt inline for doc paths (P5-D-08 discretion — include if path matches doc_snapshots row at that commit).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getFileHistory core + API route</name>
  <files>packages/core/src/read/file-history.ts, packages/core/src/read/file-history.test.ts, packages/core/src/index.ts, packages/server/src/routes/file-history.ts, packages/server/src/app.ts, tests/integration/dashboard-file-history.test.ts</files>
  <behavior>
    - Returns events oldest-first or newest-first (pick newest-first for scrubber UX; document in API)
    - Rename events include oldPath when present
    - Unknown path returns empty events array not 404
    - Path with .. rejected at API with 400
    - Integration: known fixture path returns >= 2 events across commits
  </behavior>
  <action>
    Implement getFileHistory joining file_changes + commits on sha, filter by exact path match (also match oldPath for rename discovery optional). Cursor pagination same pattern as listCommits.

    Route file-history.ts: decode path param safely; validate no directory traversal.

    Export from core; mount route at /api/files/* in app.ts.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/file-history.test.ts tests/integration/dashboard-file-history.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>File history API returns indexed touch timeline for path</done>
</task>

<task type="auto">
  <name>Task 2: FileHistoryScrubber UI + drill jump</name>
  <files>packages/dashboard/src/components/FileHistoryScrubber.tsx, packages/dashboard/src/components/FileHistoryList.tsx, packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/store/drill.ts</files>
  <action>
    Add FileHistoryScrubber panel to DashboardLayout (tab or collapsible section "File history"):
    - Path text input with autocomplete suggestions from snapshot highlights.topChurnFiles paths (optional enhancement)
    - Submit loads /api/files/:path/history via react-query

    FileHistoryList: @tanstack/react-virtual vertical list of events (DASH-04); each row shows date, changeType, commit summary short sha.

    On row click: setSelectedCommitSha + setSelectedFilePath in drill store → opens CommitDetailPanel + FileHunkView from Plan 05-04.

    Empty state when path never touched. Loading/error states consistent with CommitList.
  </action>
  <verify>
    <automated>pnpm build --filter=@gitchange/dashboard && pnpm vitest run tests/integration/dashboard-file-history.test.ts 2>&1 | tail -3 | grep -q "passed"</automated>
  </verify>
  <done>User scrubs file history and jumps to hunk evidence for any event</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Path param → SQL | Path could attempt traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | I | File path param | mitigate | Reject .. and null bytes; exact match query; max path length 1024 |
</threat_model>

<verification>
- pnpm vitest run packages/core/src/read/file-history.test.ts tests/integration/dashboard-file-history.test.ts
- File scrubber → click event → hunk panel shows patch
</verification>

<success_criteria>
- TIME-03: file-centric history scrubber from indexed file_changes only
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-05-SUMMARY.md` when done
</output>
