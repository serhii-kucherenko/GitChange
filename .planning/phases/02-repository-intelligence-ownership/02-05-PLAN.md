---
phase: 02-repository-intelligence-ownership
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - packages/core/src/schema/manifest.ts
  - packages/core/src/schema/manifest.test.ts
  - packages/core/src/intelligence/compute.ts
  - packages/core/src/index/full.ts
  - packages/core/src/index/incremental.ts
  - packages/core/src/index/types.ts
  - packages/core/src/verify/intelligence-integrity.ts
  - packages/core/src/verify/intelligence-snapshot.ts
  - tests/golden/intelligence.test.ts
  - tests/golden/helpers.ts
autonomous: true
requirements: [CONT-01, CONT-03, CONT-04]
user_setup: []

must_haves:
  truths:
    - "Golden test locks intelligence output counts for BASIC_SCENARIO + OWNSHIP_SCENARIO"
    - "Intelligence narrative records pass referential evidence integrity (EVD-01 pattern)"
    - "manifest.json records intelligence checkpoint: intelligenceComputedAt, intelligenceHeadSha"
    - "indexFull optionally triggers computeIntelligence when rebuildIntelligence:true (default false for backward compat)"
    - "Full core test suite remains green (75+ tests)"
  artifacts:
    - path: "tests/golden/intelligence.test.ts"
      provides: "Golden intelligence fixture gate"
    - path: "packages/core/src/verify/intelligence-integrity.ts"
      provides: "Evidence integrity for intelligence exports"
    - path: "packages/core/src/verify/intelligence-snapshot.ts"
      provides: "Locked intelligence snapshot counts"
  key_links:
    - from: "packages/core/src/index/full.ts"
      to: "packages/core/src/intelligence/compute.ts"
      via: "optional post-index hook"
      pattern: "computeIntelligence|rebuildIntelligence"
    - from: "tests/golden/intelligence.test.ts"
      to: ".gitchange/intelligence.json"
      via: "index + compute + assert"
      pattern: "intelligence"
---

## Phase Goal

**As a** maintainer or new team member, **I want to** see who changed what through ownership timelines and contributor expertise derived from git history, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Harden Phase 2 with **golden fixtures**, **evidence integrity verification**, **manifest intelligence checkpoint**, and optional **post-index intelligence hook**. Closes the phase with the same trust bar as Phase 1 golden tests.

Purpose: Prevents regression as Phases 3–4 consume intelligence artifacts. Proves all four roadmap success criteria are test-gated.
Output: Golden tests, verify helpers, manifest extension, optional index integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repository-intelligence-ownership/02-RESEARCH.md
@.planning/phases/01-index-foundation/01-08-SUMMARY.md
@packages/core/src/verify/evidence-integrity.ts

<interfaces>
```typescript
// Phase 1 pattern
export function checkEvidenceIntegrity(dbPath: string): { ok: boolean; errors: string[] };

// Manifest (extend additively)
export const ManifestSchema = z.object({
  // ...existing fields
  intelligenceComputedAt: z.string().optional(),
  intelligenceHeadSha: z.string().optional(),
  intelligenceSchemaVersion: z.string().optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intelligence evidence integrity + snapshot verify helpers</name>
  <files>packages/core/src/verify/intelligence-integrity.ts, packages/core/src/verify/intelligence-snapshot.ts</files>
  <action>
    `checkIntelligenceIntegrity(gitchangeDir)`: Load intelligence.json; for every evidence[] entry, verify commit SHA in commits table and file refs in file_changes (reuse logic from evidence-integrity.ts without drizzle import from tests — keep in packages/core/src/verify/).

    `collectIntelligenceSnapshot(gitchangeDir)`: Return counts — churnFiles, coChangeEdges, ownershipFiles, eraBoundaries, expertiseTopics, attributionConfidence.

    Export from @gitchange/core if needed for golden tests (mirror Phase 1 verify pattern).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/verify 2>/dev/null || pnpm vitest run packages/core/src/intelligence/compute.test.ts</automated>
  </verify>
  <done>Verify helpers exist and unit-testable with corrupted fixture</done>
</task>

<task type="auto">
  <name>Task 2: Golden intelligence tests</name>
  <files>tests/golden/intelligence.test.ts, tests/golden/helpers.ts</files>
  <action>
    Add golden helper `indexAndCompute(repo)` → runs indexFull + computeIntelligence.

    Test 1: BASIC_SCENARIO — snapshot counts locked (document expected values in intelligence-snapshot.ts constants); checkIntelligenceIntegrity ok.

    Test 2: OWNSHIP_SCENARIO — ownership top author assertion; ignore-revs case if fixture includes it.

    Test 3: intelligence.json sections present — churn, coChange, ownership, eraSignals, eraOwnership, expertise.

    Test 4: No secret leakage — grep intelligence.json for MESSAGE_SECRET/DOC_SECRET prefixes from fixtures (must not appear).

    Follow Phase 1 pattern: no direct drizzle-orm imports in tests/golden.
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/intelligence.test.ts</automated>
  </verify>
  <done>Golden intelligence tests pass and lock expected snapshot</done>
</task>

<task type="auto">
  <name>Task 3: Manifest checkpoint + optional index hook + full suite gate</name>
  <files>packages/core/src/schema/manifest.ts, packages/core/src/schema/manifest.test.ts, packages/core/src/intelligence/compute.ts, packages/core/src/index/full.ts, packages/core/src/index/incremental.ts, packages/core/src/index/types.ts</files>
  <action>
    Extend ManifestSchema with optional intelligenceComputedAt, intelligenceHeadSha, intelligenceSchemaVersion (additive — D-03). computeIntelligence updates manifest after successful export.

    Add IndexOptions.rebuildIntelligence?: boolean (default false). When true, indexFull/indexIncremental call computeIntelligence after manifest write.

    Update manifest tests for new optional fields round-trip.

    Run full suite: `pnpm vitest run packages/core/src tests/golden` — all green.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src tests/golden</automated>
  </verify>
  <done>Phase 2 complete — 75+ tests pass; manifest records intelligence checkpoint; golden gate green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden tests → SQLite/json artifacts | CI gate prevents trust regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | intelligence.json secrets | mitigate | Golden grep test for fixture secret prefixes |
| T-02-10 | Tampering | snapshot drift | mitigate | Locked counts in intelligence-snapshot.ts |
</threat_model>

<verification>
- `pnpm vitest run packages/core/src tests/golden` all pass
- checkIntelligenceIntegrity returns ok on BASIC_SCENARIO
</verification>

<success_criteria>
- All Phase 2 roadmap success criteria covered by automated tests
- Phase 1 golden tests still pass
</success_criteria>

<output>
Create `.planning/phases/02-repository-intelligence-ownership/02-05-SUMMARY.md` when done
</output>
