---
phase: 06-decisions-status-open-work
plan: 06
type: execute
wave: 5
depends_on: ["06-04", "06-05"]
files_modified:
  - packages/dashboard/src/utils/confidence.ts
  - packages/dashboard/src/utils/confidence.test.ts
  - packages/dashboard/src/utils/open-work-match.ts
  - packages/dashboard/src/utils/open-work-match.test.ts
  - packages/dashboard/src/components/ConfidenceBadge.tsx
  - packages/dashboard/src/components/EraTimeline.tsx
  - packages/dashboard/src/components/CommitList.tsx
  - packages/dashboard/src/components/EraDetailPanel.tsx
  - packages/plugin/schemas/status-query-response.schema.json
  - packages/plugin/scripts/generate-schemas.ts
  - packages/plugin/skills/gitchange/SKILL.md
  - packages/core/src/verify/decisions-integrity.ts
  - packages/core/src/verify/decisions-integrity.test.ts
  - packages/core/src/verify/decisions-snapshot.ts
  - packages/core/src/cli/validate.ts
  - packages/cli/src/commands/validate.ts
  - tests/golden/decisions.test.ts
  - tests/fixtures/decisions/basic-scenario-decisions.json
  - tests/fixtures/decisions/basic-scenario-open-work.json
  - tests/integration/decisions-dashboard-e2e.test.ts
  - tests/integration/plugin-schemas.test.ts
autonomous: true
requirements: [STAT-03, STAT-04, EVD-03, CONT-02]
user_setup: []

must_haves:
  truths:
    - "Era timeline and commit list show OpenWorkBadge when related incomplete threads exist"
    - "matchOpenWorkToSurface exported for Phase 7 tour player integration"
    - "Agent status query schema returns evidence[], confidence, and EVD03_GAP_MESSAGE when below threshold"
    - "/gitchange Phase 4 documents how to answer migration progress questions with citations"
    - "Golden decisions fixture gate passes evidence integrity on BASIC_SCENARIO"
  artifacts:
    - path: "packages/dashboard/src/utils/open-work-match.ts"
      provides: "STAT-03 badge matching + Phase 7 hook"
      exports: ["matchOpenWorkToSurface"]
    - path: "packages/plugin/schemas/status-query-response.schema.json"
      provides: "STAT-04 agent tool contract"
    - path: "tests/golden/decisions.test.ts"
      provides: "EVD-04 extension for decisions/open-work"
    - path: "packages/core/src/verify/decisions-integrity.ts"
      provides: "Referential integrity for decision evidence"
      exports: ["checkDecisionsIntegrity"]
  key_links:
    - from: "EraTimeline"
      to: "open-work.json via snapshot or /api/open-work"
      via: "matchOpenWorkToSurface(eraId)"
      pattern: "OpenWorkBadge"
    - from: "/gitchange SKILL Phase 4"
      to: "status-query-response.schema.json"
      via: "status query instructions"
      pattern: "status-query"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Phase 6 completion gate: timeline/commit inline badges (STAT-03), agent status query contract (STAT-04), decision confidence model replacing P5 heuristic for decisions, CONT-02 attribution display, golden fixtures, gitchange validate extension, full E2E.

Purpose: Proves all Phase 6 success criteria together; exports tour hook for Phase 7.
Output: Badge wiring, status schema, integrity checks, golden + E2E tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@.planning/phases/05-dashboard-evidence-drill-down/05-06-SUMMARY.md
@packages/dashboard/src/utils/confidence.ts
@packages/dashboard/src/components/OpenWorkBadge.tsx
@packages/core/src/verify/semantic-integrity.ts
@packages/cli/src/commands/validate.ts

<interfaces>
```typescript
export function matchOpenWorkToSurface(
  threads: OpenWorkThreadSummary[],
  surface: { eraId?: string; commitSha?: string; path?: string }
): OpenWorkThreadSummary[];
export const EVD03_GAP_MESSAGE = "No recorded decision found";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decision confidence model + open-work badge matching</name>
  <files>packages/dashboard/src/utils/confidence.ts, packages/dashboard/src/utils/confidence.test.ts, packages/dashboard/src/utils/open-work-match.ts, packages/dashboard/src/utils/open-work-match.test.ts, packages/dashboard/src/components/ConfidenceBadge.tsx, packages/dashboard/src/components/EraTimeline.tsx, packages/dashboard/src/components/CommitList.tsx, packages/dashboard/src/components/EraDetailPanel.tsx</files>
  <behavior>
    - decisionConfidenceToLevel maps verified|inferred_high|medium|low from numeric confidence + reviewStatus
    - matchOpenWorkToSurface returns threads where events include commitSha or paths overlap era window
    - Era timeline items show OpenWorkBadge when match non-empty
  </behavior>
  <action>
    Extend `confidence.ts` with `decisionConfidenceToLevel(confidence, reviewStatus, evidenceCount)` replacing evidence-count-only heuristic for **decision surfaces** (era claims keep existing heuristic per P5-D-05). Add `open-work-match.ts` with `matchOpenWorkToSurface` per P6-D-07 — match by commitSha in thread events, path prefix overlap, or era time window overlap. Wire OpenWorkBadge into EraTimeline custom item template, CommitList row suffix when commit in active thread, EraDetailPanel header when era-linked threads exist. Export match utility from dashboard package index if present for Phase 7 reuse.
  </action>
  <verify>
    <automated>pnpm vitest run packages/dashboard/src/utils/confidence packages/dashboard/src/utils/open-work-match</automated>
  </verify>
  <done>Badge appears on fixture thread-linked commit; match utility unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: STAT-04 agent status schema + /gitchange Phase 4</name>
  <files>packages/plugin/schemas/status-query-response.schema.json, packages/plugin/scripts/generate-schemas.ts, packages/plugin/skills/gitchange/SKILL.md, tests/integration/plugin-schemas.test.ts</files>
  <action>
    Define Zod `StatusQueryResponse`: `{ query, answer?, gap?, confidence, evidence[], relatedThreads[], relatedDecisions[] }` where gap equals EVD03_GAP_MESSAGE when no qualifying decision/thread. Generate JSON schema. Add **Phase 4 — Status queries** to /gitchange SKILL.md: when user asks migration progress or "what's in flight", read open-work.json + decisions.json, call isBelowEvidenceThreshold, return StatusQueryResponse shape with commit/doc evidence citations and confidence — never fabricate when below threshold. Extend plugin-schemas test to assert status schema registers. STAT-04 satisfied via host chat following skill — no embedded LLM runtime (PLUG-05).
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/plugin-schemas</automated>
  </verify>
  <done>status-query-response.schema.json exists; skill documents evidence-first status answers</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Golden gate + validate + Phase 6 E2E</name>
  <files>packages/core/src/verify/decisions-integrity.ts, packages/core/src/verify/decisions-integrity.test.ts, packages/core/src/verify/decisions-snapshot.ts, packages/cli/src/commands/validate.ts, tests/golden/decisions.test.ts, tests/fixtures/decisions/basic-scenario-decisions.json, tests/fixtures/decisions/basic-scenario-open-work.json, tests/integration/decisions-dashboard-e2e.test.ts</files>
  <behavior>
    - checkDecisionsIntegrity verifies every evidence ref resolves in SQLite
    - Golden test locks decision and thread counts on BASIC_SCENARIO
    - gitchange validate fails when decisions.json missing after semantic pipeline flag
    - E2E: decisions list → thread select → commit drill → badge endpoint
  </behavior>
  <action>
    Implement `checkDecisionsIntegrity` mirroring semantic-integrity pattern for decisions + open-work artifacts. Add `collectDecisionsSnapshot` with locked counts. Create golden fixtures for BASIC_SCENARIO decisions/open-work (bind in test like semantic fixture). Extend `gitchange validate` to run decisions integrity when decisions.json present. Write `decisions-dashboard-e2e.test.ts` chaining: index → bind fixture decisions → runDecisionsPipeline → API decisions + open-work + gap case + thread drill SHA. Display CONT-02 attribution in DecisionsPanel detail if not done in 06-04 — add attribution row showing name + rationale with evidence link.
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/decisions tests/integration/decisions-dashboard-e2e packages/core/src/verify/decisions-integrity</automated>
  </verify>
  <done>Golden gate passes; Phase 6 E2E green on Node 22.x; validate covers decisions integrity</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent status response → user | Must use EVD03_GAP_MESSAGE; schema enforces gap field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Spoofing | Agent cites wrong SHA | mitigate | Skill requires evidence from artifacts only |
| T-06-15 | Tampering | Golden snapshot drift | mitigate | Locked counts in collectDecisionsSnapshot |
</threat_model>

<verification>
- All Phase 6 requirement IDs covered by combined plans 01–06
- Golden decisions test passes
- decisions-dashboard-e2e passes
- plugin-schemas includes status-query schema
</verification>

<success_criteria>
- STAT-03 badges visible on timeline and commit surfaces
- STAT-04 agent contract documented with evidence + confidence
- EVD-03 and CONT-02 verified in E2E
- Phase 6 ready for Phase 7 tour integration via matchOpenWorkToSurface
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-06-SUMMARY.md` when done
</output>
