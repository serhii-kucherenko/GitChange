---
phase: 07-guided-tours-onboarding-ux
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/core/src/read/tours.ts
  - packages/core/src/read/tours.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/tours.ts
  - packages/server/src/routes/tours.test.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/types.ts
  - packages/dashboard/src/api/client.ts
autonomous: true
requirements: [TOUR-04]
user_setup: []

must_haves:
  truths:
    - "GET /api/tours returns tour list with id, kind, title, roleTag, topicKey for picker"
    - "GET /api/tours/:tourId returns full tour with chapters and stops for player"
    - "API returns 404 when tours.json missing; no live git reads"
    - "Dashboard client types mirror API shapes for react-query"
  artifacts:
    - path: "packages/core/src/read/tours.ts"
      provides: "listTours / getTourById read helpers"
      exports: ["listTours", "getTourById"]
    - path: "packages/server/src/routes/tours.ts"
      provides: "Hono tour routes"
      exports: ["toursRoutes"]
  key_links:
    - from: "packages/server/src/routes/tours.ts"
      to: "packages/core/src/read/tours.ts"
      via: "readToursArtifact"
      pattern: "getTourById|listTours"
    - from: "packages/dashboard/src/api/client.ts"
      to: "/api/tours"
      via: "fetchTours / fetchTour"
      pattern: "api/tours"
---

<objective>
Read path and thin API for tour artifacts: core list/detail helpers and Hono routes serving pre-built tours.json to the dashboard (SCALE-02). Enables tour player to load stops with evidence refs for TOUR-04 drill-down.

Purpose: Separates server read layer from UI so 07-04 can parallelize after schemas exist.
Output: read/tours.ts, GET /api/tours routes, dashboard API client extensions, route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guided-tours-onboarding-ux/07-RESEARCH.md
@packages/server/src/routes/decisions.ts
@packages/core/src/read/decisions.ts
@packages/server/src/app.ts
@packages/dashboard/src/api/client.ts

<interfaces>
From packages/server/src/routes/decisions.ts pattern:
- Resolve gitchangeDir from server paths helper
- Return JSON 404 with message when artifact missing
- No es-git imports

From packages/dashboard/src/api/client.ts:
- Existing fetchDecisions, fetchOpenWork patterns with react-query keys
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core read helpers for tours</name>
  <files>packages/core/src/read/tours.ts, packages/core/src/read/tours.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - listTours: returns { tours: Array<{ id, kind, title, description, roleTag?, topicKey?, chapterCount, stopCount }>, defaultTourId }
    - getTourById: returns full Tour or null
    - Strip internal fields if any; do not mutate artifact
  </behavior>
  <action>
    Implement read helpers using readToursArtifact from 07-01. listTours computes chapter/stop counts. Export from core index. Tests use fixture ToursArtifact written to tmp dir.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/read/tours.test.ts</automated>
  </verify>
  <done>listTours and getTourById return expected shapes from fixture tours.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hono routes + dashboard API client</name>
  <files>packages/server/src/routes/tours.ts, packages/server/src/routes/tours.test.ts, packages/server/src/app.ts, packages/dashboard/src/types.ts, packages/dashboard/src/api/client.ts</files>
  <behavior>
    - GET /api/tours → 200 list or 404 { error: "tours not found" }
    - GET /api/tours/:tourId → 200 full tour or 404
    - Client: fetchTours(), fetchTour(tourId), query keys tours.list / tours.detail(tourId)
    - Dashboard types: TourSummary, TourDetail, TourStop, TourChapter duplicated locally (no @gitchange/core in client bundle per P5 pattern)
  </behavior>
  <action>
    Mirror decisions route structure. Mount toursRoutes on app.ts under /api/tours. Add route tests with supertest or existing server test harness. Extend dashboard types.ts and client.ts. Server test asserts zero es-git imports in tours route file (SCALE-02).
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/server/src/routes/tours.test.ts</automated>
  </verify>
  <done>API returns tour list and detail from fixture; client types compile</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/tours | tourId path param is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | GET /api/tours/:tourId | mitigate | tourId validated as string; 404 if not in artifact — no path traversal |
| T-07-07 | Information Disclosure | tour API | accept | Localhost-only; same as other artifact routes |
</threat_model>

<verification>
pnpm exec vitest run packages/core/src/read/tours.test.ts packages/server/src/routes/tours.test.ts
</verification>

<success_criteria>
- GET /api/tours and GET /api/tours/:id work on fixture
- Dashboard client can fetch tour list and detail
- No es-git in server tour routes
</success_criteria>

<output>
Create `.planning/phases/07-guided-tours-onboarding-ux/07-03-SUMMARY.md` when done
</output>
