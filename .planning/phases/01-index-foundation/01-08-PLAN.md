---
phase: 01-index-foundation
plan: 08
type: execute
wave: 7
depends_on: [01-05, 01-06, 01-07]
files_modified:
  - tests/golden/evidence-integrity.test.ts
  - tests/golden/secret-leakage.test.ts
  - tests/golden/ingestion-snapshot.test.ts
  - tests/golden/dogfood.test.ts
  - packages/core/src/verify/evidence-integrity.ts
autonomous: true
requirements: [EVD-04, SCALE-03]
user_setup: []

must_haves:
  truths:
    - "A golden test indexes the synthetic fixture and asserts every evidence[].sha exists in commits and every {path,commitSha} exists in file_changes"
    - "A secret-leakage test greps the built fixture SQLite and fails if AKIA/ghp_/sk-/BEGIN ... PRIVATE KEY appears"
    - "A .gitchangeignore-matched path stores no content in the built index"
    - "An ingestion snapshot test locks commit/author/file-change counts for the canonical fixture"
    - "A dogfood test can index GitChange's own repo and produce a schema-valid manifest (opt-in / slower)"
  artifacts:
    - path: "tests/golden/evidence-integrity.test.ts"
      provides: "Referential evidence-integrity golden test (EVD-04)"
      contains: "evidence"
    - path: "tests/golden/secret-leakage.test.ts"
      provides: "Secret-grep golden test on built SQLite (Pitfall 2)"
      contains: "AKIA"
    - path: "packages/core/src/verify/evidence-integrity.ts"
      provides: "Reusable evidence-resolution checker"
      contains: "evidence"
  key_links:
    - from: "tests/golden/evidence-integrity.test.ts"
      to: "packages/core/src/verify/evidence-integrity.ts"
      via: "checkEvidenceIntegrity(db)"
      pattern: "checkEvidenceIntegrity|evidence"
    - from: "tests/golden/secret-leakage.test.ts"
      to: ".gitchange/index.sqlite"
      via: "grep raw DB bytes for secret prefixes"
      pattern: "sqlite|readFileSync"
---

<objective>
Prove the phase's trust guarantees with golden fixtures: evidence-link **referential** integrity (not just shape), the required secret-leakage grep on the built SQLite, ignored-path content exclusion, an ingestion count snapshot, and an opt-in dogfood run against GitChange's own repo. This is the SCALE-03 TDD safety net and the EVD-04 evidence-integrity gate.

Purpose: EVD-04 (evidence-link integrity) and SCALE-03 (core ingestion covered by golden fixtures), plus the mandatory secret-grep test (RESEARCH Pitfall 2, D-12).
Output: `packages/core/src/verify/evidence-integrity.ts` (reusable checker) and `tests/golden/*` covering integrity, secret leakage, ignored paths, ingestion snapshot, and dogfood.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-index-foundation/SKELETON.md
@.planning/phases/01-index-foundation/01-RESEARCH.md
@.planning/phases/01-index-foundation/01-PATTERNS.md
@.planning/phases/01-index-foundation/01-CONTEXT.md
@.planning/phases/01-index-foundation/01-VALIDATION.md

<interfaces>
<!-- Consumes: indexFull (P05), fixture builder + BASIC_SCENARIO (P01), schema/drizzle (P02). -->
```typescript
// verify/evidence-integrity.ts
export interface IntegrityReport { ok: boolean; danglingCommitRefs: string[]; danglingFileRefs: Array<{path:string;commitSha:string}>; }
export function checkEvidenceIntegrity(db: DrizzleDb): IntegrityReport;
// For every narrative record's evidence[]: commit sha ∈ commits table; {path,commitSha} ∈ file_changes.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evidence-integrity checker + golden test</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 350–353 (Pitfall 4 — referential, not shape), 71–74 (EVD-01/EVD-04)
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 358–368 (evidence contract, referential integrity)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-12, D-16
  </read_first>
  <files>packages/core/src/verify/evidence-integrity.ts, tests/golden/evidence-integrity.test.ts</files>
  <behavior>
    - After `indexFull(BASIC_SCENARIO)`, `checkEvidenceIntegrity(db)` returns `ok:true` with empty dangling lists.
    - If a file_changes evidence ref is manually corrupted to point at a non-existent sha, the checker returns `ok:false` and lists it (proves the test detects real breakage, not just shape).
  </behavior>
  <action>
    `verify/evidence-integrity.ts`: `checkEvidenceIntegrity(db)` reads all narrative-ready rows (`file_changes`, `doc_snapshots`), parses each `evidenceJson`, and asserts every `{type:'commit',sha}` resolves in `commits` and every `{type:'file',path,commitSha}` resolves in `file_changes`; collect dangling refs. `evidence-integrity.test.ts`: build + index `BASIC_SCENARIO` into a temp `.gitchange/`, assert `ok:true`; then corrupt one evidence ref in a copied DB and assert `ok:false` with the dangling ref listed. Deterministic post-index query (cheap — RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/evidence-integrity</automated>
  </verify>
  <acceptance_criteria>
    - Clean index → integrity `ok:true`.
    - Corrupted evidence ref → `ok:false` with the specific dangling ref reported.
    - Checker validates resolution, not merely presence of an `evidence[]` field.
  </acceptance_criteria>
  <done>Evidence-link referential integrity is enforced by a golden test (EVD-04, D-12).</done>
</task>

<task type="auto">
  <name>Task 2: secret-leakage + ignored-path + ingestion-snapshot golden tests</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 335–339 (Pitfall 2 — required secret grep), 552–555 (privacy tests)
    - `.planning/phases/01-index-foundation/01-VALIDATION.md` (01-08-01 golden+grep row)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-07, D-08, D-11
  </read_first>
  <files>tests/golden/secret-leakage.test.ts, tests/golden/ingestion-snapshot.test.ts</files>
  <action>
    `secret-leakage.test.ts`: index `BASIC_SCENARIO` (which includes simulated secrets), then read the built `.gitchange/index.sqlite` bytes (`fs.readFileSync`) AND query all text columns, asserting NONE contain `AKIA`, `ghp_`, `sk-`, or `-----BEGIN RSA PRIVATE KEY` / `-----BEGIN PRIVATE KEY` (Pitfall 2 — required, not optional). Also assert `secret_findings` recorded ≥1 finding (metadata retained) and the corresponding commit still exists (D-09 — commit not dropped). `ingestion-snapshot.test.ts`: lock the canonical fixture's expected counts — number of commits, authors, merges (parent_count 2), renames (change_type 'renamed'), doc_snapshots, and that at least one `.gitchangeignore`-matched path row has `content` null / `contentIgnored` true with no stored body (D-07). Use exact expected numbers derived from `BASIC_SCENARIO` so drift fails the test.
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/secret-leakage tests/golden/ingestion-snapshot</automated>
  </verify>
  <acceptance_criteria>
    - Built fixture SQLite contains none of the secret prefixes (raw bytes + text columns).
    - `secret_findings` retains metadata and the secret-bearing commit is still indexed (D-09).
    - Ignored-path row stores no content; ingestion counts match the locked canonical expectations.
  </acceptance_criteria>
  <done>Secret leakage is impossible in the built index and ingestion output is snapshot-locked (PRIV-02, D-07/D-08).</done>
</task>

<task type="auto">
  <name>Task 3: dogfood integration test (opt-in) + full-suite gate</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-11 (dogfood: required locally, optional in CI)
    - `.planning/phases/01-index-foundation/01-VALIDATION.md` (Manual-Only Verifications — dogfood)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 82–83 (dogfood step)
  </read_first>
  <files>tests/golden/dogfood.test.ts</files>
  <action>
    `dogfood.test.ts`: index the GitChange repo root itself into a temp `.gitchange/`, assert a schema-valid `manifest.json` is produced with `lastIndexedCommit === HEAD` and `commits` count > 0, and run `checkEvidenceIntegrity` → `ok:true`. Gate it behind an env flag (e.g. `describe.skipIf(!process.env.GITCHANGE_DOGFOOD)`) so it runs locally/pre-milestone but is optional in CI initially (D-11). Keep it fast enough to be a smoke test (this repo is small). Confirm the whole suite passes via `turbo test --filter=@gitchange/core` as the phase gate.
  </action>
  <verify>
    <automated>GITCHANGE_DOGFOOD=1 pnpm vitest run tests/golden/dogfood && turbo test --filter=@gitchange/core</automated>
  </verify>
  <acceptance_criteria>
    - Dogfood run indexes this repo, produces a schema-valid manifest (HEAD == lastIndexedCommit), integrity ok.
    - Test is opt-in via env flag (skipped by default in CI).
    - Full `@gitchange/core` suite is green (phase gate).
  </acceptance_criteria>
  <done>GitChange indexes its own repo cleanly and the full golden suite passes (SCALE-03, D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built index → verification | Tests are the last line asserting no secret leaked and evidence resolves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure (V8) | built SQLite index | mitigate | Required secret-grep golden test on raw DB bytes + text columns fails the build if any secret prefix appears (Pitfall 2). |
| T-08-02 | Tampering (integrity) | evidence refs | mitigate | Referential integrity checker fails on dangling commit/file refs (EVD-04); corruption-detection test proves it catches real breakage. |
| T-08-03 | Repudiation | dropped commits | mitigate | Snapshot test asserts secret-bearing commits remain indexed with findings metadata (D-09). |
</threat_model>

<verification>
- `pnpm vitest run tests/golden/*` green.
- `turbo test --filter=@gitchange/core` green (full phase gate).
- Secret-grep, evidence-integrity, ignored-path, ingestion-snapshot, and dogfood all pass.
</verification>

<success_criteria>
- Golden fixtures validate evidence-link referential integrity (EVD-04, D-12).
- Secret-leakage grep on the built index passes; ignored paths store no content (PRIV-02, D-07/D-08).
- Ingestion output snapshot-locked; dogfood indexes GitChange's own repo (SCALE-03, D-11).
</success_criteria>

<output>
Create `.planning/phases/01-index-foundation/01-08-SUMMARY.md` when done.
</output>
