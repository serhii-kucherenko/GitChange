---
phase: 08-hardening-scale-multi-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/artifacts/db.ts
  - packages/core/src/schema/manifest.ts
  - packages/core/src/schema/manifest.test.ts
  - packages/core/src/index/process-commit.ts
  - packages/core/src/index/worker.ts
  - packages/core/src/index/worker-pool.ts
  - packages/core/src/index/worker-pool.test.ts
  - packages/core/src/index/full.ts
  - packages/core/src/index/incremental.ts
  - packages/core/src/index/types.ts
  - packages/core/src/index.ts
  - packages/cli/src/commands/index.ts
  - tests/fixtures/scale-repo-builder.ts
  - tests/scale/index-benchmark.test.ts
autonomous: false
requirements: [SCALE-01]
user_setup: []

must_haves:
  truths:
    - "Full index of 10k-commit synthetic fixture completes under 120s in CI with piscina enabled"
    - "Incremental re-index after fixture processes only new commits (two-phase architecture preserved)"
    - "CLI index prints progress every 500 commits with rate and elapsed time"
    - "manifest records lastIndexDurationMs after full index"
  artifacts:
    - path: "packages/core/src/index/worker-pool.ts"
      provides: "Piscina pool orchestrating per-commit workers"
      exports: ["createIndexWorkerPool", "processCommitBatch"]
    - path: "packages/core/src/index/worker.ts"
      provides: "Worker entry: open repo, process commit, return serializable rows"
    - path: "tests/scale/index-benchmark.test.ts"
      provides: "10k scale gate; optional 100k via GITCHANGE_SCALE_100K"
  key_links:
    - from: "packages/core/src/index/full.ts"
      to: "packages/core/src/index/worker-pool.ts"
      via: "batch SHA stream from walkFromHead"
      pattern: "createIndexWorkerPool|processCommitBatch"
    - from: "packages/core/src/index/worker.ts"
      to: "packages/core/src/ingestion/diff.ts"
      via: "worker-local es-git openRepository"
      pattern: "openRepository|diffCommit"
---

<objective>
SCALE-01 hardening: piscina worker pool for per-commit processing, SQLite pragma tuning, CLI progress reporting, manifest duration field, and 10k-commit benchmark gate proving the incremental two-phase architecture scales.

Purpose: Unblocks 100k+ monorepo indexing without violating SCALE-02 (index once, read many).
Output: worker pool, benchmark fixture, progress UX, optional 100k local benchmark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@packages/core/src/index/full.ts
@packages/core/src/index/incremental.ts
@packages/core/src/index/process-commit.ts
@packages/core/src/artifacts/db.ts
@packages/core/src/artifacts/writer.ts
@packages/core/src/ingestion/git-walk.ts

<interfaces>
From `packages/core/src/index/types.ts`:
```typescript
export interface IndexOptions {
  repoPath: string;
  gitchangeDir?: string;
  batchSize?: number;
  maxBlobBytes?: number;
  rebuildIntelligence?: boolean;
}
```

From `packages/core/src/artifacts/writer.ts`:
```typescript
const DEFAULT_BATCH_SIZE = 750;
export function createWriter(db: DrizzleDb, batchSize?: number): IndexWriter;
```

From `packages/core/src/ingestion/git-walk.ts`:
```typescript
export function walkFromHead(repo: Repository): Iterable<string>;
export function walkRange(repo: Repository, cursorSha: string): Iterable<string>;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify piscina package legitimacy</name>
  <files>packages/core/package.json</files>
  <action>Before `pnpm add piscina@5.2.0 -F @gitchange/core`, human verifies https://www.npmjs.com/package/piscina — publisher nearform, millions of weekly downloads, matches STACK.md 5.2.0. Per 08-RESEARCH Package Legitimacy Audit.</action>
  <verify>
    <automated>test -f packages/core/package.json &amp;&amp; grep -q piscina packages/core/package.json || echo "awaiting install"</automated>
  </verify>
  <done>Human approved piscina install or flagged concern</done>
</task>

<task type="auto">
  <name>Task 1: Piscina worker pool + SQLite tuning</name>
  <files>packages/core/package.json, packages/core/src/index/worker.ts, packages/core/src/index/worker-pool.ts, packages/core/src/index/worker-pool.test.ts, packages/core/src/index/process-commit.ts, packages/core/src/index/full.ts, packages/core/src/index/incremental.ts, packages/core/src/artifacts/db.ts, packages/core/src/schema/manifest.ts</files>
  <action>
    Install piscina per P8-D-01. Split process-commit into: (a) `buildCommitRecords(repoPath, sha, ignoreRules)` callable from worker thread — worker opens its own read-only es-git repo; (b) main-thread writer applies returned rows. Create `worker-pool.ts` batching 500 SHAs per pool dispatch; `maxThreads = max(1, cpus-1)`. Wire `indexFull` and incremental loops through pool when `options.useWorkers !== false` (default true). Extend `openDb` per P8-D-02: `cache_size=-64000`, `mmap_size=268435456`. Add optional manifest field `lastIndexDurationMs: z.number().int().nonnegative().optional()` and set on index completion per P8-D-03. Preserve existing single-threaded path behind `useWorkers: false` for tests that need deterministic ordering. Do NOT pass Repository objects to workers.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/index/worker-pool.test.ts</automated>
  </verify>
  <done>Worker pool indexes BASIC_SCENARIO fixture; unit tests pass; WAL pragmas extended</done>
</task>

<task type="auto">
  <name>Task 2: Progress CLI + 10k scale benchmark gate</name>
  <files>packages/cli/src/commands/index.ts, tests/fixtures/scale-repo-builder.ts, tests/scale/index-benchmark.test.ts, packages/core/src/index/types.ts, packages/core/src/index.ts</files>
  <action>
    Per P8-D-03: `gitchange index` stderr progress every 500 commits: `indexed=N rate=R/s elapsed=Ts`. Create `scale-repo-builder.ts` generating synthetic git repo with configurable commit count (default 10k for CI). Benchmark test: full index 10k commits completes &lt;120s with workers enabled. Second test: incremental index after 1 new commit processes ≤5 commits. Gate 100k benchmark behind `GITCHANGE_SCALE_100K=1` env (skip in default CI) per P8-D-04. Export worker pool toggle from core if CLI needs `--no-workers` flag for debugging.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/scale/index-benchmark.test.ts</automated>
  </verify>
  <done>10k benchmark passes; incremental assertion passes; progress lines emitted during index</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread | Untrusted parallel execution — workers only receive repoPath + sha + ignore rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | worker.ts repoPath | mitigate | validate repoPath is directory with `.git` before open |
| T-08-02 | DoS | scale benchmark | mitigate | CI capped at 10k; 100k env-gated only |
| T-08-SC | Tampering | piscina npm install | mitigate | blocking human checkpoint before piscina add |
</threat_model>

<verification>
- `pnpm --dir packages/core test` passes
- `pnpm exec vitest run tests/scale/index-benchmark.test.ts` passes under 120s
- Incremental two-phase: second index on unchanged HEAD indexes 0 commits
</verification>

<success_criteria>
- SCALE-01 architecture proven on 10k fixture with piscina + incremental cursor
- CLI progress and manifest duration visible to operators
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-01-SUMMARY.md` when done
</output>
