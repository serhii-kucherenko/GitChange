---
phase: 04-era-detection-semantic-pipeline
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/core/src/semantic/assemble-graph.ts
  - packages/core/src/semantic/assemble-graph.test.ts
  - packages/core/src/semantic/graph-io.ts
  - packages/core/src/semantic/graph-io.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [ERA-01, ERA-03]
user_setup: []

must_haves:
  truths:
    - "assembleTemporalGraph merges eras.json + intelligence.json + index into temporal-graph.json"
    - "Graph includes era, commit, file, contributor, and inflection nodes with typed edges"
    - "File nodes limited to paths referenced in era evidence bundles (graph size cap per PITFALLS)"
    - "Co-change edges carry correlation disclaimer metadata from intelligence"
  artifacts:
    - path: "packages/core/src/semantic/assemble-graph.ts"
      provides: "Deterministic temporal graph assembly"
      exports: ["assembleTemporalGraph"]
    - path: "packages/core/src/semantic/graph-io.ts"
      provides: "readTemporalGraph / writeTemporalGraph with Zod gate"
      exports: ["writeTemporalGraph", "readTemporalGraph"]
  key_links:
    - from: "packages/core/src/semantic/assemble-graph.ts"
      to: ".gitchange/eras.json"
      via: "readErasArtifact"
      pattern: "readErasArtifact|eras\\.eras"
    - from: "packages/core/src/semantic/assemble-graph.ts"
      to: ".gitchange/intelligence.json"
      via: "coChange edges and era signals"
      pattern: "coChange|eraSignals"
---

## Phase Goal

**As a** new team member, **I want to** understand how the project evolved through named eras and inflection points backed by evidence, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Implement deterministic temporal graph assembly: merge named eras, inflection points, commits, contributors, and evidence-referenced files into `temporal-graph.json`.

Purpose: ROADMAP success criterion #4 requires temporal graph artifact; Phase 5/8 consume this without re-assembly. P4-D-04: no LLM in assembler.
Output: assembleTemporalGraph, graph I/O helpers, unit tests with golden fixture eras.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-era-detection-semantic-pipeline/04-RESEARCH.md
@packages/core/src/schema/zod/temporal-graph.ts
@packages/core/src/intelligence/cochange.ts
@packages/core/src/schema/drizzle/schema.ts

<interfaces>
Temporal graph node types (from 04-01):
`era | commit | file | contributor | inflection`

Edge types:
`era_contains_commit | commit_touches_file | contributor_authored_commit | era_has_inflection | files_co_changed`

From intelligence CoChangeEdge:
```typescript
{ pathA, pathB, relationship: "co_change", disclaimer: "historical correlation, not import dependency" }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Graph assembly core</name>
  <files>packages/core/src/semantic/assemble-graph.ts, packages/core/src/semantic/assemble-graph.test.ts</files>
  <behavior>
    - assembleTemporalGraph(gitchangeDir) throws if eras.json missing
    - Creates era node per NamedEra.id; inflection nodes per era inflections
    - era_contains_commit edges for commits in era window (from SQLite commits table filtered by committedAt between era startAt/endAt)
    - contributor_authored_commit from commits.authorId
    - commit_touches_file only for paths in era evidence file refs
    - files_co_changed edges from intelligence coChange where both paths in graph file set; copy disclaimer field
    - Total nodes ≤ 500 (hard error if exceeded — forces era-level aggregation)
  </behavior>
  <action>
    Implement assembleTemporalGraph returning TemporalGraphArtifact. Use ulid for edge ids. Resolve contributor nodes from authors table. Map inflection id `inflection:{ulid}`. No live git. Test with readErasArtifact fixture + BASIC_SCENARIO index in assemble-graph.test.ts.
  </action>
  <verify>
    <automated>pnpm --dir packages/core exec vitest run packages/core/src/semantic/assemble-graph.test.ts</automated>
  </verify>
  <done>Assembly produces nodes/edges with era→commit and era→inflection links on fixture</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Graph I/O + public export</name>
  <files>packages/core/src/semantic/graph-io.ts, packages/core/src/semantic/graph-io.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - writeTemporalGraph validates TemporalGraphArtifact before atomic write
    - assembleAndWriteTemporalGraph convenience calls assemble + write
  </behavior>
  <action>
    Mirror eras-io atomic write pattern to `temporal-graph.json`. Export assembleTemporalGraph, writeTemporalGraph, assembleAndWriteTemporalGraph from core index. Add assembleAndWriteTemporalGraph to plugin helper scripts list in 04-02 if scripts created.
  </action>
  <verify>
    <automated>pnpm --dir packages/core exec vitest run packages/core/src/semantic/graph-io.test.ts</automated>
  </verify>
  <done>temporal-graph.json written and re-parses through Zod</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assembler → SQLite | Read-only; parameterized Drizzle queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | Graph node explosion | mitigate | 500 node cap; file nodes evidence-referenced only |
| T-04-08 | Tampering | Stale eras in graph | mitigate | headSha field on graph artifact; integrity checker in 04-04 |
</threat_model>

<verification>
- assemble-graph tests pass on BASIC_SCENARIO + fixture eras
- No LLM imports in semantic module
</verification>

<success_criteria>
- temporal-graph.json produced deterministically from eras + intelligence + index
- Graph structure includes inflection nodes per ERA-03
</success_criteria>

<output>
Create `.planning/phases/04-era-detection-semantic-pipeline/04-03-SUMMARY.md` when done
</output>
