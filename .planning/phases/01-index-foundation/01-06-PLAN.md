---
phase: 01-index-foundation
plan: 06
type: execute
wave: 5
depends_on: [01-05]
files_modified:
  - packages/core/src/index/freshness.ts
  - packages/core/src/index/full.ts
  - packages/core/src/index/incremental.ts
  - packages/core/src/index/freshness.test.ts
autonomous: true
requirements: [INGX-05]
user_setup: []

must_haves:
  truths:
    - "D-04: Shallow clone warns, continues, sets indexCompleteness partial"
    - "D-05: Force-push halts incremental merge; requires explicit full rebuild"
    - "D-06: Freshness warnings persisted in manifest.json and echoed to stdout"
    - "Indexing a shallow clone sets indexCompleteness:partial and records a shallow_clone warning, then continues"
    - "An incremental run whose lastIndexedCommit is no longer reachable from HEAD halts the incremental merge and requires --full"
    - "Freshness warnings are persisted in manifest.json and echoed to CLI stdout"
    - "Out-of-order commit timestamps are counted and recorded as a warning without failing the index"
  artifacts:
    - path: "packages/core/src/index/freshness.ts"
      provides: "Shallow + force-push + out-of-order detection"
      contains: "shallow"
  key_links:
    - from: "packages/core/src/index/incremental.ts"
      to: "packages/core/src/index/freshness.ts"
      via: "force-push check before merge"
      pattern: "forcePush|isForcePush|reachable"
    - from: "packages/core/src/index/full.ts"
      to: "packages/core/src/index/freshness.ts"
      via: "shallow check → indexCompleteness"
      pattern: "shallow|partial"
---

<objective>
Add history-integrity detection so the index never silently lies: shallow clones produce a `partial` index with a warning (warn + continue), force-pushed/rewritten history halts the incremental merge and requires a `--full` rebuild, and out-of-order timestamps are counted. Warnings persist in `manifest.json` and echo to stdout.

Purpose: INGX-05 (freshness warnings) and RESEARCH Pitfall 3. Force-push must not append to a stale index (D-05); shallow must not masquerade as complete (D-04).
Output: `index/freshness.ts` (detection module) wired into `full.ts` (shallow) and `incremental.ts` (force-push), plus manifest warning population and integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-index-foundation/SKELETON.md
@.planning/phases/01-index-foundation/01-RESEARCH.md
@.planning/phases/01-index-foundation/01-PATTERNS.md
@.planning/phases/01-index-foundation/01-CONTEXT.md

<interfaces>
<!-- Consumes: index/full.ts, index/incremental.ts, schema/manifest.ts (P05/P02). -->
```typescript
// index/freshness.ts
export function isShallow(repoPath: string, repo: Repository): boolean; // .git/shallow file OR repo.isShallow()
export interface ForcePushResult { rewritten: boolean; reason?: string; }
export function checkCursorReachable(repo: Repository, cursorSha: string): ForcePushResult; // reachable-from-HEAD ancestry
export function countOutOfOrder(commitTimestamps: number[]): number; // committer-date axis
// Warning codes match manifest schema: "shallow_clone" | "force_push_detected" | "out_of_order_commits"
export class ForcePushHaltError extends Error {}  // thrown by incremental to require --full
```
</interfaces>

<!-- es-git accessor note (RESEARCH A1/A2, Open Question 1): confirm repo.isShallow() and the
     ancestry/revparseSingle approach against es-git reference. The `.git/shallow` file-existence
     check is the guaranteed cross-version fallback for shallow detection. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: freshness detection module</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 323–340 (freshness pattern, two distinct code paths)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 341–348 (Pitfall 3), 438–455 (§5), 494–496 (A2 ancestry)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-04, D-05, D-06
  </read_first>
  <files>packages/core/src/index/freshness.ts, packages/core/src/index/freshness.test.ts</files>
  <behavior>
    - `isShallow` returns true for a `shallowCloneOf(...)` fixture (detects `.git/shallow`), false for a full clone.
    - `checkCursorReachable` returns `{rewritten:false}` when the cursor is an ancestor of HEAD; `{rewritten:true, reason}` when the cursor SHA is missing OR not reachable from HEAD (simulate by using a fabricated/orphaned sha).
    - `countOutOfOrder` returns the number of commits whose committer timestamp is earlier than a prior commit in walk order.
  </behavior>
  <action>
    `freshness.ts`: `isShallow(repoPath, repo)` checks `fs.existsSync(${repoPath}/.git/shallow)` first (guaranteed fallback) and also `repo.isShallow()` if the accessor exists (confirm name in es-git ref, A1). `checkCursorReachable(repo, cursor)`: try `repo.revparseSingle(cursor)` — if it throws, `rewritten:true`; additionally confirm the cursor is an ancestor of HEAD (walk HEAD range or use es-git ancestry; if never reachable → `rewritten:true`, reason "cursor not ancestor of HEAD") (A2 — do not rely on existence alone). `countOutOfOrder(timestamps)` counts committer-date inversions (prefer committer date, note the chosen axis). Define `ForcePushHaltError`. Keep detection pure/inspectable; wiring is Task 2.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/index/freshness</automated>
  </verify>
  <acceptance_criteria>
    - Shallow detected via `.git/shallow` on a shallow fixture; full clone reports not-shallow.
    - Force-push detection returns rewritten=true for missing/unreachable cursor, false for a valid ancestor cursor.
    - Out-of-order counter correct on a fixture with a backdated commit.
  </acceptance_criteria>
  <done>Shallow, force-push, and out-of-order detection implemented and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: wire freshness into full + incremental, populate manifest warnings</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 340 (decision routing), 380–383 (manifest as contract)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-04, D-05, D-06
    - `packages/core/src/index/full.ts` and `incremental.ts` (from Plan 05 — read current flow before editing)
  </read_first>
  <files>packages/core/src/index/full.ts, packages/core/src/index/incremental.ts</files>
  <action>
    In `full.ts`: after opening the repo, call `isShallow`. If shallow → set `indexCompleteness:"partial"` and push a `{code:"shallow_clone", message}` warning; **continue** indexing available commits (D-04). While walking, collect committer timestamps and set `countOutOfOrder` result; if > 0, push `{code:"out_of_order_commits", message: "<n> commits out of chronological order"}`. Write all warnings into `manifest.warnings[]` and echo each to stdout on completion (D-06).
    In `incremental.ts`: BEFORE walking the range, call `checkCursorReachable(repo, manifest.lastIndexedCommit)`. If `rewritten` → do NOT append; throw `ForcePushHaltError` with a message instructing the user to run a full rebuild (`indexFull` / `--full`), and record intent (D-05: halt incremental merge, require explicit full). Cold-start on a shallow clone remains allowed (only downgrade completeness). Use exhaustive `switch` with `never` default when mapping warning codes (workspace rule).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/index</automated>
  </verify>
  <acceptance_criteria>
    - Indexing a shallow clone → `manifest.indexCompleteness === "partial"` + `shallow_clone` warning; index still populated.
    - Incremental run against a rewritten-history fixture throws `ForcePushHaltError` and does NOT append rows; manifest not silently advanced.
    - Out-of-order commits counted and surfaced as a warning without failing the run.
    - Warnings echoed to stdout on completion (D-06).
  </acceptance_criteria>
  <done>Freshness warnings drive completeness/halt decisions and persist to manifest + stdout (INGX-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo history state → index trust | Shallow/rewritten history can make a partial index look authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing/Repudiation | stale index after force-push | mitigate | Incremental halts when cursor unreachable (D-05); requires explicit full rebuild — never silently appends to a stale index. |
| T-06-02 | Information Disclosure (integrity) | shallow clone treated as complete | mitigate | `indexCompleteness:"partial"` + persisted warning (D-04); downstream reads can display a degraded badge. |
| T-06-03 | Tampering | backdated/out-of-order commits | accept | Counted and surfaced as a warning; committer-date axis documented — index remains usable. |
</threat_model>

<verification>
- `pnpm vitest run packages/core/src/index/freshness` and `.../src/index` green.
- `turbo test --filter=@gitchange/core` green.
- Shallow → partial+warn; force-push → halt (INGX-05).
</verification>

<success_criteria>
- Shallow clone yields partial index + warning and continues (D-04).
- Force-push/rewrite halts incremental and requires full rebuild (D-05).
- Warnings persisted in manifest and echoed to stdout (D-06, INGX-05).
</success_criteria>

<output>
Create `.planning/phases/01-index-foundation/01-06-SUMMARY.md` when done.
</output>
