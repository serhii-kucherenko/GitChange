---
phase: 07-guided-tours-onboarding-ux
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - packages/dashboard/src/store/tour.ts
  - packages/dashboard/src/store/tour.test.ts
  - packages/dashboard/src/components/TourPicker.tsx
  - packages/dashboard/src/components/TourPlayer.tsx
  - packages/dashboard/src/components/TourStopCard.tsx
  - packages/dashboard/src/components/TourChapterNav.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/index.ts
autonomous: true
requirements: [TOUR-02, TOUR-03, TOUR-04]
user_setup: []

must_haves:
  truths:
    - "User selects default, role, or topic tour from Tours tab picker (TOUR-02, TOUR-03)"
    - "Tour player shows chapter list, current stop narrative, and evidence list"
    - "See evidence button drills into era, commit, file, or decision via useDrillStore (TOUR-04, P7-D-06)"
    - "OpenWorkBadge appears on stops when matchOpenWorkToSurface finds incomplete threads (STAT-03)"
    - "Tour progress persists in localStorage per headSha (P7-D-07)"
  artifacts:
    - path: "packages/dashboard/src/store/tour.ts"
      provides: "useTourStore with progress persistence"
      exports: ["useTourStore"]
    - path: "packages/dashboard/src/components/TourPlayer.tsx"
      provides: "Chapter/stop navigation and evidence drill actions"
    - path: "packages/dashboard/src/components/TourPicker.tsx"
      provides: "Role and topic tour selection UI"
    - path: "packages/dashboard/src/layout/DashboardLayout.tsx"
      provides: "tours intelligence tab"
  key_links:
    - from: "packages/dashboard/src/components/TourStopCard.tsx"
      to: "packages/dashboard/src/store/drill.ts"
      via: "setSelectedEraId / setSelectedCommitSha / selectCommitAndFile / setSelectedDecisionId"
      pattern: "useDrillStore"
    - from: "packages/dashboard/src/components/TourStopCard.tsx"
      to: "packages/dashboard/src/utils/open-work-match.ts"
      via: "matchOpenWorkToSurface on stop drillTarget"
      pattern: "matchOpenWorkToSurface"
    - from: "packages/dashboard/src/App.tsx"
      to: "/api/tours"
      via: "fetchTours react-query"
      pattern: "fetchTours|fetchTour"
---

<objective>
Tour player UX: fourth dashboard tab, tour picker for role/topic variants, chapter/stop player with evidence drill-down into existing Phase 5/6 surfaces, open-work badges on stops, and local progress tracking.

Purpose: Delivers TOUR-02, TOUR-03 selection and TOUR-04 evidence navigation — the user-visible Phase 7 outcome.
Output: tourStore, TourPicker, TourPlayer, TourStopCard, DashboardLayout/App wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guided-tours-onboarding-ux/07-RESEARCH.md
@packages/dashboard/src/store/drill.ts
@packages/dashboard/src/utils/open-work-match.ts
@packages/dashboard/src/components/DecisionsPanel.tsx
@packages/dashboard/src/components/OpenWorkBadge.tsx
@packages/dashboard/src/layout/DashboardLayout.tsx
@packages/dashboard/src/App.tsx

<interfaces>
From drill.ts:
```typescript
export const useDrillStore = create<DrillState>({...});
// setSelectedEraId, setSelectedCommitSha, selectCommitAndFile, setSelectedDecisionId
```

From open-work-match.ts:
```typescript
export function matchOpenWorkToSurface(threads, surface: { eraId?, commitSha?, path?, eraWindow? }): OpenWorkThreadSummary[];
```

TourStop.drillTarget from API: { eraId?, commitSha?, filePath?, decisionId? }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useTourStore + TourPicker</name>
  <files>packages/dashboard/src/store/tour.ts, packages/dashboard/src/store/tour.test.ts, packages/dashboard/src/components/TourPicker.tsx</files>
  <behavior>
    - useTourStore: activeTourId, chapterIndex, stopIndex, completedStopIds, setActiveTour, advanceStop, goToChapter, markStopComplete, hydrateFromStorage(headSha), persistToStorage
    - localStorage key: gitchange-tour-progress:${headSha}
    - TourPicker: groups tours by kind (Onboarding default, Role variants, Topic threads); highlights defaultTourId; onSelect calls setActiveTour
  </behavior>
  <action>
    Implement tour store per P7-D-07 separate from drillStore. TourPicker uses fetchTours react-query; show roleTag labels (Backend, Frontend, etc.) and topicKey for topic tours. Empty state when tours.json missing directs user to run /gitchange tour synthesis. Unit test store persistence round-trip with mock localStorage.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/dashboard/src/store/tour.test.ts</automated>
  </verify>
  <done>Store persists progress; picker renders three tour kind groups from API mock</done>
</task>

<task type="auto">
  <name>Task 2: TourPlayer + TourStopCard with drill-down</name>
  <files>packages/dashboard/src/components/TourPlayer.tsx, packages/dashboard/src/components/TourStopCard.tsx, packages/dashboard/src/components/TourChapterNav.tsx</files>
  <action>
    TourPlayer: fetchTour(activeTourId); TourChapterNav sidebar list with chapter order; main area shows current stop via TourStopCard. Navigation: Prev/Next stop, chapter jump updates tourStore indices. TourStopCard: render narrative, evidence chips (commit sha short, file path, doc excerpt truncated), primary CTA "See evidence" that maps drillTarget to drillStore actions per P7-D-06 — era → setSelectedEraId + switch tab to timeline; commit → setSelectedCommitSha; file → selectCommitAndFile(commitSha, filePath); decision → setSelectedDecisionId + switch to decisions tab. When drilling, parent App should show CommitDetailPanel or EraDetailPanel as today. Fetch open-work threads via existing query; call matchOpenWorkToSurface with stop surface props; render OpenWorkBadge when matches (P6-D-07). Evidence list items each clickable to same drill handlers.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/dashboard/src/store/tour.test.ts 2>/dev/null; pnpm --filter @gitchange/dashboard exec tsc --noEmit</automated>
  </verify>
  <done>Player navigates chapters/stops; See evidence sets drillStore from fixture stop</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard tab wiring</name>
  <files>packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/index.ts</files>
  <action>
    Extend IntelligenceTab union with "tours" per P7-D-11. Add Tours tab button in DashboardLayout nav. App.tsx: when intelligenceTab === "tours", sidebar shows TourPicker + TourChapterNav; main shows TourPlayer when tour selected, else prompt to pick a tour. When user drills from tour stop, keep tours tab active but show CommitDetailPanel in main when selectedCommitSha set (same pattern as decisions tab drilling). Re-export useTourStore from dashboard index if exporting matchOpenWorkToSurface. Ensure keyboard-accessible nav buttons on player.
  </action>
  <verify>
    <automated>pnpm --filter @gitchange/dashboard exec tsc --noEmit</automated>
  </verify>
  <done>Tours tab visible; selecting default tour shows player with 4-6 chapters from API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tour narrative → DOM | XSS if narratives contain HTML — render as text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | localStorage progress | accept | Low value; headSha-scoped key |
| T-07-09 | XSS | TourStopCard narrative | mitigate | React text nodes only; no dangerouslySetInnerHTML |
</threat_model>

<verification>
pnpm --filter @gitchange/dashboard exec tsc --noEmit
pnpm exec vitest run packages/dashboard/src/store/tour.test.ts
</verification>

<success_criteria>
- Tours tab with picker and player
- Role and topic tours selectable
- Every stop has See evidence drill into timeline/decisions
- OpenWorkBadge on matching stops
</success_criteria>

<output>
Create `.planning/phases/07-guided-tours-onboarding-ux/07-04-SUMMARY.md` when done
</output>
