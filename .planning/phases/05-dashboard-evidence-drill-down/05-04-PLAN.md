---
phase: 05-dashboard-evidence-drill-down
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/core/migrations/0002_diff_hunks.sql
  - packages/core/src/schema/drizzle/schema.ts
  - packages/core/src/schema/zod/evidence.ts
  - packages/core/src/schema/zod/evidence.test.ts
  - packages/core/src/schema/zod/file-change.ts
  - packages/core/src/ingestion/hunks.ts
  - packages/core/src/ingestion/hunks.test.ts
  - packages/core/src/index/process-commit.ts
  - packages/core/src/artifacts/writer.ts
  - packages/core/src/read/commits.ts
  - packages/core/src/read/commits.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/commits.ts
  - packages/dashboard/src/components/CommitDetailPanel.tsx
  - packages/dashboard/src/components/DiffHunkView.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/types.ts
  - tests/golden/ingestion-hunks.test.ts
  - tests/integration/dashboard-commit-detail.test.ts
autonomous: true
requirements: [TIME-02, SCALE-02]
user_setup: []

must_haves:
  truths:
    - "User selects a commit and sees file changes for that commit from index"
    - "User selects a file and sees diff hunks (patch excerpts) without live git show"
    - "Hunk text is privacy-redacted; ignored paths show no hunks"
  artifacts:
    - path: "packages/core/src/ingestion/hunks.ts"
      provides: "captureDiffHunks at index time"
      exports: ["captureDiffHunks"]
    - path: "packages/core/src/read/commits.ts"
      provides: "getCommitDetail with files and hunks_json"
      exports: ["getCommitDetail"]
    - path: "packages/dashboard/src/components/DiffHunkView.tsx"
      provides: "Renders patch hunks for selected file"
  key_links:
    - from: "processCommit"
      to: "captureDiffHunks"
      via: "per non-binary file change"
      pattern: "captureDiffHunks"
    - from: "DiffHunkView"
      to: "/api/commits/:sha"
      via: "react-query"
      pattern: "commits/"
---

## Phase Goal

**As a** new team member or maintainer, **I want to** explore project history interactively on localhost with era-aware drill-down to commits, files, and diff hunks, **so that** I can verify every narrative claim against indexed evidence without hunting git log.

<objective>
Index-time hunk capture (P5-D-02) plus commit→file→hunk drill-down UI (TIME-02 partial: commit and file layers).

Purpose: Closes the critical gap — hunks are not in index today; enables diff proof without live git walks.
Output: hunks_json column, captureDiffHunks, getCommitDetail API, CommitDetailPanel + DiffHunkView, golden hunk test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@packages/core/src/schema/zod/evidence.ts
@packages/core/src/index/process-commit.ts
@packages/core/src/ingestion/diff.ts

<interfaces>
Evidence hunk type (enable per comment in evidence.ts):
hunk: { type: "hunk", path, commitSha, startLine, endLine }
file_changes columns: hunkStart, hunkEnd currently null — add hunksJson text nullable.
processCommit already calls diffCommit and applyPrivacy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Index-time hunk capture + schema</name>
  <files>packages/core/migrations/0002_diff_hunks.sql, packages/core/src/schema/drizzle/schema.ts, packages/core/src/schema/zod/evidence.ts, packages/core/src/schema/zod/evidence.test.ts, packages/core/src/schema/zod/file-change.ts, packages/core/src/ingestion/hunks.ts, packages/core/src/ingestion/hunks.test.ts, packages/core/src/index/process-commit.ts, packages/core/src/artifacts/writer.ts, tests/golden/ingestion-hunks.test.ts</files>
  <behavior>
    - captureDiffHunks returns array of { startLineOld, startLineNew, lineCount, patch } per hunk
    - Skips binary files and contentIgnored paths
    - applyPrivacy redacts secrets in patch text
    - Re-index BASIC_SCENARIO stores hunks_json on at least one modified text file
    - Evidence schema accepts hunk type; rejects invalid hunk refs
    - Golden: no secret patterns in hunks_json for BASIC_SCENARIO secrets fixture
  </behavior>
  <action>
    Add migration 0002 adding hunks_json TEXT nullable to file_changes. Enable hunk Evidence variant in evidence.ts (uncomment + test). Create ingestion/hunks.ts using es-git diff patch iteration for single commit+path. Cap 20 hunks/file and 32KB total patch per file per P5-D-02 discretion. Wire into processCommit after file change record built; store JSON on writer. Extend FileChangeRecord zod with optional hunks array. Update writer to persist hunksJson. Unit tests on synthetic diff. Golden grep gate on hunks_json for secret patterns. Re-run indexFull on fixture in test to populate hunks.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/ingestion/hunks.test.ts packages/core/src/schema/zod/evidence.test.ts tests/golden/ingestion-hunks.test.ts</automated>
  </verify>
  <done>Hunks persisted at index time; hunk evidence type valid; no secret leakage in golden test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getCommitDetail API + drill-down panels</name>
  <files>packages/core/src/read/commits.ts, packages/core/src/read/commits.test.ts, packages/core/src/index.ts, packages/server/src/routes/commits.ts, packages/dashboard/src/components/CommitDetailPanel.tsx, packages/dashboard/src/components/DiffHunkView.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/types.ts, tests/integration/dashboard-commit-detail.test.ts</files>
  <behavior>
    - getCommitDetail returns commit metadata + files[] with changeType, path, hunks[] parsed from hunks_json
    - GET /api/commits/:sha returns 404 for unknown sha
    - Dashboard: selecting commit shows CommitDetailPanel with file list
    - Selecting file shows DiffHunkView with monospace patch blocks
    - contentRedacted files show redaction notice instead of patch
  </behavior>
  <action>
    Add getCommitDetail to read/commits.ts joining file_changes for sha. Extend commits route GET /:sha. CommitDetailPanel appears when selectedCommitSha set (from 05-01 store). File row click sets selectedFilePath in zustand. DiffHunkView fetches detail via react-query (or uses cached detail) and renders hunks. Breadcrumb: Commit > File. Integration test: known fixture sha returns files with hunks on modified text file.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/commits.test.ts tests/integration/dashboard-commit-detail.test.ts</automated>
  </verify>
  <done>User drills commit → file → diff hunk in dashboard without live git</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index→SQLite | Patch text may contain secrets before redaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | hunks_json | mitigate | applyPrivacy on patch; golden secret grep; skip ignored paths |
| T-05-07 | Tampering | hunk line numbers | accept | Display-only; sourced from index |
</threat_model>

<verification>
- Golden hunk secret test passes
- Integration commit detail test passes
- rg "es-git" packages/server --glob '*.ts' still 0
</verification>

<success_criteria>
- TIME-02: era→commit→file→hunk path works for commit/file/hunk steps (era link via 05-03)
- SCALE-02: server reads hunks_json only
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-04-SUMMARY.md` when done
</output>
