---
phase: 07-guided-tours-onboarding-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/schema/zod/tours.ts
  - packages/core/src/schema/zod/tours.test.ts
  - packages/core/src/schema/zod/index.ts
  - packages/core/src/tours/outline.ts
  - packages/core/src/tours/outline.test.ts
  - packages/core/src/tours/context.ts
  - packages/core/src/tours/context.test.ts
  - packages/core/src/tours/tours-io.ts
  - packages/core/src/tours/tours-io.test.ts
  - packages/core/src/verify/tours-integrity.ts
  - packages/core/src/verify/tours-integrity.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [TOUR-01]
user_setup: []

must_haves:
  truths:
    - "ToursArtifact Zod schema exists with Tour, TourChapter, TourStop, and mandatory evidence on every stop"
    - "outlineDefaultTourChapters produces 4-6 chapters from eras.json ordered by startAt chronology"
    - "buildTourSynthesisContext returns bounded JSON from eras + decisions + open-work + intelligence without live git"
    - "writeToursArtifact rejects invalid JSON and enforces P7-D-10 caps before writing .gitchange/tours.json"
  artifacts:
    - path: "packages/core/src/schema/zod/tours.ts"
      provides: "ToursArtifact, Tour, TourChapter, TourStop, TourKind, RoleTag"
      exports: ["ToursArtifact", "TOURS_SCHEMA_VERSION", "TourKind", "RoleTag"]
    - path: "packages/core/src/tours/outline.ts"
      provides: "Deterministic default chapter skeleton from eras"
      exports: ["outlineDefaultTourChapters"]
    - path: "packages/core/src/tours/context.ts"
      provides: "Bounded context for tour-builder agent"
      exports: ["buildTourSynthesisContext"]
    - path: "packages/core/src/tours/tours-io.ts"
      provides: "readToursArtifact / writeToursArtifact with Zod gate"
      exports: ["readToursArtifact", "writeToursArtifact"]
  key_links:
    - from: "packages/core/src/tours/outline.ts"
      to: "packages/core/src/semantic/eras-io.ts"
      via: "readErasArtifact"
      pattern: "readErasArtifact"
    - from: "packages/core/src/schema/zod/tours.ts"
      to: "packages/core/src/schema/zod/evidence.ts"
      via: "TourStop.evidence arrays"
      pattern: "Evidence"
---

## Phase Goal

**As a** new team member, **I want to** take an evidence-backed guided tour through project history from the dashboard, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Define tour artifact contracts and the walking skeleton for tour synthesis: Zod schemas (`tours.json`), deterministic default chapter outline from eras (P7-D-02, P7-D-08 phase 1), bounded context bundler for host AI, validated read/write helpers, and integrity checker stub.

Purpose: Locks TOUR-01 data contracts before tour-builder agent or dashboard work. Unblocks all Phase 7 parallel work.
Output: Schema files, outline + context bundler, I/O helpers, integrity module, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guided-tours-onboarding-ux/07-RESEARCH.md
@.planning/phases/06-decisions-status-open-work/06-01-SUMMARY.md
@packages/core/src/schema/zod/eras.ts
@packages/core/src/schema/zod/evidence.ts
@packages/core/src/schema/zod/decisions.ts
@packages/core/src/decisions/context.ts

<interfaces>
From `packages/core/src/schema/zod/eras.ts`:
```typescript
export const NamedEra = z.object({
  id: z.string().min(1),
  name: z.string().min(3).max(80),
  summary: z.string().max(500),
  startAt: z.number().int(),
  endAt: z.number().int(),
  evidence: z.array(Evidence).min(1),
  // claims, inflections, signalIds...
});
export const ErasArtifact = z.object({ eras: z.array(NamedEra), headSha: z.string().length(40), ... });
```

From `packages/core/src/decisions/threshold.ts`:
```typescript
export function isBelowEvidenceThreshold(confidence: number, evidenceCount: number): boolean;
```

Reuse `Evidence` discriminated union from evidence.ts — do not duplicate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tours Zod schemas + P7-D-10 caps</name>
  <files>packages/core/src/schema/zod/tours.ts, packages/core/src/schema/zod/tours.test.ts, packages/core/src/schema/zod/index.ts</files>
  <behavior>
    - TourKind enum: default, role, topic
    - RoleTag enum: backend, frontend, fullstack, maintainer
    - TourStop requires narrative max 400, evidence.min(1), drillTarget with refine: at least one of eraId, commitSha, filePath, decisionId
    - TourChapter requires order, title, summary max 300, eraIds.min(1), stops.min(1)
    - Tour requires id (tour: prefix), kind, title, description, chapters
    - Default kind tour: chapters.length 4-6
    - Role kind: roleTag required
    - Topic kind: topicKey required
    - ToursArtifact: schemaVersion "1", defaultTourId, tours array with refine caps (1 default, max 3 role, max 5 topic)
    - Export TOURS_SCHEMA_VERSION = "1"
  </behavior>
  <action>
    Create tours.ts per P7-D-01, P7-D-05, P7-D-10. Use z.refine on ToursArtifact for cap enforcement and on TourStop.drillTarget for at-least-one drill field. Add exhaustive switch with assertNever for TourKind if parsing helpers added. Export from schema/zod/index.ts. Tests cover valid minimal artifact, reject stop without evidence, reject default tour with 3 chapters, reject 2 default tours.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/schema/zod/tours.test.ts</automated>
  </verify>
  <done>ToursArtifact parses golden-shaped fixture; invalid shapes fail Zod with clear paths</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic outline + tour synthesis context</name>
  <files>packages/core/src/tours/outline.ts, packages/core/src/tours/outline.test.ts, packages/core/src/tours/context.ts, packages/core/src/tours/context.test.ts</files>
  <behavior>
    - outlineDefaultTourChapters: sort eras by startAt asc; if more than 6 eras, merge adjacent smallest eras or take first 6 by chronology; if fewer than 4 eras, split largest era into intro+body using claims or duplicate era with distinct chapter titles from era name + " overview"/" deep dive" only when 1-3 eras exist
    - Each outline chapter has eraIds, placeholder stop with era primary commitSha from era evidence[0] when type commit
    - buildTourSynthesisContext includes: eraSummaries (id, name, summary, window), outlineChapters, expertiseTopics (topic + top paths), decisionSeeds (id, title, status, confidence — exclude isBelowEvidenceThreshold), openWorkSeeds (id, title, status, relatedPaths), rolePathHints (backend/frontend prefix lists per P7-D-03 discretion), headSha, caps reminder
    - Context reads artifacts via readErasArtifact, readDecisionsArtifact, readOpenWorkArtifact patterns — no es-git
  </behavior>
  <action>
    Implement outline.ts per P7-D-02 and P7-D-08 deterministic phase. Implement context.ts mirroring decisions/context.ts bounded slice pattern (top 8 decisions, top 5 open threads, top 10 expertise topics). Filter decisions below EVD-03 floor from decisionSeeds per P7-D inherited rule. Unit tests use inline ErasArtifact fixtures; context test mocks artifact readers or uses tmp .gitchange dir with bind helpers if available.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/tours/outline.test.ts packages/core/src/tours/context.test.ts</automated>
  </verify>
  <done>Outline returns 4-6 chapters for 8-era fixture; context JSON excludes below-threshold decisions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tours I/O + integrity checker</name>
  <files>packages/core/src/tours/tours-io.ts, packages/core/src/tours/tours-io.test.ts, packages/core/src/verify/tours-integrity.ts, packages/core/src/verify/tours-integrity.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - writeToursArtifact: parse ToursArtifact, run checkToursIntegrity, atomic tmp+rename to tours.json
    - readToursArtifact: parse or return null
    - checkToursIntegrity: defaultTourId exists in tours; all evidence commit SHAs exist in index.sqlite; all eraIds exist in eras.json; all decisionIds exist in decisions.json when present; drillTarget refs resolve
  </behavior>
  <action>
    Mirror eras-io.ts / decisions-io.ts write pattern. Integrity checker queries SQLite commits table for sha existence (reuse verify helper patterns from semantic-integrity.ts). Export all from packages/core/src/index.ts. Tests use BASIC_SCENARIO or minimal tmp gitchange dir.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/tours/tours-io.test.ts packages/core/src/verify/tours-integrity.test.ts</automated>
  </verify>
  <done>Round-trip write/read succeeds; integrity fails on bogus commit SHA in stop evidence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact write | Host AI output crosses into .gitchange/tours.json |
| Context bundler | SQLite/JSON artifacts → bounded JSON for agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | writeToursArtifact | mitigate | Zod parse + checkToursIntegrity before atomic write |
| T-07-02 | Spoofing | drillTarget refs | mitigate | Integrity resolves SHAs/paths against index + eras.json |
| T-07-03 | Information Disclosure | buildTourSynthesisContext | accept | Same disclosure as decision context; localhost-only |
| T-07-SC | Tampering | npm installs | mitigate | No new packages in Phase 7 |
</threat_model>

<verification>
pnpm exec vitest run packages/core/src/schema/zod/tours.test.ts packages/core/src/tours/ packages/core/src/verify/tours-integrity.test.ts
</verification>

<success_criteria>
- ToursArtifact schema exported with TOURS_SCHEMA_VERSION
- Deterministic outline produces 4-6 chapters from eras
- buildTourSynthesisContext bounded and EVD-03 filtered
- writeToursArtifact gated by Zod + integrity
</success_criteria>

<output>
Create `.planning/phases/07-guided-tours-onboarding-ux/07-01-SUMMARY.md` when done
</output>
