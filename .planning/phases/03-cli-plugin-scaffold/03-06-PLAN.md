---
phase: 03-cli-plugin-scaffold
plan: 06
type: execute
wave: 5
depends_on: ["03-03", "03-05"]
files_modified:
  - docs/QUICKSTART.md
  - tests/integration/first-run-flow.test.ts
  - README.md
  - .planning/phases/03-cli-plugin-scaffold/03-VALIDATION.md
autonomous: true
requirements: [INST-04]
user_setup: []

must_haves:
  truths:
    - "Quickstart doc covers install → /gitchange → /gitchange-dashboard in 5 or fewer steps"
    - "Integration test simulates first-run: index → serve → snapshot API → dashboard HTML"
    - "Phase 3 requirement grep gate confirms PLUG + INST coverage"
  artifacts:
    - path: "docs/QUICKSTART.md"
      provides: "Under-5-step quickstart"
      min_lines: 30
    - path: "tests/integration/first-run-flow.test.ts"
      provides: "End-to-end first-run integration test"
    - path: ".planning/phases/03-cli-plugin-scaffold/03-VALIDATION.md"
      provides: "Phase 3 Nyquist validation matrix"
  key_links:
    - from: "docs/QUICKSTART.md"
      to: "scripts/install.sh"
      via: "step 1 install command"
      pattern: "install"
    - from: "tests/integration/first-run-flow.test.ts"
      to: "gitchange index|serve"
      via: "subprocess chain"
      pattern: "gitchange"
---

<objective>
Close Phase 3 with quickstart documentation and end-to-end first-run integration test proving install → index → dashboard path (INST-04).

Purpose: Verifies all Phase 3 success criteria together; provides onboarding doc for adopters.
Output: QUICKSTART.md (≤5 steps), first-run integration test, phase validation matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cli-plugin-scaffold/03-RESEARCH.md
@.planning/phases/01-index-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QUICKSTART.md (≤5 steps)</name>
  <files>docs/QUICKSTART.md, README.md</files>
  <action>
    Write plain-language QUICKSTART.md per INST-04 and plain-human-deliverables rule:

    **Step 1 — Install:** one-line `curl -fsSL ... | bash` or marketplace install for Cursor/Claude Code
    **Step 2 — Open your repo** in the IDE (or cd in terminal)
    **Step 3 — Run `/gitchange`** (or `gitchange index`) — first analysis, no config files
    **Step 4 — Run `/gitchange-dashboard`** (or `gitchange serve` + open browser) — see index status and repo snapshot
    **Step 5 — Explore:** read expertise topics and churn highlights; Phase 5 adds full drill-down

    Max 5 numbered steps in the main path. Link to REQUIREMENTS traceability. Update README.md to link QUICKSTART prominently.

    Do not mention AI/LLM unless describing slash commands neutrally as "chat commands".
  </action>
  <verify>
    <automated>grep -c "^[0-9]\." docs/QUICKSTART.md | awk '$1 <= 5 && $1 >= 3 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>QUICKSTART has 3-5 numbered steps covering install, /gitchange, /gitchange-dashboard</done>
</task>

<task type="auto">
  <name>Task 2: First-run E2E test + phase validation matrix</name>
  <files>tests/integration/first-run-flow.test.ts, .planning/phases/03-cli-plugin-scaffold/03-VALIDATION.md</files>
  <action>
    **first-run-flow.test.ts** simulates new user on BASIC_SCENARIO:
    1. `gitchange index --repo <fixture>`
    2. `gitchange status` asserts indexed
    3. Spawn `gitchange serve` on ephemeral port (GITCHANGE_PORT env)
    4. fetch /api/snapshot → manifest + stats
    5. fetch / → HTML contains "GitChange" and commit count text
    6. Cleanup server process

    **03-VALIDATION.md** — mirror 01-VALIDATION format:
    - Map PLUG-01..05, INST-01..04 to plans and automated gates
    - Full suite command: `turbo test` or filtered integration + core
    - Note: dogfood slash commands remain manual checkpoint (host IDE)

    Run full test suite gate.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/first-run-flow.test.ts && turbo test --filter=@gitchange/core</automated>
  </verify>
  <done>First-run test passes; validation matrix documents all Phase 3 requirements</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → integration tests | Local fixture only; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Repudiation | First-run test gap | mitigate | Test covers index+serve+API+HTML chain |
| T-03-14 | Information Disclosure | QUICKSTART | accept | Public install instructions only |
</threat_model>

<verification>
- first-run-flow integration test passes
- QUICKSTART step count ≤ 5
- turbo test green for core + integration
</verification>

<success_criteria>
- INST-04 complete
- Phase 3 all success criteria verifiable via automated + documented manual checks
</success_criteria>

<output>
Create `.planning/phases/03-cli-plugin-scaffold/03-06-SUMMARY.md` when done
</output>
