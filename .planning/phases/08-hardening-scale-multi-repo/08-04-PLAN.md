---
phase: 08-hardening-scale-multi-repo
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - packages/dashboard/package.json
  - packages/core/src/read/graph.ts
  - packages/core/src/read/graph.test.ts
  - packages/core/src/read/unified/graph.ts
  - packages/core/src/read/unified/graph.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/graph.ts
  - packages/server/src/routes/graph.test.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/components/TemporalGraphView.tsx
  - packages/dashboard/src/components/TemporalGraphView.test.tsx
  - packages/dashboard/src/api/client.ts
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/store/drill.ts
autonomous: false
requirements: [DASH-02]
user_setup: []

must_haves:
  truths:
    - "Fifth intelligence tab Graph renders temporal-graph.json nodes and edges via @xyflow/react"
    - "Initial view shows era and inflection nodes only (≤500 nodes per MAX_GRAPH_NODES)"
    - "Clicking era node drills timeline via useDrillStore; commit node drills commit detail"
    - "Multi-repo workspace: graph nodes carry repoId; RepoBadge shown on node panel"
    - "GET /api/graph returns pre-built artifact; no es-git in server"
  artifacts:
    - path: "packages/dashboard/src/components/TemporalGraphView.tsx"
      provides: "Interactive temporal knowledge graph UI"
      exports: ["TemporalGraphView"]
    - path: "packages/server/src/routes/graph.ts"
      provides: "GET /api/graph with optional repoId filter"
    - path: "packages/core/src/read/unified/graph.ts"
      provides: "readGraphUnified for workspace federation"
      exports: ["readGraphUnified"]
  key_links:
    - from: "packages/dashboard/src/components/TemporalGraphView.tsx"
      to: "packages/dashboard/src/store/drill.ts"
      via: "node click sets eraId or commitSha with repoId"
      pattern: "useDrillStore|setSelectedEraId|setSelectedCommitSha"
    - from: "packages/server/src/routes/graph.ts"
      to: "packages/core/src/semantic/graph-io.ts"
      via: "readTemporalGraph per repo"
      pattern: "readTemporalGraph|readGraphUnified"
---

<objective>
DASH-02 temporal knowledge graph UI: @xyflow/react graph tab reading pre-built temporal-graph.json, era-first render with lazy expand, federated repoId routing, and drill-down into existing Phase 5 panels.

Purpose: Completes the third dashboard surface (timeline + tours from prior phases + graph now).
Output: TemporalGraphView, graph read API, fifth intelligence tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@packages/core/src/semantic/graph-io.ts
@packages/core/src/semantic/assemble-graph.ts
@packages/dashboard/src/layout/DashboardLayout.tsx
@packages/dashboard/src/store/drill.ts
@packages/dashboard/src/components/RepoBadge.tsx
@packages/server/src/routes/eras.ts

<interfaces>
From `packages/core/src/semantic/graph-io.ts`:
```typescript
export function readTemporalGraph(gitchangeDir: string): TemporalGraphArtifactType | null;
```

From `packages/core/src/semantic/assemble-graph.ts`:
```typescript
const MAX_GRAPH_NODES = 500;
```

TemporalGraphNode types: era | commit | file | contributor | inflection

From Phase 08-03 (expected):
- `RepoBadge` component with repoId + label
- `useWorkspaceStore` for selected repo filter
- drill store extended with optional `selectedRepoId` for federated routes
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify @xyflow/react package legitimacy</name>
  <files>packages/dashboard/package.json</files>
  <action>Before `pnpm add @xyflow/react@12.11.1 -F @gitchange/dashboard`, human verifies https://www.npmjs.com/package/@xyflow/react — official React Flow package, matches STACK.md 12.11.1. Per 08-RESEARCH Package Legitimacy Audit.</action>
  <verify>
    <automated>test -f packages/dashboard/package.json &amp;&amp; grep -q @xyflow/react packages/dashboard/package.json || echo "awaiting install"</automated>
  </verify>
  <done>Human approved @xyflow/react install or flagged concern</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Graph read API + unified federation</name>
  <files>packages/core/src/read/graph.ts, packages/core/src/read/graph.test.ts, packages/core/src/read/unified/graph.ts, packages/core/src/read/unified/graph.test.ts, packages/core/src/index.ts, packages/server/src/routes/graph.ts, packages/server/src/routes/graph.test.ts, packages/server/src/app.ts</files>
  <behavior>
    - readGraph(gitchangeDir) returns parsed temporal-graph.json or null
    - readGraphUnified with 2 repos returns nodes with repoId in node data envelope
    - GET /api/graph returns 404 when artifact missing
    - GET /api/graph?repoId= filters to single repo slice
    - Server module has no es-git import
  </behavior>
  <action>
    Add thin `readGraph` wrapper around `readTemporalGraph` mapping nodes to API DTO `{ id, type, repoId?, data }` where data holds drill targets (eraId from node id prefix, commit sha from commit nodes). `readGraphUnified(ctx, { repoId? })` merges graphs from workspace repos with `${repoId}:` node id prefix to avoid collisions per P8-D-11 pattern. Server route GET /api/graph wired in app.ts; when workspace absent, single-repo read from server gitchangeDir. Extend drill-related DTOs with optional repoId per P8-D-14. Grep gate: no es-git in packages/server.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/read/graph.test.ts src/read/unified/graph.test.ts &amp;&amp; pnpm exec vitest run packages/server/src/routes/graph.test.ts --passWithNoTests</automated>
  </verify>
  <done>Graph API returns federated artifact; route tests pass</done>
</task>

<task type="auto">
  <name>Task 2: TemporalGraphView + Graph intelligence tab</name>
  <files>packages/dashboard/package.json, packages/dashboard/src/components/TemporalGraphView.tsx, packages/dashboard/src/components/TemporalGraphView.test.tsx, packages/dashboard/src/api/client.ts, packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/store/drill.ts</files>
  <action>
    Install @xyflow/react per P8-D-12. Add fifth tab `graph` to IntelligenceTab union and DashboardLayout tab bar (Timeline | Decisions | Open work | Tours | Graph). TemporalGraphView: ReactFlow canvas with era/inflection nodes visible initially per P8-D-13; expand era node toggles child commit nodes already present in artifact (filter edges by era_contains_commit). Style era nodes as horizontal bands by repo color when workspace active. On node click: era → setSelectedEraId + switch to timeline tab; commit → setSelectedCommitSha with repoId; inflection → select era parent. Show RepoBadge in node detail sidebar when repoId present. fetchGraph via react-query in App.tsx when graph tab active. Unit test: renders nodes from mock artifact; click handler calls drill store setters. No live git in dashboard bundle.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard test 2>/dev/null || pnpm exec vitest run packages/dashboard/src/components/TemporalGraphView.test.tsx</automated>
  </verify>
  <done>Graph tab renders; node click drills; multi-repo badges visible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph API | Serves pre-built JSON only — no user-supplied graph mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | DoS | graph render | mitigate | P8-D-13 era-first; MAX_GRAPH_NODES at write time |
| T-08-08 | Tampering | client node ids | mitigate | drill targets validated server-side; client uses API DTO only |
| T-08-SC | Tampering | @xyflow/react npm | mitigate | blocking human checkpoint before install |
</threat_model>

<verification>
- Dashboard shows all five tabs including Graph
- Graph loads from /api/graph without network beyond localhost
- Single-repo mode: graph works without workspace.json
</verification>

<success_criteria>
- DASH-02 temporal knowledge graph view live alongside timeline and tour player
- Pre-built index only; no live git walks
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-04-SUMMARY.md` when done
</output>
