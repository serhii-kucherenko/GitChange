---
phase: 01-index-foundation
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - packages/core/src/artifacts/db.ts
  - packages/core/src/artifacts/writer.ts
  - packages/core/src/index/full.ts
  - packages/core/src/index/incremental.ts
  - packages/core/src/index/gitignore-guard.ts
  - packages/core/src/index.ts
  - packages/core/src/index/full.test.ts
  - packages/core/src/index/incremental.test.ts
autonomous: true
requirements: [INGX-01, INGX-04]
user_setup: []

must_haves:
  truths:
    - "Indexing a tiny synthetic repo produces a schema-valid .gitchange/ (index.sqlite + manifest.json) with no network access"
    - "commits, authors, and file_changes rows are written; file_changes carry resolvable evidence[]"
    - "The pipeline order is parse → privacy gate → Zod validate → batched write → manifest (written last)"
    - "A re-index after one new commit processes only commits after lastIndexedCommit (no full rescan)"
    - "First index adds .gitchange/ to the repo's .gitignore if not already present"
  artifacts:
    - path: "packages/core/src/index/full.ts"
      provides: "Cold-start end-to-end index orchestrator (the Walking Skeleton)"
      min_lines: 50
    - path: "packages/core/src/index/incremental.ts"
      provides: "Incremental pushRange re-index from manifest cursor"
      contains: "pushRange"
    - path: "packages/core/src/artifacts/writer.ts"
      provides: "Zod-validated batched SQLite transactions"
      contains: "transaction"
    - path: "packages/core/src/index.ts"
      provides: "Node-only public API (indexFull, indexIncremental)"
      contains: "export"
  key_links:
    - from: "packages/core/src/index/full.ts"
      to: "packages/core/src/privacy/index.ts"
      via: "applyPrivacy before write"
      pattern: "applyPrivacy|Privacy"
    - from: "packages/core/src/artifacts/writer.ts"
      to: "packages/core/src/schema/zod/file-change.ts"
      via: "Zod parse before insert"
      pattern: "parse"
    - from: "packages/core/src/index/incremental.ts"
      to: "packages/core/src/schema/manifest.ts"
      via: "reads lastIndexedCommit cursor"
      pattern: "lastIndexedCommit"
---

<objective>
Assemble the Walking Skeleton: the end-to-end index orchestrator that walks a local repo, runs each commit through parse → privacy gate → Zod validation → batched SQLite write, and writes `manifest.json` last. Deliver both cold-start (`indexFull`) and incremental (`indexIncremental`) paths, plus the Node-only public API. This is the first point the system proves its core value: **index a local clone into a trustworthy `.gitchange/` with no network** (INGX-01) and re-index only new commits (INGX-04).

Purpose: Locks the two foundational commitments (two-phase split, evidence contract at write boundary) into a runnable pipeline. Proves the SKELETON.md architecture end to end (one real git read + one real SQLite write).
Output: `artifacts/db.ts`, `artifacts/writer.ts`, `index/full.ts`, `index/incremental.ts`, `index/gitignore-guard.ts`, `src/index.ts` exports, integration tests over the synthetic fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-index-foundation/SKELETON.md
@.planning/phases/01-index-foundation/01-RESEARCH.md
@.planning/phases/01-index-foundation/01-PATTERNS.md
@.planning/phases/01-index-foundation/01-CONTEXT.md

<interfaces>
<!-- Consumes: schema/zod/*, schema/manifest (P02); ingestion/* (P03); privacy/* (P04). -->
<!-- Produces the public API consumed by Phase 3 CLI and Phase 5 server. -->
```typescript
// src/index.ts (Node-only public surface)
export interface IndexOptions { repoPath: string; gitchangeDir?: string; batchSize?: number; maxBlobBytes?: number; }
export interface IndexResult { commitsIndexed: number; fileChanges: number; manifest: Manifest; }
export function indexFull(opts: IndexOptions): Promise<IndexResult>;
export function indexIncremental(opts: IndexOptions): Promise<IndexResult>; // no-op-ish when up to date

// artifacts/db.ts
export function openDb(gitchangeDir: string): DrizzleDb; // WAL + synchronous=NORMAL
// artifacts/writer.ts — Zod-validated, batched (500–1000 rows)
export function createWriter(db: DrizzleDb, batchSize: number): {
  addCommit(r: CommitRecord): void;
  addFileChange(r: FileChangeRecord): void;   // Zod-parsed (evidence[] required) before buffering
  addSecretFinding(f: { commitSha: string; filePath: string | null; ruleId: string; location: string }): void;
  flush(): void;                              // db.transaction over buffered rows
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQLite client + Zod-validated batched writer</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 267–296 (db.ts + writer.ts patterns)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 300–304 (Pattern 4 batching), 405–424 (§3 client/WAL)
    - `.planning/phases/01-index-foundation/SKELETON.md` (SQLite Table Contract, Invariants)
  </read_first>
  <files>packages/core/src/artifacts/db.ts, packages/core/src/artifacts/writer.ts</files>
  <action>
    `db.ts`: `openDb(gitchangeDir)` creates `${gitchangeDir}/index.sqlite` (mkdir -p), sets `pragma('journal_mode = WAL')` and `pragma('synchronous = NORMAL')`, returns `drizzle({ client, schema })`. `writer.ts`: `createWriter(db, batchSize)` buffers rows and flushes via a single synchronous `db.transaction(...)` per batch (Pattern 4; trailing `()` to invoke). `addFileChange`/doc records MUST `schema/zod/file-change` `.parse()` (evidence[] enforced) BEFORE buffering — reject writes lacking resolvable evidence (D-16). `addCommit`/`addSecretFinding` insert evidence-source rows (no evidence[] field). Upsert authors by `(name,email)` and reference `authorId`/`committerId`. Never string-concatenate SQL — Drizzle parameterized inserts only (V5/SQLi, T-05-03). Flush automatically at `batchSize` (default 750) and on `flush()`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/artifacts</automated>
  </verify>
  <acceptance_criteria>
    - `openDb` produces a WAL-mode SQLite file under the given `.gitchange/` dir.
    - Writer buffers and flushes in batched transactions; author dedupe by (name,email) works.
    - `addFileChange` rejects records without `evidence[].min(1)` (Zod parse throws before insert).
    - No raw SQL string concatenation anywhere in the writer.
  </acceptance_criteria>
  <done>Batched, Zod-validated SQLite persistence layer with WAL and evidence enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Walking Skeleton — indexFull orchestrator + gitignore guard + public API</name>
  <read_first>
    - `.planning/phases/01-index-foundation/SKELETON.md` (Thinnest End-to-End Path, Invariants 1–5)
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 300–306 (full.ts flow), 344–348 (index.ts exports), 370–383 (privacy-gate-before-persist, manifest-last)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 179–217 (pipeline diagram), 305–310 (anti-patterns)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-01, D-02, D-06, D-16, D-17, D-18
  </read_first>
  <files>packages/core/src/index/full.ts, packages/core/src/index/gitignore-guard.ts, packages/core/src/index.ts</files>
  <action>
    `full.ts`: `indexFull({repoPath, gitchangeDir=`${repoPath}/.gitchange`, batchSize=750, maxBlobBytes})` — `openRepo` → `walkFromHead` (stream) → for each SHA: `parseCommit` + `diffCommit`; run each file change's captured content (when applicable) and the commit message through `applyPrivacy` (ignore → redaction) BEFORE building records; attach `evidence:[{type:'file',path,commitSha}]` to each FileChangeRecord and `evidence:[{type:'commit',sha}]` semantics for narrative records; push through the writer (Zod-validated, batched). Cap blob content at `maxBlobBytes` (default e.g. 1 MiB) and skip binary (T-04-03/T-05-02). After the walk: `writer.flush()`, then write `manifest.json` LAST with `schemaVersion`, `lastIndexedCommit = repo HEAD sha`, `indexedAt`, `repo{head,branch}`, `indexCompleteness: "complete"` (freshness downgrades come in Plan 06), empty `warnings[]`. Echo a one-line completion summary to stdout (D-06). `gitignore-guard.ts`: `ensureGitignored(repoRoot)` adds a `.gitchange/` line to the repo's `.gitignore` if absent (D-18); idempotent. `src/index.ts`: export `indexFull`, `indexIncremental` (Task 3), and shared types — top-level re-exports only, no `export default`, Node-only. Enforce pipeline order exactly parse → privacy → Zod → write → manifest (Invariant 2 — never reorder).
    Assert local-only: no network imports/calls anywhere in the index path (PRIV-01).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/index/full</automated>
  </verify>
  <acceptance_criteria>
    - Indexing `BASIC_SCENARIO` yields `.gitchange/index.sqlite` (commits+authors+file_changes populated) and a schema-valid `manifest.json` with `indexCompleteness:"complete"` and `lastIndexedCommit === HEAD`.
    - Every `file_changes` row has `evidence[]` whose refs resolve to existing commits/paths (spot-checked in test).
    - `manifest.json` is written only after all DB writes complete (last step).
    - First index adds `.gitchange/` to the repo `.gitignore` (idempotent on re-run).
    - No network access during indexing.
  </acceptance_criteria>
  <done>End-to-end cold-start index works on a synthetic repo — the Walking Skeleton is proven (INGX-01).</done>
</task>

<task type="auto">
  <name>Task 3: Incremental re-index from manifest cursor</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 308–319 (incremental.ts, foundational rule)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 426–436 (§4 pushRange), 329–333 (Pitfall 1)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-05 (force-push interaction — full detection is Plan 06; here just consume the cursor)
  </read_first>
  <files>packages/core/src/index/incremental.ts, packages/core/src/index/incremental.test.ts, packages/core/src/index/full.test.ts</files>
  <action>
    `incremental.ts`: `indexIncremental(opts)` reads `manifest.lastIndexedCommit`; if no manifest exists, delegate to `indexFull`. Otherwise walk `walkRange(repo, cursor)` (`'<cursor>..HEAD'`) and process ONLY new commits through the same parse→privacy→Zod→write pipeline (extract the shared per-commit step used by `full.ts` so both paths share it — avoid duplication). If HEAD equals the cursor, it is a no-op that just refreshes `indexedAt`. Update `manifest.lastIndexedCommit = HEAD` at the end. This file is the primary safeguard against Pitfall 1 — never full-rescan on a warm run. (Force-push detection that would halt the incremental merge is added in Plan 06; here, assume the cursor is reachable.) Add `incremental.test.ts`: index `BASIC_SCENARIO`, append one new commit to the fixture, run `indexIncremental`, assert only the new commit's rows were added (commit count increased by exactly 1; older commits not reprocessed). Extend `full.test.ts` with a no-network assertion and re-run stability (indexFull twice yields consistent manifest HEAD).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/index/incremental</automated>
  </verify>
  <acceptance_criteria>
    - After one new commit, `indexIncremental` inserts exactly the new commit's rows; prior commits are not reprocessed (grep/count assertion).
    - Missing manifest → delegates to full index.
    - HEAD == cursor → no-op refresh of `indexedAt`; `lastIndexedCommit` advanced to HEAD on each run.
    - Per-commit processing shared between full and incremental (no duplicated pipeline).
  </acceptance_criteria>
  <done>Incremental re-index processes only new commits (INGX-04); two-phase split enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion records → privacy gate → writer | Untrusted content must be redacted before it is persisted |
| orchestrator → local filesystem | Writes only under `.gitchange/`; touches repo `.gitignore` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure (V8) | write path ordering | mitigate | Pipeline order parse→privacy→Zod→write is fixed; content passes `applyPrivacy` before reaching the writer (Invariant 2). Verified by Plan 08 secret-grep on built fixture DB. |
| T-05-02 | Denial of Service (V12) | large/binary blobs | mitigate | Cap content at `maxBlobBytes`; skip binary deltas; stream the revwalk (never materialize full history — Pitfall 1). |
| T-05-03 | Tampering (SQLi, V5) | writer inserts | mitigate | Drizzle parameterized inserts only; commit content never interpolated into SQL. |
| T-05-04 | Tampering (V12 path safety) | manifest/db write paths | mitigate | Outputs written only under the resolved `.gitchange/` dir; commit-supplied paths are stored as data, never used to open write targets. |
| T-05-05 | Information Disclosure (V4) | `.gitchange/` file perms | accept | Respect umask; `.gitchange/` gitignored (D-17) and local-only; documented as local artifact. |
</threat_model>

<verification>
- `pnpm vitest run packages/core/src/artifacts` and `.../src/index` green.
- `turbo test --filter=@gitchange/core` green (wave merge gate).
- Indexing a fixture produces schema-valid `.gitchange/` with no network (INGX-01) and incremental processes only new commits (INGX-04).
- `pnpm exec tsc -p packages/core/tsconfig.json --noEmit` passes.
</verification>

<success_criteria>
- Walking Skeleton runs end to end: local git read → privacy gate → Zod → batched SQLite write → manifest-last, no network (INGX-01).
- Incremental re-index uses the manifest cursor and never full-rescans (INGX-04, Pitfall 1).
- Evidence contract enforced on every written narrative record; `.gitchange/` auto-gitignored (D-16, D-17, D-18).
</success_criteria>

<output>
Create `.planning/phases/01-index-foundation/01-05-SUMMARY.md` when done.
</output>
