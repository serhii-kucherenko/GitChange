---
phase: 08-hardening-scale-multi-repo
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/core/src/schema/zod/evidence.ts
  - packages/core/src/schema/zod/evidence.test.ts
  - packages/core/src/schema/zod/tours.ts
  - packages/core/src/read/unified/commits.ts
  - packages/core/src/read/unified/commits.test.ts
  - packages/core/src/read/unified/tours.ts
  - packages/core/src/read/unified/tours.test.ts
  - packages/core/src/read/unified/workspace-context.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/workspace.ts
  - packages/server/src/routes/commits.ts
  - packages/server/src/routes/tours.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/store/workspace.ts
  - packages/dashboard/src/components/RepoBadge.tsx
  - packages/dashboard/src/components/CommitList.tsx
  - packages/dashboard/src/components/EraTimeline.tsx
  - packages/dashboard/src/components/TourStopCard.tsx
  - packages/dashboard/src/App.tsx
autonomous: true
requirements: [MULTI-01, MULTI-02]
user_setup: []

must_haves:
  truths:
    - "Unified commit list shows repoId badge on every row when workspace has multiple repos"
    - "Commits sorted by committedAt desc with repoId tiebreaker — no implied cross-repo causality"
    - "Tour stops display repo attribution; merged tour IDs are namespaced with repoId prefix"
    - "GET /api/workspace returns repos and manual links for dashboard repo picker"
    - "Filtering by repoId returns single-repo slice without cross-contamination"
  artifacts:
    - path: "packages/core/src/read/unified/commits.ts"
      provides: "listCommitsUnified with repoId on CommitSummary"
      exports: ["listCommitsUnified"]
    - path: "packages/core/src/read/unified/tours.ts"
      provides: "mergeToursForWorkspace with repoId on stops"
      exports: ["mergeToursForWorkspace"]
    - path: "packages/server/src/routes/workspace.ts"
      provides: "GET /api/workspace"
    - path: "packages/dashboard/src/components/RepoBadge.tsx"
      provides: "repo label chip in timeline, commits, tours"
  key_links:
    - from: "packages/server/src/routes/commits.ts"
      to: "packages/core/src/read/unified/commits.ts"
      via: "workspace-aware list handler"
      pattern: "listCommitsUnified"
    - from: "packages/dashboard/src/components/TourStopCard.tsx"
      to: "packages/dashboard/src/components/RepoBadge.tsx"
      via: "stop.repoId prop"
      pattern: "RepoBadge"
---

<objective>
MULTI-02 federation read layer: repoId on evidence and API DTOs, unified commit/tour merge with explicit attribution, server workspace routes, and dashboard badges — manual cross-repo links only per PITFALLS #11.

Purpose: Delivers unified timeline and tours with provenance before graph UI (08-04).
Output: unified read modules, extended APIs, RepoBadge across timeline/tour/commit surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@packages/core/src/read/commits.ts
@packages/core/src/schema/zod/evidence.ts
@packages/server/src/routes/commits.ts
@packages/dashboard/src/components/CommitList.tsx
@packages/dashboard/src/components/EraTimeline.tsx

<interfaces>
From `packages/core/src/read/commits.ts`:
```typescript
export function listCommits(
  gitchangeDir: string,
  options: ListCommitsOptions,
): ListCommitsResult;
```

From `packages/core/src/schema/zod/evidence.ts` — extend each variant with optional `repoId`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repoId on Evidence + unified commit merge</name>
  <files>packages/core/src/schema/zod/evidence.ts, packages/core/src/schema/zod/evidence.test.ts, packages/core/src/read/unified/workspace-context.ts, packages/core/src/read/unified/commits.ts, packages/core/src/read/unified/commits.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - listCommitsUnified with 2 repos returns commits from both with distinct repoId fields
    - Sort order: committedAt desc, repoId asc, sha desc (P8-D-09)
    - repoId filter returns only matching repo commits
    - Evidence with repoId round-trips Zod parse
  </behavior>
  <action>
    Per P8-D-08 add optional `repoId` to all Evidence variants (backward compatible). Implement `resolveWorkspaceContext(workspacePath)` loading workspace.json + validating each gitchangeDir manifest. `listCommitsUnified(ctx, options)` fans out to per-repo `listCommits`, merges pages with k-way merge on sort key, supports `repoId` filter and cursor encoding `(committedAt, repoId, sha)` base64url. When workspace absent, delegate to single-repo listCommits with primary repoId omitted. No SQLite schema migration — federation is read-only overlay per P8-D-06.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/read/unified/commits.test.ts src/schema/zod/evidence.test.ts</automated>
  </verify>
  <done>Unified commit merge tests pass; evidence repoId optional</done>
</task>

<task type="auto">
  <name>Task 2: Unified tours + server/dashboard repo attribution</name>
  <files>packages/core/src/schema/zod/tours.ts, packages/core/src/read/unified/tours.ts, packages/core/src/read/unified/tours.test.ts, packages/server/src/routes/workspace.ts, packages/server/src/routes/commits.ts, packages/server/src/routes/tours.ts, packages/server/src/app.ts, packages/dashboard/src/store/workspace.ts, packages/dashboard/src/components/RepoBadge.tsx, packages/dashboard/src/components/CommitList.tsx, packages/dashboard/src/components/EraTimeline.tsx, packages/dashboard/src/components/TourStopCard.tsx, packages/dashboard/src/App.tsx</files>
  <action>
    Extend TourStop schema with optional `repoId` (required when workspace repos.length &gt; 1 at write validation in merge layer). `mergeToursForWorkspace` per P8-D-11: prefix tour/chapter/stop ids with `${repoId}:`; attach repoId to every stop; cross-repo topic tours only when workspace.links[] references both repoIds (surface link label in tour description, do not auto-create). Server: GET /api/workspace; extend GET /api/commits and /api/tours to use unified readers when workspace.json present. Dashboard: `useWorkspaceStore` for selected repo filter (all | single); RepoBadge on CommitList rows, EraTimeline items (repo-colored lane per P8-D discretion), TourStopCard. Drill actions pass repoId to drill store so federated commit detail hits correct gitchangeDir. Extend App.tsx data fetching.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/read/unified/tours.test.ts &amp;&amp; pnpm --dir packages/server test 2>/dev/null || pnpm exec vitest run packages/server/src --passWithNoTests</automated>
  </verify>
  <done>Two-repo fixture shows repo badges; repo filter works; tours merged with prefixed IDs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unified merge | Must not invent cross-repo narrative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | cross-repo inference | mitigate | P8-D-10 manual links only; no proximity-based decision merge |
| T-08-06 | Spoofing | repoId on evidence | mitigate | repoId assigned server-side from workspace registry, not client input |
</threat_model>

<verification>
- Multi-repo integration: two indexed repos, unified /api/commits returns both with repoId
- Single-repo regression: no workspace.json — identical API shape minus repoId
</verification>

<success_criteria>
- MULTI-02: unified timeline and tours show explicit repo attribution
- PITFALLS #11: no date-only cross-repo narrative
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-03-SUMMARY.md` when done
</output>
