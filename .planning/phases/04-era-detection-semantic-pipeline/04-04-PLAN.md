---
phase: 04-era-detection-semantic-pipeline
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-03]
files_modified:
  - packages/core/src/verify/semantic-integrity.ts
  - packages/core/src/verify/semantic-integrity.test.ts
  - packages/core/src/verify/semantic-snapshot.ts
  - packages/core/src/schema/manifest.ts
  - packages/core/src/schema/manifest.test.ts
  - packages/core/src/semantic/pipeline.ts
  - packages/core/src/semantic/pipeline.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [ERA-01, ERA-02, ERA-03]
user_setup: []

must_haves:
  truths:
    - "checkSemanticIntegrity validates eras.json and temporal-graph.json evidence refs against index"
    - "Every era claim and inflection evidence resolves to indexed commits/files/docs"
    - "Graph node IDs referenced by edges exist; era→commit edges point to commits in era window"
    - "manifest.json records semantic checkpoint only when integrity passes"
  artifacts:
    - path: "packages/core/src/verify/semantic-integrity.ts"
      provides: "checkErasIntegrity, checkTemporalGraphIntegrity, checkSemanticIntegrity"
      exports: ["checkSemanticIntegrity"]
    - path: "packages/core/src/semantic/pipeline.ts"
      provides: "runSemanticPipeline: assemble graph + integrity + manifest update"
      exports: ["runSemanticPipeline"]
  key_links:
    - from: "packages/core/src/semantic/pipeline.ts"
      to: "packages/core/src/verify/semantic-integrity.ts"
      via: "gate manifest write on integrity ok"
      pattern: "checkSemanticIntegrity"
    - from: "packages/core/src/verify/semantic-integrity.ts"
      to: "packages/core/src/schema/zod/evidence.ts"
      via: "doc + file + commit ref resolution"
      pattern: "case \"doc\"|case \"commit\""
---

## Phase Goal

**As a** new team member, **I want to** understand how the project evolved through named eras and inflection points backed by evidence, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Deliver deterministic graph reviewer and semantic pipeline orchestration: referential integrity checks for eras + temporal graph, manifest semantic checkpoint, and `runSemanticPipeline` glue.

Purpose: ROADMAP success criterion #4 — temporal graph passes referential integrity before downstream consumption. Mirrors Phase 2 intelligence-integrity pattern.
Output: verify helpers, pipeline orchestrator, manifest extension, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-era-detection-semantic-pipeline/04-RESEARCH.md
@packages/core/src/verify/intelligence-integrity.ts
@packages/core/src/schema/manifest.ts

<interfaces>
```typescript
export interface IntelligenceIntegrityReport {
  ok: boolean;
  errors: string[];
  danglingCommitRefs: string[];
  danglingFileRefs: Array<{ path: string; commitSha: string }>;
}
// Mirror for semantic artifacts
export function checkIntelligenceIntegrity(gitchangeDir: string): IntelligenceIntegrityReport;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic integrity checker</name>
  <files>packages/core/src/verify/semantic-integrity.ts, packages/core/src/verify/semantic-integrity.test.ts</files>
  <behavior>
    - checkErasIntegrity: all evidence refs (era, claims, inflections) resolve; signalIds exist in intelligence.json; commit SHAs in index; doc refs match doc_snapshots
    - checkTemporalGraphIntegrity: all edge source/target node ids exist; era_contains_commit commits fall within era time window; inflection nodes linked to parent era
    - checkSemanticIntegrity combines both + returns unified report
    - Handles doc evidence type in switch with exhaustive never default
  </behavior>
  <action>
    Implement following intelligence-integrity pattern: openDb, load artifacts, collect all Evidence refs, validate against commits/file_changes/doc_snapshots tables. Add graph-specific checks for node/edge consistency. Test with valid fixture (ok: true) and deliberately dangling SHA (ok: false).
  </action>
  <verify>
    <automated>pnpm --dir packages/core exec vitest run packages/core/src/verify/semantic-integrity.test.ts</automated>
  </verify>
  <done>Integrity checker catches dangling refs; valid golden fixture passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manifest semantic checkpoint + pipeline</name>
  <files>packages/core/src/schema/manifest.ts, packages/core/src/schema/manifest.test.ts, packages/core/src/semantic/pipeline.ts, packages/core/src/semantic/pipeline.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - ManifestSchema extended with semanticComputedAt, semanticHeadSha, semanticSchemaVersion (optional fields, additive per D-03)
    - runSemanticPipeline(gitchangeDir): requires eras.json → assembleAndWriteTemporalGraph → checkSemanticIntegrity → update manifest on success; throws on integrity failure without setting checkpoint
  </behavior>
  <action>
    Extend manifest.ts additively (P4-D-01). runSemanticPipeline does not invoke LLM — assumes eras.json already written by host AI (04-02). Export from core index. pipeline.test.ts uses fixture eras + BASIC_SCENARIO index.
  </action>
  <verify>
    <automated>pnpm --dir packages/core exec vitest run packages/core/src/semantic/pipeline.test.ts packages/core/src/schema/manifest.test.ts</automated>
  </verify>
  <done>Successful pipeline sets semantic checkpoint; failed integrity leaves checkpoint unset</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Semantic artifacts → downstream Phase 5 | Integrity gate is trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Dangling graph edges | mitigate | checkTemporalGraphIntegrity before manifest checkpoint |
| T-04-10 | Tampering | Fabricated era claims | mitigate | checkErasIntegrity on every claim evidence ref |
</threat_model>

<verification>
- semantic-integrity and pipeline tests pass
- Manifest round-trip with new optional fields
</verification>

<success_criteria>
- Graph reviewer rejects invalid artifacts
- manifest semantic checkpoint set only on integrity pass
</success_criteria>

<output>
Create `.planning/phases/04-era-detection-semantic-pipeline/04-04-SUMMARY.md` when done
</output>
