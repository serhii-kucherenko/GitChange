---
phase: 06-decisions-status-open-work
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - packages/core/src/read/decisions.ts
  - packages/core/src/read/decisions.test.ts
  - packages/core/src/read/open-work.ts
  - packages/core/src/read/open-work.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/decisions.ts
  - packages/server/src/routes/open-work.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/types.ts
  - packages/dashboard/src/components/DecisionsPanel.tsx
  - packages/dashboard/src/components/OpenThreadsPanel.tsx
  - packages/dashboard/src/components/MigrationThreadPanel.tsx
  - packages/dashboard/src/components/OpenWorkBadge.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/store/drill.ts
  - tests/integration/decisions-api.test.ts
autonomous: true
requirements: [DEC-02, STAT-02, TIME-04, EVD-03]
user_setup: []

must_haves:
  truths:
    - "User browses decisions list with status, evidence count, supersession, and review badges"
    - "Below-threshold decision detail shows EVD03_GAP_MESSAGE not fabricated summary"
    - "Open threads panel lists in-flight work with confidence scores"
    - "Selecting a migration thread shows chronological events and drills to commit on click"
  artifacts:
    - path: "packages/server/src/routes/decisions.ts"
      provides: "GET /api/decisions and GET /api/decisions/:id"
      exports: ["decisionsRoutes"]
    - path: "packages/server/src/routes/open-work.ts"
      provides: "GET /api/open-work and GET /api/open-work/:id"
      exports: ["openWorkRoutes"]
    - path: "packages/dashboard/src/components/DecisionsPanel.tsx"
      provides: "DEC-02 browse UI"
    - path: "packages/dashboard/src/components/MigrationThreadPanel.tsx"
      provides: "TIME-04 thread navigation"
  key_links:
    - from: "DecisionsPanel"
      to: "/api/decisions"
      via: "useQuery"
      pattern: "fetch.*api/decisions"
    - from: "MigrationThreadPanel"
      to: "drill store setSelectedCommitSha"
      via: "onEventClick"
      pattern: "setSelectedCommitSha"
    - from: "read/decisions.ts"
      to: "isBelowEvidenceThreshold"
      via: "detail response shaping"
      pattern: "EVD03_GAP_MESSAGE"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Dashboard and API vertical slice: browse decisions (DEC-02), open threads panel (STAT-02), migration thread drill-down (TIME-04), EVD-03 honest gaps in UI.

Purpose: First user-visible Phase 6 capability — answers core questions 3–5 in the dashboard.
Output: Core read queries, Hono routes, dashboard panels, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@.planning/phases/05-dashboard-evidence-drill-down/05-04-SUMMARY.md
@packages/server/src/routes/eras.ts
@packages/dashboard/src/layout/DashboardLayout.tsx
@packages/dashboard/src/store/drill.ts
@packages/core/src/decisions/threshold.ts

<interfaces>
From Phase 5 drill store pattern:
```typescript
// drill.ts — extend with selectedDecisionId, selectedThreadId
setSelectedCommitSha(sha: string): void;
setSelectedEra(era: SelectedEra | null): void;
```

From threshold.ts (06-01):
```typescript
export const EVD03_GAP_MESSAGE = "No recorded decision found";
export function isBelowEvidenceThreshold(decision: DecisionRecord): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core read queries + Hono API routes</name>
  <files>packages/core/src/read/decisions.ts, packages/core/src/read/decisions.test.ts, packages/core/src/read/open-work.ts, packages/core/src/read/open-work.test.ts, packages/core/src/index.ts, packages/server/src/routes/decisions.ts, packages/server/src/routes/open-work.ts, packages/server/src/app.ts</files>
  <behavior>
    - listDecisions returns paginated summaries sorted by confidence desc
    - getDecisionById returns full record OR { gap: EVD03_GAP_MESSAGE } when below threshold
    - listOpenWork returns threads sorted by status priority (in_progress first)
    - getOpenWorkThread returns events chronologically for TIME-04
  </behavior>
  <action>
    Implement read helpers loading decisions.json and open-work.json from gitchangeDir passed via server context (same pattern as listErasForDashboard). `getDecisionById` must call `isBelowEvidenceThreshold` and return `{ id, gap: EVD03_GAP_MESSAGE, evidence: [] }` without summary text when below floor per P6-D-03 — never paraphrase or invent. Add Zod response schemas in server routes. Register routes in app.ts before static handler. No es-git imports (SCALE-02). Export read fns from core index.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/decisions packages/core/src/read/open-work tests/integration/decisions-api</automated>
  </verify>
  <done>API returns decisions and threads on fixture; below-threshold id returns gap message only</done>
</task>

<task type="auto">
  <name>Task 2: Decisions + open threads dashboard panels</name>
  <files>packages/dashboard/src/types.ts, packages/dashboard/src/components/DecisionsPanel.tsx, packages/dashboard/src/components/OpenThreadsPanel.tsx, packages/dashboard/src/store/drill.ts, packages/dashboard/src/layout/DashboardLayout.tsx</files>
  <action>
    Add dashboard types mirroring API responses (no @gitchange/core in client bundle per P3 pattern). Build `DecisionsPanel`: virtualized list showing title, status chip, reviewStatus badge, confidence, evidence count, superseded strikethrough. Detail drawer shows evidence list with links to existing commit drill (reuse DrillBreadcrumb pattern). When API returns gap field, render EVD03_GAP_MESSAGE prominently — no fallback narrative. Build `OpenThreadsPanel`: list threads with kind icon, status, confidence, last event date. Extend drill store with selectedDecisionId and selectedThreadId. Add third column or tab in DashboardLayout for Decisions | Open Work without breaking existing era/commit/file panels.
  </action>
  <verify>
    <automated>pnpm --filter @gitchange/dashboard build</automated>
  </verify>
  <done>Dashboard builds; panels fetch from /api/decisions and /api/open-work</done>
</task>

<task type="auto">
  <name>Task 3: Migration thread panel + TIME-04 drill wiring</name>
  <files>packages/dashboard/src/components/MigrationThreadPanel.tsx, packages/dashboard/src/components/OpenWorkBadge.tsx, tests/integration/decisions-api.test.ts</files>
  <action>
    Build `MigrationThreadPanel` shown when thread selected: virtualized event list (commit summary, date, paths touched) newest-first. Click event calls setSelectedCommitSha and opens existing CommitDetailPanel. Add compact `OpenWorkBadge` component (amber in_progress, gray stale) for reuse in plan 06-06. Integration test: seed fixture with decisions + open-work, hit API list/detail, assert EVD-03 gap response shape, assert thread events non-empty and SHAs resolvable in /api/commits/:sha.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/decisions-api</automated>
  </verify>
  <done>Thread panel drills to commit detail; integration test passes on Node 22.x fixture</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | Below-threshold decisions must not leak agent summary text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | Decision detail API | mitigate | Strip summary when isBelowEvidenceThreshold |
| T-06-10 | Spoofing | Path traversal in thread paths | mitigate | Reuse validateFilePath from file-history |
</threat_model>

<verification>
- decisions-api integration test passes
- Dashboard build succeeds
- EVD-03 gap string exact match in API test assertion
</verification>

<success_criteria>
- User can browse decisions and open threads in dashboard
- TIME-04 migration thread navigates to commit drill-down
- EVD-03 enforced in API and UI
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-04-SUMMARY.md` when done
</output>
