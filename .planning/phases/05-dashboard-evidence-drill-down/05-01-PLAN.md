---
phase: 05-dashboard-evidence-drill-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/read/commits.ts
  - packages/core/src/read/commits.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/commits.ts
  - packages/server/src/app.ts
  - packages/dashboard/package.json
  - packages/dashboard/src/main.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/api/client.ts
  - packages/dashboard/src/store/drill.ts
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/components/CommitList.tsx
  - tests/integration/dashboard-commits.test.ts
autonomous: false
requirements: [DASH-01, DASH-03, DASH-04, SCALE-02]
user_setup: []

must_haves:
  truths:
    - "User opens localhost dashboard and sees a paginated commit list from indexed SQLite — not live git"
    - "Commit list scrolls smoothly with virtualization when fixture has many commits"
    - "Dashboard uses react-query for /api/commits; no @gitchange/core imports in client bundle"
    - "@gitchange/server has no es-git import (SCALE-02 grep gate)"
  artifacts:
    - path: "packages/core/src/read/commits.ts"
      provides: "listCommits cursor pagination from SQLite"
      exports: ["listCommits", "CommitListPage", "CommitSummary"]
    - path: "packages/server/src/routes/commits.ts"
      provides: "GET /api/commits"
    - path: "packages/dashboard/src/components/CommitList.tsx"
      provides: "Virtualized commit list"
    - path: "packages/dashboard/src/store/drill.ts"
      provides: "zustand drill-down state scaffold"
  key_links:
    - from: "packages/dashboard/src/components/CommitList.tsx"
      to: "/api/commits"
      via: "react-query useInfiniteQuery"
      pattern: "api/commits"
    - from: "packages/server/src/routes/commits.ts"
      to: "listCommits"
      via: "core read function"
      pattern: "listCommits"
---

## Phase Goal

**As a** new hire or maintainer, **I want to** explore project history interactively with era→commit→file drill-down on localhost, **so that** I can understand how the codebase evolved with linked evidence without hunting git log.

<objective>
Deliver the Phase 5 walking skeleton: paginated commit list API + virtualized dashboard panel wired end-to-end (DASH-01, DASH-04). Establishes read-only SCALE-02 path and multi-panel layout that later plans extend.

Purpose: Proves dashboard expands Phase 3 minimal UI in place (P3-D-05) with real drill-down data path before filters, timeline, and hunks land.
Output: GET /api/commits, DashboardLayout with CommitList, zustand drill store scaffold, react-query client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@.planning/phases/03-cli-plugin-scaffold/03-03-SUMMARY.md
@packages/dashboard/src/App.tsx
@packages/server/src/app.ts

<interfaces>
From packages/core read surface (new):
- listCommits(gitchangeDir, { limit?, cursor?, filters? }) → CommitListPage
- CommitSummary: { sha, summary, committedAt, authorName, authorEmail }
- CommitListPage: { commits: CommitSummary[]; nextCursor: string | null }
- Default limit 50 per P5-D-07; cursor encodes committedAt+sha tuple

From packages/server (existing):
- createApp({ gitchangeDir, dashboardDistPath? }) mounts routes under /api
- GET /api/snapshot already returns manifest + stats

From packages/dashboard (existing):
- fetchSnapshot() in snapshot.ts — keep for header/index status
- IndexStatusCard shows schemaVersion, warnings (DASH-03 partial)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify Phase 5 dashboard packages</name>
  <files>packages/dashboard/package.json</files>
  <action>
    Human verifies vis-timeline@8.5.1, @tanstack/react-virtual@3.14.5, @tanstack/react-query@5.101.2, zustand@5.0.14 on npm before executor runs pnpm add. Per Package Legitimacy Audit in 05-RESEARCH.md (T-05-SC).
  </action>
  <what-built>Package legitimacy gate for four STACK.md dashboard dependencies</what-built>
  <how-to-verify>
    1. Confirm each package on npmjs.com under @tanstack or expected publisher
    2. Reply "approved"
  </how-to-verify>
  <verify>
    <automated>test -f packages/dashboard/package.json</automated>
  </verify>
  <done>Human replied "approved"; executor may install dashboard packages in Task 2</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: listCommits core query + failing commits API integration test</name>
  <files>packages/core/src/read/commits.ts, packages/core/src/read/commits.test.ts, packages/core/src/index.ts, packages/server/src/routes/commits.ts, packages/server/src/app.ts, tests/integration/dashboard-commits.test.ts</files>
  <behavior>
    - listCommits on BASIC_SCENARIO fixture returns commits ordered by committedAt desc
    - Second page with cursor returns disjoint set when more than limit commits exist
    - GET /api/commits?limit=10 returns 200 JSON matching Zod response schema
    - Integration test: index fixture → serve → /api/commits returns commitCount-aligned page
    - grep: packages/server/src must not import es-git (SCALE-02)
  </behavior>
  <action>
    Implement listCommits in core using Drizzle read-only openDb — join commits + authors for display fields. Cursor pagination per P5-D-07 (committedAt desc, sha tiebreaker). Export from core index.ts.

    Add packages/server/src/routes/commits.ts with GET /commits parsing limit (default 50, max 200) and cursor query params; validate response with Zod. Mount in app.ts alongside snapshot routes.

    Write failing integration test tests/integration/dashboard-commits.test.ts mirroring dashboard-snapshot.test.ts patterns (buildRepo, gitchange index, ephemeral port serve).

    Add scripts/grep-scale02.sh or vitest test asserting @gitchange/server source tree has zero es-git imports.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/commits.test.ts tests/integration/dashboard-commits.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>listCommits tested; /api/commits integration green; server es-git grep gate passes</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard layout + virtualized CommitList + react-query</name>
  <files>packages/dashboard/package.json, packages/dashboard/src/main.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/api/client.ts, packages/dashboard/src/store/drill.ts, packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/components/CommitList.tsx</files>
  <action>
    After checkpoint approval, pnpm add to @gitchange/dashboard: @tanstack/react-query@5.101.2, @tanstack/react-virtual@3.14.5, zustand@5.0.14 (vis-timeline deferred to Plan 05-03).

    Wrap app in QueryClientProvider (main.tsx). Create api/client.ts with typed fetch helpers for /api/commits (duplicate response types locally — no core import in client per P3-D-03).

    Create zustand drill store (drill.ts) with selectedCommitSha, selectedFilePath, selectedEraId nullable fields + clearDownstream actions — scaffold only; selection handlers wire in later plans.

    Replace App.tsx single-column layout with DashboardLayout: header (GitChange + HEAD from snapshot), left IndexStatusCard (keep existing), main CommitList panel. Preserve loading/error/empty states from Phase 3.

    CommitList: useInfiniteQuery on /api/commits; @tanstack/react-virtual virtualizer with estimateSize ~44px; fetchNextPage when scroll nears end (DASH-04). Click row sets selectedCommitSha in drill store (no detail panel yet).

    Do NOT import @gitchange/core in dashboard package.
  </action>
  <verify>
    <automated>pnpm build --filter=@gitchange/dashboard && pnpm vitest run tests/integration/dashboard-commits.test.ts 2>&1 | tail -3 | grep -q "passed"</automated>
  </verify>
  <done>Dashboard builds; commit list renders from API with virtualization; drill store scaffold exists</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Hono API | Untrusted query params (limit, cursor) |
| Hono → SQLite | Read-only; parameterized queries only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | GET /api/commits | mitigate | Cap limit at 200; Zod parse response; Drizzle parameterized filters |
| T-05-02 | I | Cursor param | mitigate | Decode cursor as structured tuple; reject malformed cursors with 400 |
| T-05-SC | Tampering | npm dashboard deps | mitigate | Blocking human checkpoint before pnpm add per 05-RESEARCH audit |
</threat_model>

<verification>
- pnpm vitest run packages/core/src/read/commits.test.ts tests/integration/dashboard-commits.test.ts
- pnpm build --filter=@gitchange/dashboard
- Confirm packages/server has no es-git import
</verification>

<success_criteria>
- GET /api/commits returns paginated commits from indexed fixture
- Dashboard shows virtualized commit list at localhost after gitchange serve
- Phase 3 snapshot/index status still visible in expanded layout
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-01-SUMMARY.md` when done
</output>
