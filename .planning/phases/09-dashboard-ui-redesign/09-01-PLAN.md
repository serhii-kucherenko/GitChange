---
phase: 09-dashboard-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/App.tsx
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "The primary tab nav (Timeline · Decisions · Open work · Tours · Graph) renders as a full-width horizontal bar under the header, not inside a sidebar, and no tab label wraps to two lines"
    - "Each of the five views owns its own body layout (per-view), not a single fixed 22rem+1fr grid shared by all"
    - "Loading, empty, and error states render centered in the content region (max-w-md), not jammed into a sidebar column"
    - "Every existing behavioral test stays green and typecheck passes — no component logic, query, or store wiring changed"
  artifacts:
    - path: "packages/dashboard/src/layout/DashboardLayout.tsx"
      provides: "Shared app frame (sticky header + full-width tablist) and per-view body layouts"
      contains: "role=\"tablist\""
    - path: "packages/dashboard/src/App.tsx"
      provides: "Slot re-wiring to the restructured DashboardLayout props — behavior-neutral"
      contains: "DashboardLayout"
  key_links:
    - from: "packages/dashboard/src/App.tsx"
      to: "packages/dashboard/src/layout/DashboardLayout.tsx"
      via: "props (slots + onIntelligenceTabChange + drill callbacks)"
      pattern: "onIntelligenceTabChange"
    - from: "packages/dashboard/src/layout/DashboardLayout.tsx"
      to: "IntelligenceTab tab state"
      via: "tab buttons call onIntelligenceTabChange(tab)"
      pattern: "onIntelligenceTabChange\\("
---

<objective>
Rewrite the shared app frame and information architecture: move the primary tab nav OUT of the sidebar to a full-width horizontal bar under a sticky header, replace the single fixed `lg:grid-cols-[minmax(0,22rem)_minmax(0,1fr)]` grid with per-view layouts, raise content max-width to `max-w-[96rem]`, and center the loading/empty/error states in the content region. This is the foundation slice — it gates every other Phase 9 plan because it defines the layout slots those plans restyle into.

Purpose: Fixes the two structural defects named in the phase goal — content crammed into a narrow sidebar and dead space beside it (DASH-01 presentation, DASH-02 five-view IA). Behavior is preserved exactly; only JSX structure, class names, and copy change.

Output: Restructured `DashboardLayout.tsx` (frame + per-view body) and behavior-neutral `App.tsx` slot re-wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-dashboard-ui-redesign/09-UI-SPEC.md
@.planning/phases/09-dashboard-ui-redesign/09-RESEARCH.md

<interfaces>
<!-- Current DashboardLayout props (from packages/dashboard/src/layout/DashboardLayout.tsx). -->
<!-- The planner permits restructuring this interface as long as App.tsx (the single caller) is updated in the same task and typecheck stays green. -->

export type IntelligenceTab = "timeline" | "decisions" | "open-work" | "tours" | "graph";

Current DashboardLayoutProps slots: loadState, intelligenceTab, onIntelligenceTabChange, sidebar, fileHistory?, timeline?, commitFilterBar?, main, intelligencePanel?

Current TAB_LABELS map: timeline→"Timeline", decisions→"Decisions", open-work→"Open work", tours→"Tours", graph→"Graph"

Cross-view drill callbacks that MUST be preserved (defined in App.tsx, passed to TourPlayer/TemporalGraphView):
onDrillToTimeline = () => setIntelligenceTab("timeline")
onDrillToDecisions = () => setIntelligenceTab("decisions")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild the shared app frame — sticky header + full-width tablist</name>
  <files>packages/dashboard/src/layout/DashboardLayout.tsx</files>
  <read_first>
    - packages/dashboard/src/layout/DashboardLayout.tsx (current frame: header lines 57-70, sidebar-embedded tab nav lines 97-116, fixed grid line 72) — the file being modified
    - 09-UI-SPEC.md §Information Architecture → "App frame (shared, all views)" and §Component Treatment Contract → "Primary tab nav"
    - 09-UI-SPEC.md §Color, §Typography (header uses Display 24px/600; tab labels Label 12px, active 600)
    - 09-RESEARCH.md §"Tab navigation wiring" (Pitfall 5) — a11y must be additive, keep the handler
  </read_first>
  <action>
    Restructure the header into a sticky full-width bar: `sticky top-0 z-20 h-14 border-b border-slate-800 bg-slate-900/80 px-6 backdrop-blur`, inner row `mx-auto flex max-w-[96rem] items-center justify-between`. Left: product title `GitChange` at `text-2xl font-semibold tracking-tight`. Right: existing `attributionBadge`, then `HEAD {sha7}` at `font-mono text-xs text-slate-400`, then repo/workspace badge slot if present. Do NOT recompute attribution — keep the existing `resolveDisplayedAttribution` call and `AttributionBadge` exactly.

    Move the tab `<nav>` OUT of `<aside>` to a full-width bar directly under the header: `border-b border-slate-800 bg-slate-900/80`, inner `mx-auto flex max-w-[96rem] gap-1 px-6`. Convert to a tablist: container `role="tablist" aria-label="Primary views"`, each tab a `role="tab"` button with `aria-selected`, `whitespace-nowrap px-4 py-2` (so "Open work" never wraps), min height ≥32px. Active tab: `text-slate-100` + 2px `border-b-2 border-sky-400` underline (underline style, preferred per spec); inactive: `text-slate-400 hover:text-slate-200`. Every tab gets `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-400 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950`. Keep `onClick={() => onIntelligenceTabChange(tab)}` and the `TAB_LABELS` map unchanged; keep existing `aria-current` if retained alongside `aria-selected`. Only render the tablist when `loadState.status === "ready"`.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes (green).
    - `pnpm --dir packages/dashboard test` stays green (existing behavioral tests unchanged).
    - Header element carries `sticky top-0 z-20 h-14` and `max-w-[96rem]`; title is `text-2xl font-semibold tracking-tight`.
    - The tab nav is a sibling of the header (not a child of `<aside>`), carries `role="tablist"`, each tab has `role="tab"` + `aria-selected` + `whitespace-nowrap px-4 py-2` + the `focus-visible:ring-sky-400` ring, and the active tab has `border-b-2 border-sky-400`.
    - `onIntelligenceTabChange`, `TAB_LABELS`, and the `IntelligenceTab` union are unchanged (no handler/state relocation).
  </acceptance_criteria>
  <done>Full-width sticky header and a full-width accessible tablist render under it; typecheck + existing tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Per-view body layouts + centered states + App.tsx slot re-wiring</name>
  <files>packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/App.tsx</files>
  <read_first>
    - packages/dashboard/src/layout/DashboardLayout.tsx (grid line 72, aside states lines 74-93, per-tab slotting lines 118-138) — the file being modified
    - packages/dashboard/src/App.tsx (the single caller; slot props passed lines 225-258, drill callbacks lines 192-193 & 212) — the file being modified
    - 09-UI-SPEC.md §Information Architecture → "Per-view body layouts" table + "Load / empty / error states (frame-level)"
    - 09-UI-SPEC.md §Copywriting Contract (empty/error/loading copy strings — apply verbatim)
    - 09-RESEARCH.md Pitfall 2 (prop-shape desync — change interface + caller together, guard with typecheck)
  </read_first>
  <action>
    Replace the single fixed grid with per-view body layouts selected by `intelligenceTab`, each centered `mx-auto max-w-[96rem] px-6 py-8`. Timeline: full-width era-timeline card on top (its slot), then full-width filter bar (repo + commit filters slot), then a two-pane split `lg:grid-cols-[minmax(20rem,24rem)_minmax(0,1fr)] gap-8` (left rail = Index status + Era detail + File history stacked with `gap-6`; right = commit list filling width/height); collapses to single column below `lg`. Decisions/Open work: two-pane split `lg:grid-cols-[minmax(22rem,28rem)_minmax(0,1fr)] gap-8`. Tours: two-pane split `lg:grid-cols-[minmax(20rem,24rem)_minmax(0,1fr)] gap-8`. Graph: full-bleed within content width, canvas region `1fr` with a slim helper strip (no 22rem sidebar). You MAY restructure `DashboardLayoutProps` slots and/or extract per-view sub-layout components — either is acceptable; keep the change behavior-neutral.

    Move the loading/empty/error states OUT of `<aside>` to a frame-level centered region: wrap in `mx-auto flex min-h-[60vh] max-w-md flex-col items-center justify-center text-center`. Apply the exact copy from §Copywriting: empty heading "No analysis yet" + body "Run `/gitchange` in your IDE, or `gitchange index` in this repo, then reload to explore its history."; error surface keeps `role="alert"` with copy "Couldn't load the index. Confirm `.gitchange/` exists and re-run `gitchange index`, then reload."; loading uses "Loading index status…". Update `App.tsx` to pass the slots in the new shape and preserve the cross-view drill callbacks `onDrillToTimeline`/`onDrillToDecisions` (TourPlayer, TemporalGraphView) exactly — do not touch any query, `useState`, Zustand selector, or `useEffect`.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes (interface + single caller in sync).
    - `pnpm --dir packages/dashboard test` stays green (TemporalGraphView drill callbacks + tour store tests unaffected).
    - No `lg:grid-cols-[minmax(0,22rem)_minmax(0,1fr)]` remains; the Timeline body uses a full-width era strip + full-width filter bar + a `minmax(20rem,24rem)_minmax(0,1fr)` split; Decisions/Open work use `minmax(22rem,28rem)_minmax(0,1fr)`.
    - Loading/empty/error render in a centered `max-w-md` region (not inside `<aside>`); error keeps `role="alert"`; empty/error/loading copy matches §Copywriting verbatim.
    - Content regions use `max-w-[96rem] mx-auto px-6`.
    - `App.tsx` diff contains no changes to `useQuery`, `useState`, `useDrillStore`/`useTourStore`/`useWorkspaceStore` selectors, `useEffect`, `fetchSnapshot`, or `mergedFilters` logic; `onDrillToTimeline`/`onDrillToDecisions` still passed to TourPlayer/TemporalGraphView.
  </acceptance_criteria>
  <done>Each view renders its own body layout; states are centered; App.tsx re-wires slots with zero behavior change; typecheck + existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → local /api | Existing read-only proxy to `127.0.0.1:9876`; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | DashboardLayout/App restyle | accept | No new threat surface — presentational restyle of a read-only, localhost-only dashboard over pre-built artifacts (SCALE-02). No new inputs, network calls, auth, writes, or `dangerouslySetInnerHTML` introduced; tab handler and queries preserved. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (no new dependencies permitted); Package Legitimacy Gate is N/A. |
</threat_model>

<verification>
- `pnpm --dir packages/dashboard typecheck` green.
- `pnpm --dir packages/dashboard test` green (existing behavioral suite).
- Manual (per 09-VALIDATION.md): header + tab nav full-width, tabs never wrap, active tab `sky-400` underline, keyboard-focusable with visible ring; loading/empty/error centered `max-w-md`.
</verification>

<success_criteria>
- Full-width sticky header + full-width accessible tablist replace the sidebar-embedded nav.
- Per-view body layouts replace the single fixed 22rem+1fr grid; content width is `max-w-[96rem]`.
- Loading/empty/error states are centered in the content region with contract copy.
- Zero logic/query/store change; typecheck + existing tests green.
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-ui-redesign/09-01-SUMMARY.md` when done.
</output>
