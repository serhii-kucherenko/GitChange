---
phase: 02-repository-intelligence-ownership
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - packages/core/src/intelligence/era-ownership.ts
  - packages/core/src/intelligence/expertise.ts
  - packages/core/src/intelligence/compute.ts
  - packages/core/src/intelligence/export.ts
  - packages/core/src/intelligence/era-ownership.test.ts
  - packages/core/src/intelligence/expertise.test.ts
autonomous: true
requirements: [CONT-01, CONT-03]
user_setup: []

must_haves:
  truths:
    - "Each era window shows per-file ownership timeline — how stewardship shifted over time (CONT-01)"
    - "Contributor expertise profiles map topics to authors with evidence (CONT-03)"
    - "Expertise topics derive from path prefixes (e.g. src/auth/**) and cc_scope values"
    - "Every expertise entry includes evidence[] with commit SHA + file path resolvable in index"
    - "intelligence.json exports eraOwnership and expertise sections"
  artifacts:
    - path: "packages/core/src/intelligence/era-ownership.ts"
      provides: "Per-era ownership rollups"
    - path: "packages/core/src/intelligence/expertise.ts"
      provides: "Contributor expertise profiles"
  key_links:
    - from: "packages/core/src/intelligence/era-ownership.ts"
      to: "era_boundaries + file_changes"
      via: "ownership proxy per window"
      pattern: "era_boundaries"
    - from: "packages/core/src/intelligence/expertise.ts"
      to: "file_ownership + cc_scope"
      via: "topic scoring"
      pattern: "contributor_expertise"
---

## Phase Goal

**As a** maintainer or new team member, **I want to** see who changed what through ownership timelines and contributor expertise derived from git history, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Complete the **contributor lens data layer**: per-era ownership timelines (CONT-01) and expertise profiles for "who to ask about X" (CONT-03). Uses era boundaries from 02-03 and file-level signals from index + ownership from 02-02.

Purpose: Answers roadmap success criteria #2 and #3 without dashboard UI.
Output: `era-ownership.ts`, `expertise.ts`, export sections, contributor_expertise table rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repository-intelligence-ownership/02-RESEARCH.md
@.planning/phases/02-repository-intelligence-ownership/02-02-SUMMARY.md
@.planning/phases/02-repository-intelligence-ownership/02-03-SUMMARY.md

<interfaces>
```typescript
// era_boundaries: windows with start_at, end_at, start_commit_sha, end_commit_sha
// file_ownership: path, author_id, line_count, percentage (HEAD snapshot from 02-02)
// commits: cc_scope, author_id, committed_at
// file_changes: path, commit_sha, change_type
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing era-ownership + expertise tests</name>
  <files>packages/core/src/intelligence/era-ownership.test.ts, packages/core/src/intelligence/expertise.test.ts</files>
  <behavior>
    - Era ownership: for a file touched by author A in era1 and author B in era2, timeline shows A dominant in era1 window and B gaining share in era2 (by commit-touch proxy or blame-at-era-end if feasible — prefer commit-touch weighted by lines added from file_changes counts for Phase 2 performance)
    - Expertise: author with most commits touching src/auth/** ranks top for topic "auth" with evidence listing commit SHA + path
    - Expertise record without evidence[] fails Zod export parse
  </behavior>
  <action>
    Use OWNSHIP_SCENARIO or extended fixture. Tests call computeIntelligence after index. Fail until era-ownership and expertise modules exist. Document in test comments: era ownership uses **commit-window proxy** (sum of add/modify touches per author per era per path) — full historical blame per era deferred to Phase 8 scale work; still satisfies CONT-01 "how stewardship shifted" at era granularity.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/era-ownership.test.ts packages/core/src/intelligence/expertise.test.ts 2>&1 | grep -q "FAIL\|AssertionError" || (test ! -f packages/core/src/intelligence/era-ownership.ts && test ! -f packages/core/src/intelligence/expertise.ts)</automated>
  </verify>
  <done>Tests exist and fail for missing implementation</done>
</task>

<task type="auto">
  <name>Task 2: Per-era ownership timelines</name>
  <files>packages/core/src/intelligence/era-ownership.ts</files>
  <action>
    `computeEraOwnership(db)`: For each era boundary window, for each path with activity in window, aggregate touch score per author: count file_changes rows where commit committed_at in [start_at, end_at] and change_type not Delete-only. Normalize to percentages per path per era. Store in `era_ownership` table (created in 02-01 migration): era_id, path, author_id, touch_count, percentage, evidence_json.

    evidence_json: top commit SHA per author-path-era from highest-touch commit.

    Export `eraOwnership.eras[]` with eraId, label (signal_type + date range, not LLM name), files[{ path, authors[] }].
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/era-ownership.test.ts</automated>
  </verify>
  <done>Era ownership tests pass; stewardship shift visible across two eras in fixture</done>
</task>

<task type="auto">
  <name>Task 3: Contributor expertise profiles + export</name>
  <files>packages/core/src/intelligence/expertise.ts, packages/core/src/intelligence/compute.ts, packages/core/src/intelligence/export.ts</files>
  <action>
    `computeExpertise(db)`: Build topics:
    1) Path-prefix topics: top-level dirs (src/auth, src/api, docs) with >= 3 touches
    2) cc_scope topics from conventional commits where scope non-null
    3) Co-change clusters: optional top co-change hub paths as topic "related:{path}"

    Score authors per topic: weighted sum of HEAD ownership line % (if available) + commit touches in topic paths + cc_scope matches. Top 3 authors per topic with score 0–1 normalized.

    Persist contributor_expertise (author_id, topic, score, evidence_json). evidence[] min 1: file + commit refs from top contributing commits.

    Export `expertise.topics[]` with topic, suggestedContributors[{ authorId, name, email, score, rationale, evidence[] }].

    Wire both modules into computeIntelligence (after ownership, co-change, era signals). Extend IntelligenceArtifact Zod schema.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/era-ownership.test.ts packages/core/src/intelligence/expertise.test.ts packages/core/src/intelligence/compute.test.ts</automated>
  </verify>
  <done>CONT-01 and CONT-03 satisfied in intelligence.json; expertise evidence passes checkEvidenceIntegrity-style resolution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expertise export → host LLM | Structured JSON only; claims must be evidence-backed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing | expertise claims | mitigate | Zod evidence[].min(1); referential check in 02-05 |
| T-02-08 | Repudiation | era ownership proxy | accept | Document commit-touch proxy; HEAD blame used where available |
</threat_model>

<verification>
- Era ownership shows different top author across eras in fixture
- Expertise topic includes resolvable evidence refs
</verification>

<success_criteria>
- intelligence.json has eraOwnership and expertise sections
- CONT-01 and CONT-03 requirement behaviors verified by tests
</success_criteria>

<output>
Create `.planning/phases/02-repository-intelligence-ownership/02-04-SUMMARY.md` when done
</output>
