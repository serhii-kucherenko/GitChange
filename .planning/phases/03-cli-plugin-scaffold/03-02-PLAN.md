---
phase: 03-cli-plugin-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/tsconfig.build.json
  - packages/server/src/app.ts
  - packages/server/src/routes/snapshot.ts
  - packages/server/src/start.ts
  - packages/cli/src/bin.ts
  - packages/cli/src/commands/serve.ts
  - packages/cli/src/commands/status.ts
  - packages/core/src/read/snapshot.ts
  - tests/integration/cli-serve-status.test.ts
autonomous: false
requirements: [PLUG-03]
user_setup: []

must_haves:
  truths:
    - "User runs `gitchange status` and sees manifest freshness, commit count, and warnings without network"
    - "User runs `gitchange serve` and Hono listens on 127.0.0.1:9876 (P3-D-01)"
    - "GET /api/snapshot returns manifest + stats + intelligence highlights from pre-built index only"
  artifacts:
    - path: "packages/server/src/app.ts"
      provides: "Hono app with /api/snapshot route"
      exports: ["createApp"]
    - path: "packages/cli/src/commands/serve.ts"
      provides: "serve subcommand"
    - path: "packages/cli/src/commands/status.ts"
      provides: "status subcommand"
  key_links:
    - from: "packages/server/src/routes/snapshot.ts"
      to: "packages/core read/snapshot"
      via: "getRepoSnapshot"
      pattern: "getRepoSnapshot"
    - from: "packages/cli/src/commands/serve.ts"
      to: "packages/server"
      via: "startServer"
      pattern: "127\\.0\\.0\\.1"
---

<objective>
Add Hono localhost server with read-only snapshot API; implement `gitchange serve` and `gitchange status` CLI commands.

Purpose: Server API is the data contract for minimal dashboard (Plan 03-03). Localhost-only bind satisfies first-run dashboard safety (P3-D-01).
Output: Working serve/status commands, `/api/snapshot` JSON endpoint, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-plugin-scaffold/03-RESEARCH.md
@.planning/phases/03-cli-plugin-scaffold/03-01-SUMMARY.md

<interfaces>
From Plan 03-01 `packages/core/src/read/snapshot.ts` (create if 03-01 named differently, use same contract):
```typescript
export interface RepoSnapshot {
  manifest: Manifest | null;
  stats: { commitCount: number; fileChangeCount: number; authorCount: number };
  intelligence: IntelligenceArtifactType | null;
  highlights: { topChurnFiles: Array<{ path: string; changeCount: number }>; topExpertiseTopics: Array<{ topic: string; label: string }> };
}
export function getRepoSnapshot(gitchangeDir: string): RepoSnapshot;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify hono packages before install</name>
  <files>packages/server/package.json</files>
  <action>
    Human verifies hono@4.12.27 and @hono/node-server@2.0.6 on npm before Task 2 runs pnpm add. Executor pauses until resume signal received.
  </action>
  <what-built>hono@4.12.27 and @hono/node-server@2.0.6 per STACK.md</what-built>
  <how-to-verify>
    1. Confirm https://www.npmjs.com/package/hono publisher and @hono scope on node-server
    2. Reply "approved" to proceed
  </how-to-verify>
  <verify>
    <automated>test -f packages/server/package.json</automated>
  </verify>
  <done>Human replied "approved"; executor may install hono packages in Task 2</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for serve and status</name>
  <files>tests/integration/cli-serve-status.test.ts</files>
  <behavior>
    - Index BASIC_SCENARIO repo via core indexFull + computeIntelligence (or gitchange index)
    - `gitchange status --repo <dir>` exits 0 and stdout contains commit count and lastIndexedCommit substring
    - Start server in subprocess on port 9876 (or random port via env); GET /api/snapshot returns 200 JSON with manifest.schemaVersion and stats.commitCount >= 1
    - Server binds 127.0.0.1 only (not 0.0.0.0)
  </behavior>
  <action>
    Write integration test using vitest + child_process. Test fails initially because serve/status commands and server app don't exist.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/cli-serve-status.test.ts 2>&1 | grep -qE "FAIL|AssertionError" || test ! -f packages/server/src/app.ts</automated>
  </verify>
  <done>Tests exist and fail correctly</done>
</task>

<task type="auto">
  <name>Task 2: Hono server + serve/status commands</name>
  <files>packages/server/src/app.ts, packages/server/src/routes/snapshot.ts, packages/server/src/start.ts, packages/server/tsconfig.build.json, packages/cli/src/commands/serve.ts, packages/cli/src/commands/status.ts, packages/cli/src/bin.ts, packages/core/src/read/snapshot.ts</files>
  <action>
    Implement `createApp({ gitchangeDir })` in server package: Hono app with `GET /api/health` → `{ ok: true }` and `GET /api/snapshot` → JSON from `getRepoSnapshot`. Validate responses with Zod at route boundary. Return 404 JSON `{ error: 'not_indexed' }` when manifest null.

    `startServer({ host: '127.0.0.1', port: Number(process.env.GITCHANGE_PORT ?? 9876), gitchangeDir })` using @hono/node-server serve(). Export for CLI.

    `gitchange serve` flags: `--repo`, `--port`, `--gitchange-dir`. Resolve repo like index command. Print listening URL on start.

    `gitchange status` prints human-readable table: head sha, last indexed, indexedAt, indexCompleteness, warning codes, commit/file/author counts, intelligence computedAt if present. Exit 1 with message if not indexed.

    Extend `getRepoSnapshot` if needed: slice top 5 churn files and top 3 expertise topics for highlights per P3-D-05.

    Wire commander subcommands in bin.ts.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/cli-serve-status.test.ts && pnpm build --filter=@gitchange/cli --filter=@gitchange/server</automated>
  </verify>
  <done>serve and status pass integration tests; API returns snapshot JSON</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/snapshot | Localhost-only; no auth in v1 |
| Server → SQLite | Read-only queries; gitchangeDir from CLI flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Hono bind address | mitigate | Default host 127.0.0.1; reject 0.0.0.0 unless explicit `--host` flag documented as unsafe |
| T-03-04 | Denial of Service | Large SQLite read | accept | COUNT queries only; no full table scans in snapshot |
| T-03-SC | Tampering | hono npm install | mitigate | Human checkpoint before install |
</threat_model>

<verification>
- Integration tests pass for serve/status
- curl http://127.0.0.1:9876/api/snapshot returns valid JSON after index
</verification>

<success_criteria>
- PLUG-03 complete: all three CLI commands work
- Snapshot API ready for dashboard consumption
</success_criteria>

<output>
Create `.planning/phases/03-cli-plugin-scaffold/03-02-SUMMARY.md` when done
</output>
