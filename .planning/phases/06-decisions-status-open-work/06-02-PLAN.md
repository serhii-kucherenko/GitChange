---
phase: 06-decisions-status-open-work
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/plugin/agents/decision-miner.md
  - packages/plugin/scripts/build-decision-context.ts
  - packages/plugin/scripts/write-decisions.ts
  - packages/plugin/scripts/generate-schemas.ts
  - packages/plugin/schemas/decision-mining-context.schema.json
  - packages/plugin/schemas/decisions.schema.json
  - packages/core/src/decisions/attribution.ts
  - packages/core/src/decisions/attribution.test.ts
  - packages/core/src/decisions/merge-agent-output.ts
  - packages/core/src/decisions/merge-agent-output.test.ts
  - packages/plugin/skills/gitchange/SKILL.md
  - packages/plugin/agents/gitchange-orchestrator.md
autonomous: true
requirements: [DEC-01, DEC-02, CONT-02]
user_setup: []

must_haves:
  truths:
    - "decision-miner agent spec constrains output to candidate IDs from context bundle"
    - "Merged decisions include status, evidence, supersession, and attribution per CONT-02"
    - "/gitchange skill documents Phase 3 decision synthesis after era pipeline"
    - "decisions.schema.json generated from Zod for host validation"
  artifacts:
    - path: "packages/plugin/agents/decision-miner.md"
      provides: "Host LLM agent spec for DEC-01 LLM phase"
    - path: "packages/core/src/decisions/merge-agent-output.ts"
      provides: "Validates agent JSON against candidates + writes artifact"
      exports: ["mergeDecisionMinerOutput"]
    - path: "packages/core/src/decisions/attribution.ts"
      provides: "CONT-02 pivot attribution from evidence commits + expertise"
      exports: ["resolveDecisionAttribution"]
    - path: "packages/plugin/schemas/decisions.schema.json"
      provides: "Host-AI JSON Schema for DecisionsArtifact"
  key_links:
    - from: "packages/plugin/agents/decision-miner.md"
      to: "buildDecisionMiningContext"
      via: "build-decision-context.ts script"
      pattern: "build-decision-context"
    - from: "merge-agent-output.ts"
      to: "writeDecisionsArtifact"
      via: "Zod gate"
      pattern: "writeDecisionsArtifact"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Host-LLM decision-miner agent, helper scripts, agent output merge with attribution (CONT-02), and /gitchange orchestration Phase 3 for decisions.json synthesis.

Purpose: Completes DEC-01 LLM phase and DEC-02 data population.
Output: Agent spec, scripts, merge helper, JSON schemas, skill updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@packages/plugin/agents/era-synthesizer.md
@packages/plugin/skills/gitchange/SKILL.md
@packages/core/src/decisions/context.ts
@packages/core/src/decisions/decisions-io.ts
@packages/core/src/intelligence/expertise.ts

<interfaces>
From plan 06-01 (expected exports):
```typescript
export function buildDecisionMiningContext(gitchangeDir: string): DecisionMiningContext;
export function writeDecisionsArtifact(gitchangeDir: string, artifact: DecisionsArtifact): void;
export const DecisionRecord = z.object({ /* id, status, evidence, supersededBy, attribution?, reviewStatus */ });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: decision-miner agent spec + helper scripts</name>
  <files>packages/plugin/agents/decision-miner.md, packages/plugin/scripts/build-decision-context.ts, packages/plugin/scripts/write-decisions.ts, packages/plugin/schemas/decision-mining-context.schema.json, packages/plugin/schemas/decisions.schema.json, packages/plugin/scripts/generate-schemas.ts</files>
  <action>
    Create `decision-miner.md` mirroring `era-synthesizer.md` structure: input is only `DecisionMiningContext` from `build-decision-context.ts`; output is single `DecisionsArtifact` JSON. Agent MUST only emit decisions referencing `candidateId` values present in context; MUST NOT invent SHAs/paths outside context; each decision needs evidence min 1, status, confidence 0-1, optional supersededBy/supersedes linking other decision ids. Cap output at 40 decisions. Add scripts `build-decision-context.ts` and `write-decisions.ts` (same CLI pattern as era scripts). Extend `generate-schemas.ts` to emit `decisions.schema.json` and `decision-mining-context.schema.json` from Zod. Regenerate schemas via plugin build script.
  </action>
  <verify>
    <automated>pnpm --filter @gitchange/plugin build && test -f packages/plugin/schemas/decisions.schema.json</automated>
  </verify>
  <done>Agent spec and scripts exist; JSON schemas generated from Zod</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attribution resolver + agent output merge</name>
  <files>packages/core/src/decisions/attribution.ts, packages/core/src/decisions/attribution.test.ts, packages/core/src/decisions/merge-agent-output.ts, packages/core/src/decisions/merge-agent-output.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - resolveDecisionAttribution picks primary author from evidence commit SHAs joined to authors table; boosts score when expertise topic prefix matches relatedPaths
    - mergeDecisionMinerOutput rejects decisions whose evidence SHAs not in index
    - mergeDecisionMinerOutput rejects unknown candidateId references
    - Supersession graph must not contain cycles
  </behavior>
  <action>
    Implement `resolveDecisionAttribution(db, decision, intelligence)` per P6-D-09: query authors for evidence commit SHAs, cross-reference intelligence.expertise.topics against relatedPaths path prefixes, return attribution block with rationale and evidence min 1. Cap attribution confidence at inferred_medium without interview evidence. Implement `mergeDecisionMinerOutput(gitchangeDir, agentJson)` that validates against DecisionsArtifact, verifies all evidence refs via existing checkEvidenceIntegrity patterns, attaches attribution, sets reviewStatus pending for agent-mined rows, calls writeDecisionsArtifact. Export from core index.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/decisions/attribution packages/core/src/decisions/merge-agent-output</automated>
  </verify>
  <done>Agent output merge validates refs; attribution populated on fixture decisions</done>
</task>

<task type="auto">
  <name>Task 3: Extend /gitchange skill Phase 3 — decisions</name>
  <files>packages/plugin/skills/gitchange/SKILL.md, packages/plugin/agents/gitchange-orchestrator.md</files>
  <action>
    Add **Phase 3 — Decision synthesis** section after existing Phase 2 era synthesis in SKILL.md: prerequisites (eras.json + intelligence.json), staleness check on decisions headSha vs manifest, steps to run build-decision-context → spawn decision-miner → write-decisions via merge helper. Document supersession and reviewStatus fields for maintainer visibility. Update gitchange-orchestrator.md artifact table with decisions.json and decisions.schema.json. Per PLUG-05: no LLM SDK imports in scripts — host chat is the model.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/plugin-schemas</automated>
  </verify>
  <done>Skill documents full decision synthesis flow; PLUG-05 grep gate still passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host LLM → merge-agent-output | Untrusted JSON; must not bypass Zod or referential checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Agent-invented SHAs | mitigate | merge-agent-output rejects unindexed refs |
| T-06-05 | Elevation | Agent adds decisions without candidates | mitigate | candidateId required; reject unknown ids |
| T-06-06 | Repudiation | Unconfirmed decisions shown as fact | mitigate | reviewStatus pending default; UI shows in 06-04 |
</threat_model>

<verification>
- decision-miner.md references only bounded context script
- merge tests pass on fixture agent output
- plugin-schemas integration test passes
</verification>

<success_criteria>
- decisions.json can be produced end-to-end via documented /gitchange Phase 3
- CONT-02 attribution block present on merged decisions
- DEC-02 schema supports browse fields: status, evidence, supersession
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-02-SUMMARY.md` when done
</output>
