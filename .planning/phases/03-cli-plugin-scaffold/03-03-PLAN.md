---
phase: 03-cli-plugin-scaffold
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/vite.config.ts
  - packages/dashboard/index.html
  - packages/dashboard/src/main.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/components/IndexStatusCard.tsx
  - packages/dashboard/src/components/RepoSnapshot.tsx
  - packages/dashboard/src/index.css
  - packages/server/src/app.ts
  - packages/server/src/static.ts
  - packages/cli/src/commands/serve.ts
  - tests/integration/dashboard-snapshot.test.ts
autonomous: false
requirements: [INST-03, PLUG-02]
user_setup: []

must_haves:
  truths:
    - "User opens http://127.0.0.1:9876 and sees index status without manual configuration"
    - "Dashboard shows manifest freshness, schema version, warnings, commit count, and intelligence highlights"
    - "Dashboard fetches /api/snapshot only — no @gitchange/core imports in client bundle"
  artifacts:
    - path: "packages/dashboard/src/App.tsx"
      provides: "Minimal first-run dashboard shell"
      min_lines: 40
    - path: "packages/server/src/static.ts"
      provides: "Hono serveStatic for dashboard dist"
    - path: "packages/dashboard/vite.config.ts"
      provides: "Vite build with API proxy in dev"
  key_links:
    - from: "packages/dashboard/src/App.tsx"
      to: "/api/snapshot"
      via: "fetch on mount"
      pattern: "fetch.*api/snapshot"
    - from: "packages/server/src/app.ts"
      to: "packages/dashboard/dist"
      via: "serveStatic fallback"
      pattern: "serveStatic"
---

<objective>
Build minimal React dashboard SPA served by Hono; shows index status and basic repo snapshot for first-run value (INST-03).

Purpose: Delivers the "see initial value" success criterion before Phase 5 full drill-down. Proves read-only artifact consumption path.
Output: Built dashboard in `packages/dashboard/dist`, served at `/` by `gitchange serve`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-plugin-scaffold/03-RESEARCH.md
@.planning/phases/03-cli-plugin-scaffold/03-02-SUMMARY.md

<interfaces>
GET /api/snapshot response shape from Plan 03-02:
```typescript
interface SnapshotResponse {
  manifest: Manifest | null;
  stats: { commitCount: number; fileChangeCount: number; authorCount: number };
  intelligence: IntelligenceArtifact | null;
  highlights: { topChurnFiles: { path: string; changeCount: number }[]; topExpertiseTopics: { topic: string; label: string }[] };
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify React/Vite/Tailwind packages</name>
  <files>packages/dashboard/package.json</files>
  <action>
    Human verifies react@19.2.7, vite@8.1.2, tailwindcss@4.3.2 on npm before Task 2 runs pnpm add. Executor pauses until resume signal received.
  </action>
  <what-built>react@19.2.7, vite@8.1.2, tailwindcss@4.3.2 per STACK.md</what-built>
  <how-to-verify>
    1. Confirm react, vite, tailwindcss on npmjs.com under expected publishers
    2. Reply "approved"
  </how-to-verify>
  <verify>
    <automated>test -f packages/dashboard/package.json</automated>
  </verify>
  <done>Human replied "approved"; executor may install dashboard packages in Task 2</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard build + static serve smoke test</name>
  <files>tests/integration/dashboard-snapshot.test.ts, packages/dashboard/vite.config.ts, packages/dashboard/package.json</files>
  <behavior>
    - After indexing fixture repo, start gitchange serve
    - GET / returns HTML containing root mount point
    - GET /api/snapshot returns 200 (API still works alongside static)
    - Built dashboard dist exists after `pnpm build --filter=@gitchange/dashboard`
  </behavior>
  <action>
  Scaffold Vite React TS app in dashboard package. Configure vite proxy `/api` → `http://127.0.0.1:9876` for local dev only. Write failing integration test checking serve returns HTML + API. Test fails until static middleware wired.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/dashboard-snapshot.test.ts 2>&1 | grep -qE "FAIL|AssertionError" || test ! -f packages/dashboard/src/App.tsx</automated>
  </verify>
  <done>Smoke test exists and fails until dashboard built and served</done>
</task>

<task type="auto">
  <name>Task 2: Minimal dashboard UI + Hono static hosting</name>
  <files>packages/dashboard/src/main.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/components/IndexStatusCard.tsx, packages/dashboard/src/components/RepoSnapshot.tsx, packages/dashboard/src/index.css, packages/dashboard/index.html, packages/server/src/static.ts, packages/server/src/app.ts, packages/cli/src/commands/serve.ts</files>
  <action>
    Build minimal dashboard per P3-D-05 — no routing library needed (single page):
    - Header: "GitChange" + repo head sha from manifest
    - IndexStatusCard: schemaVersion, indexedAt, lastIndexedCommit, indexCompleteness, warning badges
    - RepoSnapshot: commit/file/author counts; top churn files list; top expertise topics ("Ask … about …")
    - Empty state when manifest null: "Run /gitchange or gitchange index to analyze this repo"
    - Loading and error states for fetch failures

    Use Tailwind v4 CSS-first setup. No default export in components.

    Add `wireStatic(app, dashboardDistPath)` in server: serve `packages/dashboard/dist` at `/` with SPA fallback to index.html; API routes take precedence over static.

    Update `gitchange serve` to resolve dashboard dist path relative to installed package (use import.meta.url / fileURLToPath — not hardcoded monorepo relative path from cwd). Document dev mode: build dashboard first or use `pnpm dev` in dashboard with proxy.

    Update turbo build pipeline: server build depends on dashboard build.
  </action>
  <verify>
    <automated>pnpm build --filter=@gitchange/dashboard --filter=@gitchange/server && pnpm vitest run tests/integration/dashboard-snapshot.test.ts</automated>
  </verify>
  <done>Dashboard renders snapshot data; serve hosts SPA + API on localhost</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/snapshot | Same-origin from localhost SPA; no secrets in API response |
| Dashboard bundle | Must not include Node native modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Snapshot in browser | accept | Local-only; data already on disk in .gitchange/ |
| T-03-06 | Tampering | XSS in dashboard | mitigate | React default escaping; no dangerouslySetInnerHTML; snapshot data rendered as text |
| T-03-SC | Tampering | react/vite npm install | mitigate | Human checkpoint before install |
</threat_model>

<verification>
- Dashboard build succeeds
- Manual: index repo → serve → browser shows status cards
- Grep gate: dashboard src must not import @gitchange/core or better-sqlite3
</verification>

<success_criteria>
- INST-03: initial value visible without manual config
- PLUG-02 partial: localhost UI exists at serve URL
</success_criteria>

<output>
Create `.planning/phases/03-cli-plugin-scaffold/03-03-SUMMARY.md` when done
</output>
