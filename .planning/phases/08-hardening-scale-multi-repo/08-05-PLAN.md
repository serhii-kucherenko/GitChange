---
phase: 08-hardening-scale-multi-repo
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - tests/golden/multi-repo-integrity.test.ts
  - tests/golden/multi-repo-fixture.ts
  - tests/e2e/dashboard-phase8.test.ts
  - packages/core/src/verify/workspace-integrity.ts
  - packages/core/src/verify/workspace-integrity.test.ts
  - packages/cli/src/commands/validate.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [SCALE-01, MULTI-01, MULTI-02, DASH-02]
user_setup: []

must_haves:
  truths:
    - "Golden test asserts two-repo workspace unified commits have repoId on every row"
    - "Golden test rejects unified tour merge without repoId when multi-repo"
    - "gitchange validate checks workspace.json integrity when present"
    - "E2E test opens dashboard graph tab and timeline tab in one session"
    - "Scale benchmark from 08-01 still passes in CI"
  artifacts:
    - path: "tests/golden/multi-repo-integrity.test.ts"
      provides: "MULTI-01/02 integrity gate"
    - path: "tests/e2e/dashboard-phase8.test.ts"
      provides: "DASH-02 E2E smoke"
    - path: "packages/core/src/verify/workspace-integrity.ts"
      provides: "checkWorkspaceIntegrity for validate CLI"
  key_links:
    - from: "packages/cli/src/commands/validate.ts"
      to: "packages/core/src/verify/workspace-integrity.ts"
      via: "validate workspace when workspace.json exists"
      pattern: "checkWorkspaceIntegrity"
    - from: "tests/golden/multi-repo-integrity.test.ts"
      to: "packages/core/src/read/unified/commits.ts"
      via: "fixture workspace + unified list"
      pattern: "listCommitsUnified"
---

<objective>
Phase 8 integration gate: golden multi-repo integrity tests, workspace validate CLI extension, dashboard E2E for graph+timeline+tours tabs, and regression run of scale benchmark — proves all four requirements before milestone close.

Purpose: Nyquist validation for SCALE-01, MULTI-01, MULTI-02, DASH-02.
Output: golden fixtures, validate hook, E2E smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@tests/golden/semantic-integrity.test.ts
@packages/cli/src/commands/validate.ts
@packages/core/src/verify/decisions-integrity.ts
@.planning/phases/03-cli-plugin-scaffold/03-06-SUMMARY.md

<interfaces>
From validate CLI pattern (Phase 4/6):
```typescript
// Extend to call checkWorkspaceIntegrity when workspace.json present
// Exit 1 on integrity failures (consistent with eras/decisions gates)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-repo golden fixtures + workspace integrity</name>
  <files>tests/golden/multi-repo-fixture.ts, tests/golden/multi-repo-integrity.test.ts, packages/core/src/verify/workspace-integrity.ts, packages/core/src/verify/workspace-integrity.test.ts, packages/cli/src/commands/validate.ts, packages/core/src/index.ts</files>
  <action>
    Create `multi-repo-fixture.ts` building two synthetic repos (backend + frontend labels) with distinct commit timestamps, each indexed into separate .gitchange/, plus workspace.json with manual link entry. Golden tests: (1) listCommitsUnified returns both repoIds, sorted correctly; (2) no commit row missing repoId; (3) mergeToursForWorkspace prefixes IDs and sets repoId on stops; (4) proximity-only dates do not create cross-repo link (negative test). Implement checkWorkspaceIntegrity: all repoPaths exist, gitchangeDirs have manifest, repoIds unique, links reference valid repoIds. Wire into `gitchange validate` — exit 1 on failure. Export checkWorkspaceIntegrity from core.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/golden/multi-repo-integrity.test.ts packages/core/src/verify/workspace-integrity.test.ts</automated>
  </verify>
  <done>Golden multi-repo tests pass; validate fails on broken workspace fixture</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Phase 8 E2E + scale regression</name>
  <files>tests/e2e/dashboard-phase8.test.ts</files>
  <action>
    E2E test (follow Phase 3/5/6 pattern with GITCHANGE_PORT): index fixture repo with semantic + tours artifacts, start server, fetch /api/workspace (or empty for single-repo), /api/graph, /api/commits, /api/tours; assert 200 and schema shapes. Dashboard smoke via fetch or playwright-lite HTTP: verify graph tab route/data available. Re-run `tests/scale/index-benchmark.test.ts` as regression gate for SCALE-01. Document in test header: Phase 8 milestone gate — timeline + graph + tours endpoints all reachable in one serve session per DASH-02.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/e2e/dashboard-phase8.test.ts tests/scale/index-benchmark.test.ts</automated>
  </verify>
  <done>E2E passes; scale benchmark passes; Phase 8 requirements covered by automated gates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden tests | Regression gate for federation correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | workspace golden | mitigate | fixture uses manual links only; asserts no auto cross-repo edges |
</threat_model>

<verification>
- Full `pnpm test` passes
- `gitchange validate` on multi-repo fixture exits 0
- Scale + golden + E2E green
</verification>

<success_criteria>
- All Phase 8 ROADMAP success criteria have automated or integration coverage
- v1 milestone Phase 8 ready for verification
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-05-SUMMARY.md` when done
</output>
