---
phase: 08-hardening-scale-multi-repo
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/store/drill.ts
  - packages/dashboard/src/store/graph.ts
  - packages/dashboard/src/components/TemporalGraphView.tsx
  - packages/dashboard/src/components/TemporalGraphNode.tsx
  - packages/dashboard/src/components/RepoBadge.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/App.tsx
  - packages/server/src/routes/graph.ts
  - packages/server/src/app.ts
autonomous: false
requirements: [DASH-02]
user_setup: []

must_haves:
  truths:
    - "User opens Graph tab and sees era and inflection nodes from temporal-graph.json"
    - "Clicking era node drills timeline via useDrillStore"
    - "Clicking commit node in expanded era opens commit detail panel"
    - "Multi-repo workspace shows repo badge on graph nodes when repoId present"
    - "Graph reads pre-built artifact only — no es-git in server graph route"
  artifacts:
    - path: "packages/dashboard/src/components/TemporalGraphView.tsx"
      provides: "@xyflow/react canvas for temporal graph"
    - path: "packages/server/src/routes/graph.ts"
      provides: "GET /api/graph?repoId="
    - path: "packages/dashboard/src/layout/DashboardLayout.tsx"
      provides: "graph intelligence tab alongside timeline/tours"
  key_links:
    - from: "packages/dashboard/src/components/TemporalGraphView.tsx"
      to: "/api/graph"
      via: "react-query fetch"
      pattern: "fetchGraph|/api/graph"
    - from: "packages/dashboard/src/components/TemporalGraphView.tsx"
      to: "packages/dashboard/src/store/drill.ts"
      via: "onNodeClick → setSelectedEraId / setSelectedCommitSha"
      pattern: "useDrillStore"
---

<objective>
DASH-02 temporal knowledge graph UI: fifth dashboard tab rendering pre-built temporal-graph.json with @xyflow/react, era-first layout with lazy commit expansion, drill-down integration, and multi-repo node attribution.

Purpose: Completes dashboard surface trio (timeline + graph + tours) per ROADMAP success criterion 4.
Output: TemporalGraphView, graph API route, graph tab in layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@packages/core/src/schema/zod/temporal-graph.ts
@packages/core/src/semantic/graph-io.ts
@packages/core/src/semantic/assemble-graph.ts
@packages/dashboard/src/layout/DashboardLayout.tsx
@packages/dashboard/src/store/drill.ts
@packages/dashboard/src/App.tsx

<interfaces>
From `packages/core/src/schema/zod/temporal-graph.ts`:
```typescript
export const TemporalGraphNodeType = z.enum([
  "era", "commit", "file", "contributor", "inflection",
]);
export const TemporalGraphArtifact = z.object({
  schemaVersion: z.string(),
  nodes: z.array(TemporalGraphNode),
  edges: z.array(TemporalGraphEdge),
});
```

From `packages/core/src/semantic/assemble-graph.ts`:
```typescript
const MAX_GRAPH_NODES = 500;
```

From `packages/dashboard/src/layout/DashboardLayout.tsx`:
```typescript
export type IntelligenceTab = "timeline" | "decisions" | "open-work";
// Extend to include "tours" (Phase 7) and "graph"
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify @xyflow/react package legitimacy</name>
  <files>packages/dashboard/package.json</files>
  <action>Before `pnpm add @xyflow/react@12.11.1 -F @gitchange/dashboard`, human verifies https://www.npmjs.com/package/@xyflow/react — xyflow org, React Flow official, matches STACK.md 12.11.1.</action>
  <verify>
    <automated>grep -q @xyflow/react packages/dashboard/package.json || echo "awaiting install"</automated>
  </verify>
  <done>Human approved @xyflow/react install</done>
</task>

<task type="auto">
  <name>Task 1: Graph read API + xyflow graph view</name>
  <files>packages/dashboard/package.json, packages/server/src/routes/graph.ts, packages/server/src/app.ts, packages/dashboard/src/store/graph.ts, packages/dashboard/src/components/TemporalGraphView.tsx, packages/dashboard/src/components/TemporalGraphNode.tsx</files>
  <action>
    Install @xyflow/react per P8-D-12. Server GET /api/graph: resolve workspace context; when repoId query param set return that repo's temporal-graph.json via existing readTemporalGraph; when omitted and multi-repo return array `{repoId, graph}` per repo (max 5 repos). Validate response with TemporalGraphArtifact Zod on server. Client TemporalGraphView: initial render shows only era + inflection nodes (P8-D-13); double-click era expands child commit nodes from artifact edges `era_contains_commit`. Custom node component shows label + RepoBadge when repoId in data. Use ReactFlow with pan/zoom, fitView on load. No live git imports in server route (grep gate). useGraphStore holds expandedEraIds set.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <done>Graph API returns valid artifact; TemporalGraphView renders era nodes from BASIC_SCENARIO fixture</done>
</task>

<task type="auto">
  <name>Task 2: Graph tab + drill-down wiring</name>
  <files>packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/store/drill.ts, packages/dashboard/src/components/RepoBadge.tsx</files>
  <action>
    Extend IntelligenceTab with `graph` (and `tours` if Phase 7 added it — do not remove existing tabs). Add Graph tab label in TAB_LABELS. App.tsx: when graph tab active render TemporalGraphView in main column; pass workspace repo filter from useWorkspaceStore (08-03). Wire onNodeClick per P8-D-14: era → setSelectedEraId + switch to timeline tab; commit → setSelectedCommitSha(repoId-aware drill routing from 08-03); inflection → setSelectedEraId from edge parent. Multi-repo: pass repoId into drill store selectors so commit detail fetches correct gitchangeDir. DASH-02 success: user can access timeline, graph, and tour player tabs in one dashboard session.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck &amp;&amp; pnpm --dir packages/dashboard build</automated>
  </verify>
  <done>Graph tab navigable; node click drills to timeline/commit panels; build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/graph | Serves pre-validated JSON only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | DoS | graph render | mitigate | era-first render; MAX_GRAPH_NODES 500 at artifact write |
| T-08-SC | Tampering | @xyflow/react install | mitigate | blocking human checkpoint before add |
</threat_model>

<verification>
- Dashboard build passes
- Graph tab shows nodes when temporal-graph.json exists
- Server graph route has no es-git import
</verification>

<success_criteria>
- DASH-02: temporal knowledge graph view alongside existing timeline and tour player
- All graph data from pre-built index (SCALE-02)
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-04-SUMMARY.md` when done
</output>
