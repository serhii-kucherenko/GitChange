---
phase: 07-guided-tours-onboarding-ux
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/plugin/agents/tour-builder.md
  - packages/plugin/schemas/tour-synthesis-context.schema.json
  - packages/plugin/schemas/tours.schema.json
  - packages/plugin/scripts/build-tour-context.ts
  - packages/plugin/scripts/write-tours.ts
  - packages/core/src/tours/merge-agent-output.ts
  - packages/core/src/tours/merge-agent-output.test.ts
  - packages/core/src/tours/pipeline.ts
  - packages/core/src/tours/pipeline.test.ts
  - packages/core/src/tours/bind-basic-scenario-tours.ts
  - packages/core/src/schema/manifest.ts
  - packages/plugin/agents/gitchange-orchestrator.md
  - packages/plugin/skills/gitchange/SKILL.md
autonomous: true
requirements: [TOUR-01, TOUR-02, TOUR-03]
user_setup: []

must_haves:
  truths:
    - "tour-builder agent spec constrains output to context era/decision/thread IDs only"
    - "mergeTourBuilderOutput binds agent JSON to deterministic outline and validates caps"
    - "runToursPipeline sets manifest toursComputedAt after tours.json exists"
    - "/gitchange skill documents Phase 7 tour synthesis step after decisions"
  artifacts:
    - path: "packages/plugin/agents/tour-builder.md"
      provides: "Host-AI tour synthesis spec"
    - path: "packages/plugin/schemas/tours.schema.json"
      provides: "JSON Schema for agent output validation"
    - path: "packages/core/src/tours/merge-agent-output.ts"
      provides: "Merge gate for agent tours artifact"
      exports: ["mergeTourBuilderOutput"]
    - path: "packages/core/src/tours/pipeline.ts"
      provides: "runToursPipeline manifest checkpoint"
      exports: ["runToursPipeline"]
  key_links:
    - from: "packages/plugin/scripts/build-tour-context.ts"
      to: "packages/core/src/tours/context.ts"
      via: "buildTourSynthesisContext stdout"
      pattern: "buildTourSynthesisContext"
    - from: "packages/plugin/scripts/write-tours.ts"
      to: "packages/core/src/tours/merge-agent-output.ts"
      via: "merge + writeToursArtifact"
      pattern: "mergeTourBuilderOutput"
---

<objective>
Host-LLM tour synthesis layer: tour-builder agent spec, JSON schemas, merge gate binding agent output to deterministic outline, CLI scripts, pipeline step, and orchestrator/skill documentation for TOUR-01 default tour plus TOUR-02 role variants and TOUR-03 topic-thread tours in one ToursArtifact.

Purpose: Produces `.gitchange/tours.json` with all tour kinds per P7-D-03, P7-D-04, P7-D-08 phase 2.
Output: Agent spec, schemas, merge helper, scripts, pipeline, skill updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guided-tours-onboarding-ux/07-RESEARCH.md
@packages/plugin/agents/decision-miner.md
@packages/plugin/scripts/write-decisions.ts
@packages/core/src/decisions/merge-agent-output.ts
@packages/core/src/semantic/pipeline.ts

<interfaces>
From 07-01 (will exist after Wave 1):
```typescript
export function buildTourSynthesisContext(gitchangeDir: string): TourSynthesisContext;
export function outlineDefaultTourChapters(gitchangeDir: string): OutlineChapter[];
export function mergeTourBuilderOutput(gitchangeDir: string, agentOutput: unknown): ToursArtifact;
export function writeToursArtifact(gitchangeDir: string, artifact: ToursArtifact): void;
export function runToursPipeline(gitchangeDir: string): { ok: true };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tour-builder agent + JSON schemas + merge gate</name>
  <files>packages/plugin/agents/tour-builder.md, packages/plugin/schemas/tour-synthesis-context.schema.json, packages/plugin/schemas/tours.schema.json, packages/core/src/tours/merge-agent-output.ts, packages/core/src/tours/merge-agent-output.test.ts, packages/core/src/tours/bind-basic-scenario-tours.ts</files>
  <behavior>
    - Agent outputs ToursArtifact with: 1 default tour (4-6 chapters), up to 3 role tours, up to 5 topic tours
    - Every stop references eraIds/commitSha/filePath/decisionId from input context only
    - mergeTourBuilderOutput: preserve outline chapter order for default tour; assign ULID ids where missing; reject unknown era/decision/thread refs; call writeToursArtifact
    - bindBasicScenarioToursTemplate: fixture tours bound to indexed SHAs like bindBasicScenarioErasTemplate
  </behavior>
  <action>
    Write tour-builder.md mirroring decision-miner structure per P7-D-08, P7-D-09. Generate JSON schemas from Zod or hand-write matching 07-01 shapes. Implement merge-agent-output.ts: validate agent JSON, merge narratives onto outline skeleton for default tour, verify roleTag/topicKey presence per kind. Add bind-basic-scenario-tours.ts for test fixtures. Tests: merge accepts valid agent output; rejects invented era id; role tour requires roleTag.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/tours/merge-agent-output.test.ts</automated>
  </verify>
  <done>mergeTourBuilderOutput produces valid ToursArtifact from fixture agent JSON + outline</done>
</task>

<task type="auto">
  <name>Task 2: Scripts, pipeline, orchestrator + skill Phase 7</name>
  <files>packages/plugin/scripts/build-tour-context.ts, packages/plugin/scripts/write-tours.ts, packages/core/src/tours/pipeline.ts, packages/core/src/tours/pipeline.test.ts, packages/core/src/schema/manifest.ts, packages/plugin/agents/gitchange-orchestrator.md, packages/plugin/skills/gitchange/SKILL.md</files>
  <action>
    Add build-tour-context.ts (stdout JSON from buildTourSynthesisContext) and write-tours.ts (read agent JSON path, call mergeTourBuilderOutput). Implement runToursPipeline: require eras.json, decisions.json, open-work.json; verify tours.json integrity; set manifest fields toursComputedAt, toursHeadSha, toursSchemaVersion (add to manifest schema if missing). Extend gitchange-orchestrator.md with Phase 6 — Tour synthesis section. Extend gitchange SKILL.md with tour step after decision synthesis: prerequisites, build context, tour-builder delegation, write-tours persist, present tour list to user. Pipeline test uses bindBasicScenarioToursTemplate on fixture dir.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/core/src/tours/pipeline.test.ts</automated>
  </verify>
  <done>runToursPipeline updates manifest; skill documents full tour synthesis flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host AI → merge gate | Untrusted agent JSON crosses into persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | mergeTourBuilderOutput | mitigate | Reject refs not in context; Zod + integrity before write |
| T-07-05 | Spoofing | tour-builder agent | mitigate | Agent spec forbids inventing SHAs; merge validates |
| T-07-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
pnpm exec vitest run packages/core/src/tours/merge-agent-output.test.ts packages/core/src/tours/pipeline.test.ts
</verification>

<success_criteria>
- tour-builder.md and tours.schema.json committed
- merge + write scripts runnable via tsx
- runToursPipeline sets manifest checkpoint
- Skill documents tour synthesis after decisions
</success_criteria>

<output>
Create `.planning/phases/07-guided-tours-onboarding-ux/07-02-SUMMARY.md` when done
</output>
