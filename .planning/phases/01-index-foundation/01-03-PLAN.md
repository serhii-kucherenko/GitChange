---
phase: 01-index-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - packages/core/src/ingestion/git-walk.ts
  - packages/core/src/ingestion/commit-parse.ts
  - packages/core/src/ingestion/diff.ts
  - packages/core/src/ingestion/index.ts
  - packages/core/src/ingestion/commit-parse.test.ts
  - packages/core/src/ingestion/diff.test.ts
autonomous: true
requirements: [INGX-02]
user_setup: []

must_haves:
  truths:
    - "A commit's author, committer, timestamps, summary, and full message are read from local git via es-git"
    - "Merge commits are identified by parent count > 1"
    - "File-level diffs report added/modified/deleted/renamed changes with rename detection enabled"
    - "Conventional-commit type/scope/breaking are parsed deterministically when present"
    - "The revwalk streams SHAs (newest→oldest) without materializing the full history in memory"
  artifacts:
    - path: "packages/core/src/ingestion/git-walk.ts"
      provides: "es-git revwalk streaming (pushHead + pushRange)"
      contains: "revwalk"
    - path: "packages/core/src/ingestion/commit-parse.ts"
      provides: "Pure CommitRecord builder from an es-git Commit"
      min_lines: 30
    - path: "packages/core/src/ingestion/diff.ts"
      provides: "diffTreeToTree + findSimilar rename detection → FileChange[]"
      contains: "findSimilar"
  key_links:
    - from: "packages/core/src/ingestion/commit-parse.ts"
      to: "packages/core/src/schema/zod/commit.ts"
      via: "returns CommitRecord shape"
      pattern: "CommitRecord"
    - from: "packages/core/src/ingestion/diff.ts"
      to: "packages/core/src/schema/zod/file-change.ts"
      via: "returns FileChangeRecord (pre-evidence) shape"
      pattern: "changeType"
---

<objective>
Implement deterministic, network-free git ingestion primitives over `es-git`: stream the revwalk, parse each commit into a typed `CommitRecord` (pure function), and extract file-level changes with rename/merge handling. These are the pure, TDD-covered building blocks the orchestrator (Plan 05) composes; they do not touch SQLite or the manifest.

Purpose: INGX-02 — parse commits, authors, timestamps, messages, merges, renames, and file-level diffs from local git. Keeping parse pure enables later piscina workers without refactor (RESEARCH A3).
Output: `ingestion/git-walk.ts`, `ingestion/commit-parse.ts`, `ingestion/diff.ts`, barrel export, and golden tests over the synthetic fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-index-foundation/SKELETON.md
@.planning/phases/01-index-foundation/01-RESEARCH.md
@.planning/phases/01-index-foundation/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Consumes schema types from Plan 02 (schema/zod/*). Produces primitives for Plan 05 orchestrator. -->
```typescript
// ingestion/git-walk.ts
import { openRepository } from "es-git";
export function openRepo(repoPath: string): Promise<Repository>;
// Generator streaming newest→oldest (Pitfall 1: never materialize all SHAs)
export function walkFromHead(repo: Repository): Iterable<string>;
export function walkRange(repo: Repository, cursorSha: string): Iterable<string>; // '<cursor>..HEAD'

// ingestion/commit-parse.ts  (PURE — es-git Commit in, CommitRecord out; no I/O)
export function parseCommit(repo: Repository, sha: string): CommitRecord; // CommitRecord from schema/zod/commit

// ingestion/diff.ts
export interface RawFileChange {
  commitSha: string; path: string; oldPath: string | null;
  changeType: "added"|"modified"|"deleted"|"renamed"|"copied"|"typechange";
  isBinary: boolean;
}
export function diffCommit(repo: Repository, sha: string): RawFileChange[]; // root commit → all 'added'
```
</interfaces>

<!-- es-git API note (RESEARCH A1/A2, Open Question 1): exact accessor names for parents/parentCount
     must be confirmed against es-git Commit reference at implementation time. Confirm via Context7
     /toss/es-git before finalizing merge detection. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: revwalk streaming + commit parse (pure)</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 86–128 (git-walk + commit-parse patterns)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 365–383 (§1), 426–436 (§4), 503–507 (Open Question 1 — parent accessors)
    - `.planning/phases/01-index-foundation/01-CONTEXT.md` D-01, Claude's Discretion (batch sizes)
    - Context7 `/toss/es-git` for `Commit.parents()`/`parentCount()` accessor confirmation
  </read_first>
  <files>packages/core/src/ingestion/git-walk.ts, packages/core/src/ingestion/commit-parse.ts, packages/core/src/ingestion/index.ts, packages/core/src/ingestion/commit-parse.test.ts</files>
  <behavior>
    - `parseCommit` on a known fixture commit returns matching author name/email, committer, authoredAt/committedAt (epoch ms), summary (first line), full message.
    - A merge commit fixture yields `isMerge === true` and `parentCount === 2`.
    - A root commit yields `parentCount === 0`, `isMerge === false`.
    - `walkFromHead` yields SHAs newest→oldest; iterating does not require collecting the whole list (generator/iterator).
    - A conventional-commit message `feat(core): x` parses to `{type:'feat', scope:'core', breaking:false}`; a non-conventional message yields undefined conventional fields (not a throw).
  </behavior>
  <action>
    `git-walk.ts`: `openRepo` wraps `openRepository`; `walkFromHead` returns `repo.revwalk().pushHead()` iterated as a generator (stream, don't collect — Pitfall 1); `walkRange` uses `repo.revwalk().pushRange(`${cursor}..HEAD`)` (equivalently `.pushHead().hide(cursor)`). `commit-parse.ts`: pure `parseCommit(repo, sha)` reading `getCommit(sha)` → author/committer (name,email,timestamp→epoch ms), `summary()`, `message()`, parents. Confirm the parent accessor name against es-git (`parents()`/`parentCount()`); `isMerge = parentCount > 1`; store `parents[]` as SHA strings. Pipe `message()` through `conventional-commits-parser` (do not hand-roll — RESEARCH Don't Hand-Roll) to fill optional `conventional{type,scope,breaking}`. Return the `CommitRecord` shape from `schema/zod/commit.ts` (no `evidence[]` — commit is an evidence source). Barrel-export from `ingestion/index.ts` (top-level, no `export default`). Tests build `BASIC_SCENARIO` via the fixture builder and assert against known values.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/ingestion/commit-parse</automated>
  </verify>
  <acceptance_criteria>
    - Commit metadata parsed correctly for normal, merge, and root commits over the synthetic fixture.
    - Conventional-commit fields extracted via the library; non-conventional messages do not throw.
    - Revwalk is a stream/iterator, not a pre-collected array (grep confirms generator/`yield` or lazy iteration).
    - `parseCommit` performs no disk writes / network (pure over es-git objects).
  </acceptance_criteria>
  <done>Commits/authors/timestamps/messages/merges are read from local git via a pure, streaming parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tree diff + rename detection → FileChange[]</name>
  <read_first>
    - `.planning/phases/01-index-foundation/01-PATTERNS.md` lines 132–150 (diff pattern)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 385–403 (§2 diffTreeToTree + findSimilar)
    - `.planning/phases/01-index-foundation/01-RESEARCH.md` lines 310–311 (merge commits recorded, not skipped)
    - `.planning/phases/01-index-foundation/01-VALIDATION.md` (T-02: paths stored as data only)
  </read_first>
  <files>packages/core/src/ingestion/diff.ts, packages/core/src/ingestion/diff.test.ts</files>
  <behavior>
    - A commit that adds a file → one `RawFileChange` with `changeType:'added'`, `oldPath:null`.
    - A commit that renames `a→b` (fixture rename) → `changeType:'renamed'`, `oldPath:'a'`, `path:'b'` (via `findSimilar({renames:true})`).
    - A commit that deletes a file → `changeType:'deleted'`.
    - The root commit (no parent) diffs against `null` tree → all files `'added'`.
    - A binary blob is flagged `isBinary:true` and its content is not required by the change record.
  </behavior>
  <action>
    `diff.ts`: `diffCommit(repo, sha)` gets `commit.tree()`, resolves first-parent tree (`null` for root), runs `repo.diffTreeToTree(parentTree, commitTree)`, then `diff.findSimilar({ renames: true })` BEFORE reading deltas (do not hand-roll rename detection — RESEARCH Don't Hand-Roll). Map each `delta.status()` to the `changeType` enum from `schema/zod/file-change.ts`; capture `oldFile().path()`/`newFile().path()` and binary flag. Merge commits: diff against first parent for a representative change set (record them — do not skip; RESEARCH Anti-Patterns). Return `RawFileChange[]` WITHOUT `evidence[]` — evidence is attached at the write boundary in Plan 05 (`evidence:[{type:'file',path,commitSha}]`). Treat all paths as inert data — never resolve or write to them (V12 path safety, VALIDATION T-02).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/ingestion/diff</automated>
  </verify>
  <acceptance_criteria>
    - Added/modified/deleted/renamed statuses correctly produced over the synthetic fixture.
    - Renames detected via `findSimilar({renames:true})` with correct `oldPath`.
    - Root commit produces all-`added`; binary blobs flagged and not required to carry content.
    - No path from commit data is ever passed to a filesystem write/resolve call.
  </acceptance_criteria>
  <done>File-level diffs with rename/merge handling produced deterministically as pure output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.git` → es-git read | Repo history (may contain adversarial paths/content) enters the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (V12 Files) | commit path handling in diff.ts | mitigate | Paths from `delta.oldFile()/newFile()` are stored as data only — never `fs`-resolved or used to write; outputs go only under `.gitchange/` (Plan 05). |
| T-03-02 | Denial of Service (V12) | oversized/binary blobs | mitigate | Flag binary via libgit2 delta; do not load large/binary blob content in the diff step (content capture is Plan 07 for docs only, size-capped there). |
| T-03-03 | Information Disclosure (V8) | raw diff content | transfer | This plan produces change metadata only; raw content redaction is enforced by Plan 04 privacy gate before any persistence. |
</threat_model>

<verification>
- `pnpm vitest run packages/core/src/ingestion` green (commit-parse + diff).
- `pnpm exec tsc -p packages/core/tsconfig.json --noEmit` passes.
- No network or filesystem writes performed by ingestion primitives (pure).
</verification>

<success_criteria>
- Commits/authors/timestamps/messages/merges/renames/file-diffs parsed from local git via es-git (INGX-02).
- Parse steps are pure and streaming (Pitfall 1 avoided; worker-ready per A3).
- Change records carry no unredacted content and no evidence yet (attached downstream).
</success_criteria>

<output>
Create `.planning/phases/01-index-foundation/01-03-SUMMARY.md` when done.
</output>
