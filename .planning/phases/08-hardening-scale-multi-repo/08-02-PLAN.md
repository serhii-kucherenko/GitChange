---
phase: 08-hardening-scale-multi-repo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schema/zod/workspace.ts
  - packages/core/src/schema/zod/workspace.test.ts
  - packages/core/src/schema/zod/index.ts
  - packages/core/src/workspace/workspace-io.ts
  - packages/core/src/workspace/workspace-io.test.ts
  - packages/core/src/workspace/index-workspace.ts
  - packages/core/src/workspace/index-workspace.test.ts
  - packages/core/src/schema/manifest.ts
  - packages/core/src/index.ts
  - packages/cli/src/commands/workspace.ts
  - packages/cli/src/bin.ts
autonomous: true
requirements: [MULTI-01]
user_setup: []

must_haves:
  truths:
    - "User runs gitchange workspace add <path> --label <name> and repo appears in workspace.json"
    - "User runs gitchange workspace list and sees all registered repos with repoId and label"
    - "gitchange workspace index indexes each repo sequentially into its own .gitchange/"
    - "Single-repo installs without workspace.json continue to work unchanged"
  artifacts:
    - path: "packages/core/src/schema/zod/workspace.ts"
      provides: "WorkspaceArtifact, RepoEntry, CrossRepoLink schemas"
      exports: ["WorkspaceArtifact", "WORKSPACE_SCHEMA_VERSION"]
    - path: "packages/core/src/workspace/workspace-io.ts"
      provides: "readWorkspace, writeWorkspace, addRepo, removeRepo"
      exports: ["readWorkspace", "writeWorkspace", "addRepo"]
    - path: "packages/cli/src/commands/workspace.ts"
      provides: "workspace add | list | remove | index subcommands"
  key_links:
    - from: "packages/core/src/workspace/index-workspace.ts"
      to: "packages/core/src/index/full.ts"
      via: "sequential indexFull per RepoEntry"
      pattern: "indexFull|indexIncremental"
    - from: "packages/core/src/workspace/workspace-io.ts"
      to: ".gitchange/workspace.json"
      via: "atomic tmp+rename write"
      pattern: "workspace.json"
---

<objective>
MULTI-01 workspace layer: Zod-validated workspace.json, CLI for manual repo selection, and sequential per-repo indexing — per-repo .gitchange/ caches remain independent per P8-D-06.

Purpose: User can register multiple related repos before unified dashboard federation (Plan 08-03).
Output: workspace schema, I/O, indexWorkspace orchestrator, CLI commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@.planning/research/PITFALLS.md
@packages/core/src/index/full.ts
@packages/core/src/index/incremental.ts
@packages/cli/src/commands/index.ts

<interfaces>
From `packages/core/src/index/types.ts`:
```typescript
export interface IndexOptions {
  repoPath: string;
  gitchangeDir?: string;
  rebuildIntelligence?: boolean;
}
export async function indexFull(options: IndexOptions): Promise<IndexResult>;
export async function indexIncremental(options: IndexOptions): Promise<IndexResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace schema + I/O</name>
  <files>packages/core/src/schema/zod/workspace.ts, packages/core/src/schema/zod/workspace.test.ts, packages/core/src/schema/zod/index.ts, packages/core/src/workspace/workspace-io.ts, packages/core/src/workspace/workspace-io.test.ts, packages/core/src/schema/manifest.ts</files>
  <action>
    Implement P8-D-05 WorkspaceArtifact: schemaVersion "1", primaryRepoId, repos[] with repoId (slug from label), label, repoPath (absolute), gitchangeDir (default join(repoPath, '.gitchange')), links[] per P8-D-10 (manual cross-repo links only). `addRepo` generates unique repoId; rejects duplicate paths. `writeWorkspace` uses atomic tmp+rename like eras-io. Add optional `repoId` on Manifest per P8-D-15. Validate repoPath exists and contains `.git`. Workspace file lives at `join(primaryGitchangedir, 'workspace.json')` where primary is first added repo's .gitchange OR cwd `.gitchange/workspace.json` when invoked from a repo root. Export from `@gitchange/core`.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/workspace/workspace-io.test.ts src/schema/zod/workspace.test.ts</automated>
  </verify>
  <done>Workspace round-trips; invalid paths rejected; manifest repoId optional field parses</done>
</task>

<task type="auto">
  <name>Task 2: indexWorkspace + CLI workspace commands</name>
  <files>packages/core/src/workspace/index-workspace.ts, packages/core/src/workspace/index-workspace.test.ts, packages/core/src/index.ts, packages/cli/src/commands/workspace.ts, packages/cli/src/bin.ts</files>
  <action>
    Implement `indexWorkspace(workspace, options)` per P8-D-07: for each repo entry call indexIncremental (falls back to indexFull when no manifest); collect per-repo results; continue on single-repo failure with aggregated error report. CLI subcommands: `gitchange workspace add <path> [--label] [--id]`, `list`, `remove <repoId>`, `index [--full] [--rebuild-intelligence]`. Register in bin.ts. When workspace has one repo, behavior matches today’s single-repo `gitchange index`. Integration test: two synthetic repos in temp dirs, add both, index workspace, assert two manifest files exist with distinct repoIds.
  </action>
  <verify>
    <automated>pnpm --dir packages/core test -- src/workspace/index-workspace.test.ts</automated>
  </verify>
  <done>CLI workspace add/list/index works; two-repo fixture indexes independently</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace.json | User-controlled paths — must validate before index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | workspace repoPath | mitigate | resolve to absolute; reject `..` traversal; require `.git` directory |
| T-08-04 | Information Disclosure | workspace list | accept | local-only CLI; paths are user's own repos |
</threat_model>

<verification>
- `pnpm --dir packages/core test` passes for workspace modules
- `gitchange workspace add` + `list` manual smoke on synthetic repo
</verification>

<success_criteria>
- MULTI-01: user manually selects multiple repos via workspace CLI
- Per-repo indexes remain separate SQLite files
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-02-SUMMARY.md` when done
</output>
