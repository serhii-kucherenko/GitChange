---
phase: 07-guided-tours-onboarding-ux
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - packages/core/src/verify/tours-snapshot.ts
  - packages/core/src/verify/tours-snapshot.test.ts
  - tests/golden/tours-evidence-integrity.test.ts
  - tests/integration/dashboard-tour-e2e.test.ts
  - packages/cli/src/commands/validate.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [TOUR-01, TOUR-02, TOUR-03, TOUR-04]
user_setup: []

must_haves:
  truths:
    - "Golden test validates every tour stop evidence SHA resolves in BASIC_SCENARIO index"
    - "Default tour has 4-6 chapters in fixture artifact"
    - "E2E test: load tours API → select default tour → drill stop commit → commit detail returns hunks"
    - "gitchange validate runs checkToursIntegrity when tours.json present"
    - "Role and topic tours present in fixture with correct kind tags"
  artifacts:
    - path: "tests/golden/tours-evidence-integrity.test.ts"
      provides: "EVD-04 tours golden gate"
    - path: "tests/integration/dashboard-tour-e2e.test.ts"
      provides: "Phase 7 acceptance E2E"
    - path: "packages/core/src/verify/tours-snapshot.ts"
      provides: "Snapshot helper for golden tests"
  key_links:
    - from: "tests/integration/dashboard-tour-e2e.test.ts"
      to: "GET /api/tours + GET /api/commits/:sha"
      via: "HTTP sequence after bindBasicScenarioToursTemplate"
      pattern: "api/tours|commits"
    - from: "packages/cli/src/commands/validate.ts"
      to: "checkToursIntegrity"
      via: "readToursArtifact gate"
      pattern: "checkToursIntegrity"
---

<objective>
Phase 7 completion gate: golden evidence integrity for tours.json, dashboard tour E2E proving drill-down from stop to commit detail, validate CLI tours check, and fixture-bound BASIC_SCENARIO tours for CI without live LLM.

Purpose: Proves all TOUR-01–TOUR-04 success criteria together; mirrors 06-06 and 05-06 pattern.
Output: Golden tests, E2E integration test, validate CLI extension, tours snapshot verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guided-tours-onboarding-ux/07-RESEARCH.md
@.planning/phases/06-decisions-status-open-work/06-06-SUMMARY.md
@tests/integration/dashboard-drilldown-e2e.test.ts
@packages/core/src/tours/bind-basic-scenario-tours.ts
@packages/cli/src/commands/validate.ts

<interfaces>
Existing E2E harness from dashboard-drilldown-e2e.test.ts:
- Spawns gitchange serve on GITCHANGE_PORT
- Uses BASIC_SCENARIO fixture with semantic artifacts bound
- Asserts API JSON shapes and drill sequence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Golden tours integrity + BASIC_SCENARIO fixture</name>
  <files>packages/core/src/verify/tours-snapshot.ts, packages/core/src/verify/tours-snapshot.test.ts, tests/golden/tours-evidence-integrity.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - verifyToursEvidenceIntegrity(gitchangeDir): all stops have evidence; all commit SHAs in index; default tour 4-6 chapters; at least 1 role tour and 1 topic tour in fixture
    - Golden test runs on BASIC_SCENARIO after bindBasicScenarioToursTemplate
    - Snapshot helper exports structured report for debugging
  </behavior>
  <action>
    Implement tours-snapshot.ts verifier wrapping checkToursIntegrity plus chapter count assertions. Golden test in tests/golden/ uses verify helpers from core (no drizzle in test). Fixture template includes: default onboarding tour (5 chapters), backend role tour, auth topic tour — bound to indexed SHAs from BASIC_SCENARIO. Export verifier from core index.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/golden/tours-evidence-integrity.test.ts packages/core/src/verify/tours-snapshot.test.ts</automated>
  </verify>
  <done>Golden passes on fixture; fails when stop evidence SHA invalid</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard tour E2E + validate CLI</name>
  <files>tests/integration/dashboard-tour-e2e.test.ts, packages/cli/src/commands/validate.ts</files>
  <action>
    E2E test mirrors dashboard-drilldown-e2e: index BASIC_SCENARIO, bind eras/decisions/open-work/tours fixtures, serve dashboard API. Sequence: GET /api/tours → assert defaultTourId → GET /api/tours/:id → take first stop drillTarget.commitSha → GET /api/commits/:sha/detail → assert hunks or files present. Assert role tour and topic tour listed with correct kind. Extend validate.ts: when tours.json exists OR manifest.toursComputedAt set, run checkToursIntegrity; exit 1 on failure (mirror decisions block). Do not require tours.json when manifest has no tours checkpoint (backward compatible).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/integration/dashboard-tour-e2e.test.ts</automated>
  </verify>
  <done>E2E tour drill path passes; gitchange validate fails on corrupt tours.json</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI fixture | Pre-bound tours must not leak secrets — reuse BASIC_SCENARIO privacy rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | Golden fixture tours | mitigate | SHA binding via bindBasicScenarioToursTemplate only |
| T-07-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
pnpm exec vitest run tests/golden/tours-evidence-integrity.test.ts tests/integration/dashboard-tour-e2e.test.ts
pnpm exec vitest run packages/cli 2>/dev/null || pnpm --filter @gitchange/cli test
</verification>

<success_criteria>
- Golden tours evidence integrity passes
- E2E: tour stop → commit detail drill works
- validate CLI includes tours integrity when artifact present
- Fixture includes default + role + topic tours
</success_criteria>

<output>
Create `.planning/phases/07-guided-tours-onboarding-ux/07-05-SUMMARY.md` when done
</output>
