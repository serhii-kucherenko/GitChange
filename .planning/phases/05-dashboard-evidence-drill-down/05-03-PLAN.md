---
phase: 05-dashboard-evidence-drill-down
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - packages/core/src/read/eras.ts
  - packages/core/src/read/eras.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/eras.ts
  - packages/server/src/app.ts
  - packages/dashboard/package.json
  - packages/dashboard/src/components/EraTimeline.tsx
  - packages/dashboard/src/components/EraDetailPanel.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/store/drill.ts
  - packages/dashboard/src/index.css
  - packages/dashboard/src/main.tsx
  - tests/integration/dashboard-eras.test.ts
autonomous: true
requirements: [TIME-01, DASH-01]
user_setup: []

must_haves:
  truths:
    - "User sees interactive timeline with era bands from eras.json"
    - "Clicking an era selects it and filters commit list to era commit window"
    - "Era detail shows name, summary, inflections, and claims with evidence count"
    - "Timeline mounts imperatively without React re-render flicker"
  artifacts:
    - path: "packages/dashboard/src/components/EraTimeline.tsx"
      provides: "vis-timeline era bands"
    - path: "packages/core/src/read/eras.ts"
      provides: "listErasForDashboard from eras.json + commit counts"
    - path: "packages/server/src/routes/eras.ts"
      provides: "GET /api/eras"
  key_links:
    - from: "packages/dashboard/src/components/EraTimeline.tsx"
      to: "drill store selectedEraId"
      via: "timeline select event"
      pattern: "setSelectedEraId"
    - from: "packages/dashboard/src/store/drill.ts"
      to: "/api/commits"
      via: "after/before from era startAt/endAt"
      pattern: "selectedEraId"
---

## Phase Goal

**As a** new hire or maintainer, **I want to** explore project history interactively with era→commit→file drill-down on localhost, **so that** I can understand how the codebase evolved with linked evidence without hunting git log.

<objective>
Interactive era timeline (TIME-01): vis-timeline bands from eras.json, era selection filters commits, era detail panel with claims/inflections.

Purpose: Connects Phase 4 semantic artifacts to primary navigation surface for drill-down.
Output: GET /api/eras, EraTimeline + EraDetailPanel, drill store era→commit window wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@.planning/phases/04-era-detection-semantic-pipeline/04-05-SUMMARY.md
@packages/core/src/semantic/eras-io.ts
@packages/core/src/schema/zod/eras.ts

<interfaces>
NamedEra fields used for timeline:
- id, name, startAt, endAt (unix seconds), startCommitSha, endCommitSha
- inflections[], claims[] with evidence[]

listErasForDashboard(gitchangeDir) → { eras: DashboardEra[] } | null
DashboardEra: NamedEra fields + commitCountInWindow from SQLite COUNT between startAt/endAt

readErasArtifact already exported from core — reuse, do not duplicate JSON parse.

Drill store: setSelectedEraId(id) sets after/before filters derived from era window; clearEra clears era filters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listErasForDashboard + GET /api/eras</name>
  <files>packages/core/src/read/eras.ts, packages/core/src/read/eras.test.ts, packages/core/src/index.ts, packages/server/src/routes/eras.ts, packages/server/src/app.ts, tests/integration/dashboard-eras.test.ts</files>
  <behavior>
    - Returns null/404 when eras.json missing
    - Returns eras with commit counts when semantic pipeline ran on fixture
    - GET /api/eras returns Zod-valid JSON array
    - Integration: index + semantic fixture path → /api/eras eraCount >= 1
  </behavior>
  <action>
    Implement read/eras.ts wrapping readErasArtifact + optional SQLite commit count per era window (committedAt between startAt/endAt). Export listErasForDashboard.

    Add routes/eras.ts GET /eras; mount in app.ts. 404 when not indexed or no eras.

    Integration test: use fixture with eras (bindBasicScenarioErasTemplate or run semantic on BASIC_SCENARIO if test helper exists from Phase 4 golden tests). Mirror dashboard-commits test harness.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/eras.test.ts tests/integration/dashboard-eras.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>/api/eras serves era list with windows from eras.json</done>
</task>

<task type="auto">
  <name>Task 2: EraTimeline vis-timeline + era-driven commit filter</name>
  <files>packages/dashboard/package.json, packages/dashboard/src/components/EraTimeline.tsx, packages/dashboard/src/components/EraDetailPanel.tsx, packages/dashboard/src/layout/DashboardLayout.tsx, packages/dashboard/src/store/drill.ts, packages/dashboard/src/index.css, packages/dashboard/src/main.tsx</files>
  <action>
    pnpm add vis-timeline@8.5.1 to dashboard. Import vis-timeline CSS in main.tsx or index.css per P5-D-03.

    EraTimeline: useRef container + useEffect mount Timeline with groups disabled; items = era bands (start/end from startAt/endAt as Date); on select call setSelectedEraId; destroy on unmount; update items via timeline.setItems not remount (pitfall 2 in RESEARCH).

    EraDetailPanel: react-query fetch /api/eras; show selected era summary, inflection types, claims list (evidence count only — confidence chips in Plan 05-06).

    DashboardLayout: timeline panel above or beside commit list (responsive). When era selected, CommitList inherits after/before from era window via drill store + CommitFilterBar shows era badge with clear.

    Extend listCommits era filter OR map era selection to after/before in client — prefer after/before from era timestamps to reuse Plan 05-02 filters (INGX-06 compatible).
  </action>
  <verify>
    <automated>pnpm build --filter=@gitchange/dashboard && pnpm vitest run tests/integration/dashboard-eras.test.ts 2>&1 | tail -3 | grep -q "passed"</automated>
  </verify>
  <done>Timeline renders era bands; era click filters commits to era window</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eras.json → API | Pre-validated artifact; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | I | Era detail claims text | accept | Static from local artifact; no HTML injection — React text nodes only |
| T-05-06 | D | vis-timeline DOM | accept | Localhost-only dashboard |
</threat_model>

<verification>
- pnpm vitest run packages/core/src/read/eras.test.ts tests/integration/dashboard-eras.test.ts
- Era selection reduces commit list to era window on fixture with known era bounds
</verification>

<success_criteria>
- TIME-01: interactive timeline with era markers; era click narrows commit exploration
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-03-SUMMARY.md` when done
</output>
