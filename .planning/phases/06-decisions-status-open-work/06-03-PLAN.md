---
phase: 06-decisions-status-open-work
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/core/src/status/infer.ts
  - packages/core/src/status/infer.test.ts
  - packages/core/src/status/thread-events.ts
  - packages/core/src/status/thread-events.test.ts
  - packages/core/src/decisions/assemble-open-work.ts
  - packages/core/src/decisions/assemble-open-work.test.ts
  - packages/core/src/semantic/pipeline.ts
  - packages/core/src/semantic/pipeline.test.ts
  - packages/core/src/schema/manifest.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [STAT-01, STAT-02]
user_setup: []

must_haves:
  truths:
    - "inferOpenWorkStatus classifies threads using keyword, trailer, and docs-vs-code methods per STAT-01"
    - "assembleOpenWork builds open-work.json from decisions + indexed file_changes chronology"
    - "Open threads list includes in-flight migrations, WIP refactors, and stale work with confidence scores"
    - "runDecisionsPipeline writes open-work.json and updates manifest checkpoint fields"
  artifacts:
    - path: "packages/core/src/status/infer.ts"
      provides: "STAT-01 tri-method status inference"
      exports: ["inferOpenWorkStatus", "detectDocsCodeDivergence"]
    - path: "packages/core/src/status/thread-events.ts"
      provides: "TIME-04 event chronology from file_changes"
      exports: ["buildThreadEvents"]
    - path: "packages/core/src/decisions/assemble-open-work.ts"
      provides: "OpenWorkArtifact assembly"
      exports: ["assembleOpenWork"]
    - path: "packages/core/src/semantic/pipeline.ts"
      provides: "Extended pipeline with decisions + open-work steps"
      exports: ["runDecisionsPipeline"]
  key_links:
    - from: "assemble-open-work.ts"
      to: "decisions.json in_flight status"
      via: "decision filter"
      pattern: "in_flight"
    - from: "thread-events.ts"
      to: "file_changes table"
      via: "path prefix query ordered by committedAt"
      pattern: "file_changes"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Deterministic status inferencer (STAT-01), open-work artifact assembly (STAT-02), migration thread event chronology (TIME-04 data layer), and pipeline extension to produce open-work.json after decisions exist.

Purpose: Populates open-work.json without LLM for status core path; unblocks dashboard open threads panel.
Output: infer module, thread events builder, assemble-open-work, runDecisionsPipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@packages/core/src/semantic/pipeline.ts
@packages/core/src/read/file-history.ts
@packages/core/src/decisions/candidates.ts
@packages/core/src/ingestion/commit-parse.ts

<interfaces>
From plan 06-01:
```typescript
export function readOpenWorkArtifact(gitchangeDir: string): OpenWorkArtifact | null;
export function writeOpenWorkArtifact(gitchangeDir: string, artifact: OpenWorkArtifact): void;
export type OpenWorkThread = { id, kind, status, confidence, events[], evidence[], linkedDecisionId? };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STAT-01 status inference engine</name>
  <files>packages/core/src/status/infer.ts, packages/core/src/status/infer.test.ts</files>
  <behavior>
    - Keyword method: WIP/TODO/FIXME in recent commits on thread paths → in_progress
    - Trailer method: Refs/Closes without matching completion commit → in_progress
    - Docs-vs-code: doc excerpt claims complete but file_changes in last 30 days on paths → in_progress with elevated confidence
    - Stale: no events in 90 days → stale kind
  </behavior>
  <action>
    Implement `inferOpenWorkStatus(thread, db, docSnapshots)` per P6-D-06 returning `{ status, confidence, signals[] }`. Parse trailer patterns from commit message text (regex for Refs:, Closes:, BREAKING CHANGE:). Implement `detectDocsCodeDivergence` comparing latest doc snapshot excerpt keywords (complete/done/migrated) against recent file_changes on relatedPaths. Use committedAt from indexed commits only. Export inference signal codes for dashboard tooltips.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/status/infer</automated>
  </verify>
  <done>Tri-method inference passes fixture scenarios for in_progress, stale, and possibly_complete</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread events + open-work assembly</name>
  <files>packages/core/src/status/thread-events.ts, packages/core/src/status/thread-events.test.ts, packages/core/src/decisions/assemble-open-work.ts, packages/core/src/decisions/assemble-open-work.test.ts</files>
  <behavior>
    - buildThreadEvents returns commits touching relatedPaths ordered newest-first, max 100 events
    - assembleOpenWork creates threads from decisions with status in_flight or unknown plus orphan WIP candidates not promoted to decisions
    - Each thread has evidence min 1 and confidence 0-1
  </behavior>
  <action>
    Implement `buildThreadEvents(db, relatedPaths[])` querying file_changes joined to commits for path prefix match — reuse validateFilePath guards. Implement `assembleOpenWork(gitchangeDir)` reading decisions.json (if present) and extractDecisionCandidates for WIP orphans; group by relatedPaths cluster; run inferOpenWorkStatus per thread; assign kind migration/refactor/wip/stale per P6-D-06 rules; cap 20 threads. Write via writeOpenWorkArtifact.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/status/thread-events packages/core/src/decisions/assemble-open-work</automated>
  </verify>
  <done>open-work.json assembles on BASIC_SCENARIO with at least one thread and ordered events</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runDecisionsPipeline + manifest checkpoint</name>
  <files>packages/core/src/semantic/pipeline.ts, packages/core/src/semantic/pipeline.test.ts, packages/core/src/schema/manifest.ts, packages/core/src/index.ts</files>
  <behavior>
    - runDecisionsPipeline requires eras.json and decisions.json present
    - Pipeline calls assembleOpenWork then validates open-work artifact
    - Manifest gains decisionsComputedAt, openWorkComputedAt, decisionsSchemaVersion, openWorkSchemaVersion
  </behavior>
  <action>
    Add `runDecisionsPipeline(gitchangeDir)` after existing semantic graph steps per P6-D-08: verify decisions.json exists (throw helpful error if missing), run assembleOpenWork, Zod-validate result, update manifest checkpoint fields additively (do not break existing manifest consumers). Extend pipeline.test.ts. Export runDecisionsPipeline from core index. Document invocation in 06-02 skill as post-decision-miner step.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/semantic/pipeline</automated>
  </verify>
  <done>Pipeline produces open-work.json and updates manifest on fixture with decisions + eras</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inference → open-work.json | Status labels are derived claims; must carry evidence and confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | False "complete" status | mitigate | docs-vs-code divergence downgrade |
| T-06-08 | Denial of Service | Unbounded thread events | mitigate | Cap 100 events per thread |
</threat_model>

<verification>
- Status inference unit tests cover all three STAT-01 methods
- assemble-open-work produces valid OpenWorkArtifact on fixture
- Manifest schema accepts new optional checkpoint fields
</verification>

<success_criteria>
- STAT-01 and STAT-02 satisfied at artifact layer
- TIME-04 event chronology available in open-work.json
- runDecisionsPipeline integrates with existing semantic pipeline
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-03-SUMMARY.md` when done
</output>
