---
phase: 05-dashboard-evidence-drill-down
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/core/src/ingestion/hunks.ts
  - packages/core/src/ingestion/hunks.test.ts
  - packages/core/src/index/process-commit.ts
  - packages/core/src/schema/zod/evidence.ts
  - packages/core/src/schema/zod/file-change.ts
  - packages/core/src/schema/drizzle/schema.ts
  - packages/core/migrations/0002_hunks_json.sql
  - packages/core/migrations/meta/0002_snapshot.json
  - packages/core/src/artifacts/writer.ts
  - packages/core/src/read/commit-detail.ts
  - packages/core/src/read/commit-detail.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/commit-detail.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/components/CommitDetailPanel.tsx
  - packages/dashboard/src/components/FileHunkView.tsx
  - packages/dashboard/src/components/DrillBreadcrumb.tsx
  - packages/dashboard/src/store/drill.ts
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - tests/integration/dashboard-commit-detail.test.ts
  - tests/golden/hunk-capture.test.ts
autonomous: true
requirements: [TIME-02, SCALE-02, DASH-01]
user_setup: []

must_haves:
  truths:
    - "Re-indexing fixture persists redacted patch hunks in SQLite hunks_json"
    - "User drills commit → file → sees diff hunk text without live git show"
    - "Ignored/redacted paths skip hunk capture; binary files skip hunks"
    - "Evidence hunk type enabled in Zod schema"
    - "Breadcrumb shows era → commit → file selection path"
  artifacts:
    - path: "packages/core/src/ingestion/hunks.ts"
      provides: "captureDiffHunks with privacy + caps per P5-D-02"
    - path: "packages/core/src/read/commit-detail.ts"
      provides: "getCommitDetail with file changes + hunks_json"
    - path: "packages/dashboard/src/components/FileHunkView.tsx"
      provides: "Diff hunk display panel"
  key_links:
    - from: "processCommit"
      to: "hunks_json column"
      via: "captureDiffHunks after diffCommit"
      pattern: "hunks_json"
    - from: "packages/dashboard/src/components/CommitDetailPanel.tsx"
      to: "/api/commits/:sha"
      via: "react-query"
      pattern: "commits/"
---

## Phase Goal

**As a** new hire or maintainer, **I want to** explore project history interactively with era→commit→file drill-down on localhost, **so that** I can understand how the codebase evolved with linked evidence without hunting git log.

<objective>
Index-time hunk capture (P5-D-02) + commit/file drill-down API and UI (TIME-02). Enables diff hunk evidence without es-git in server read path (SCALE-02).

Purpose: Closes critical gap — file_changes currently has null hunk fields; TIME-02 impossible without this plan.
Output: hunks_json migration, captureDiffHunks, GET /api/commits/:sha, CommitDetailPanel + FileHunkView + breadcrumbs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@packages/core/src/index/process-commit.ts
@packages/core/src/schema/zod/evidence.ts

<interfaces>
HunkRecord (stored in hunks_json per file_change):
- startLine, endLine, patch: string (redacted unified diff hunk body)
- Cap: max 20 hunks per file, 32KB total patch bytes per file (P5-D-02 discretion)

Enable Evidence union member:
{ type: "hunk", path, commitSha, startLine, endLine }

FileChangeRecord extends with optional hunks?: HunkRecord[] at write boundary.

getCommitDetail(gitchangeDir, sha) → { commit, files: [{ path, changeType, hunks, contentIgnored, contentRedacted }] }

Migration 0002: add hunks_json TEXT nullable to file_changes; drizzle meta snapshot.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Index-time hunk capture + migration</name>
  <files>packages/core/src/ingestion/hunks.ts, packages/core/src/ingestion/hunks.test.ts, packages/core/src/index/process-commit.ts, packages/core/src/schema/zod/evidence.ts, packages/core/src/schema/zod/file-change.ts, packages/core/src/schema/drizzle/schema.ts, packages/core/migrations/0002_hunks_json.sql, packages/core/migrations/meta/0002_snapshot.json, packages/core/src/artifacts/writer.ts, tests/golden/hunk-capture.test.ts</files>
  <behavior>
    - captureDiffHunks returns hunks for modified file with line ranges
    - applyPrivacy redacts secret patterns in patch text
    - contentIgnored paths produce zero hunks
    - Binary files produce zero hunks
    - Golden: re-index BASIC_SCENARIO → file_changes row has non-null hunks_json for known modified file
    - Evidence schema accepts hunk type; existing golden tests still pass
  </behavior>
  <action>
    Implement captureDiffHunks using es-git patch/diff on hot index path only (P5-D-02). Parse unified diff into hunk records; run applyPrivacy on patch strings; enforce caps.

    Uncomment/add hunk variant to Evidence zod per P4-D-07. Extend FileChangeRecord with optional hunks array for writer.

    Drizzle migration add hunks_json column; update writer to persist JSON.stringify(hunks) when present; keep hunkStart/hunkEnd null or deprecate unused.

    Wire into processCommit after diffCommit loop for non-binary, non-ignored paths.

  Add hunk-capture golden test requiring re-index of fixture. Document that existing .gitchange dirs need re-index for hunk data.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/ingestion/hunks.test.ts tests/golden/hunk-capture.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>Hunks persisted at index time with privacy; Evidence hunk type live</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit detail API + drill-down panels</name>
  <files>packages/core/src/read/commit-detail.ts, packages/core/src/read/commit-detail.test.ts, packages/core/src/index.ts, packages/server/src/routes/commit-detail.ts, packages/server/src/app.ts, packages/dashboard/src/components/CommitDetailPanel.tsx, packages/dashboard/src/components/FileHunkView.tsx, packages/dashboard/src/components/DrillBreadcrumb.tsx, packages/dashboard/src/store/drill.ts, packages/dashboard/src/layout/DashboardLayout.tsx, tests/integration/dashboard-commit-detail.test.ts</files>
  <behavior>
    - getCommitDetail returns commit message + files with hunks_json parsed
    - GET /api/commits/:sha returns 404 for unknown sha
    - Integration: index fixture → GET detail → files[].hunks length >= 1 for modified file
    - Click commit in list opens detail panel; click file shows FileHunkView
    - DrillBreadcrumb: Era name (if selected) > short sha > file path; click upstream clears downstream
  </behavior>
  <action>
    Implement getCommitDetail read-only from SQLite — no es-git. Mount GET /commits/:sha in separate commit-detail.ts route file (avoid conflict with list route).

    Dashboard: CommitDetailPanel slides or splits beside CommitList when selectedCommitSha set. FileHunkView renders patch with monospace pre; show "content redacted" / "path ignored" states without leaking bytes.

    DrillBreadcrumb wired to drill store clearDownstream actions per RESEARCH state machine.

    TIME-02: era (05-03) → commit → file → hunk path complete when combined with Plan 05-03; this plan delivers commit → file → hunk.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/commit-detail.test.ts tests/integration/dashboard-commit-detail.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>User drills commit to file to diff hunk from indexed data only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patch text → SQLite → UI | Secrets could leak if redaction skipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | I | hunks_json patch content | mitigate | applyPrivacy on every hunk; skip ignored paths; golden secret-leakage grep on hunk output |
| T-05-08 | I | Commit detail API | mitigate | 404 unknown sha; no path traversal — sha is 40-char hex validated |
</threat_model>

<verification>
- pnpm vitest run packages/core/src/ingestion/hunks.test.ts packages/core/src/read/commit-detail.test.ts tests/golden/hunk-capture.test.ts tests/integration/dashboard-commit-detail.test.ts
- grep confirms @gitchange/server still has no es-git
</verification>

<success_criteria>
- TIME-02 commit→file→hunk drill works from dashboard on re-indexed fixture
- SCALE-02: hunk data served from SQLite only at request time
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-04-SUMMARY.md` when done
</output>
