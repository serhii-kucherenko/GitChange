---
phase: 08-hardening-scale-multi-repo
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-03", "08-04"]
files_modified:
  - packages/core/src/verify/workspace-integrity.ts
  - packages/core/src/verify/workspace-integrity.test.ts
  - tests/golden/workspace-evidence-integrity.test.ts
  - tests/integration/dashboard-multi-repo-e2e.test.ts
  - tests/integration/dashboard-dash02-acceptance.test.ts
  - packages/cli/src/commands/validate.ts
  - packages/cli/src/commands/serve.ts
  - packages/server/src/start.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [SCALE-01, MULTI-01, MULTI-02, DASH-02]
user_setup: []

must_haves:
  truths:
    - "Golden test validates workspace with two repos: every unified commit has repoId; tour stops attributed"
    - "E2E: workspace index → serve → timeline commits show repo badges → graph tab loads → tour player drills with repoId"
    - "gitchange validate runs checkWorkspaceIntegrity when workspace.json present"
    - "Incremental scale test: second workspace index on unchanged HEAD indexes 0 commits per repo"
    - "DASH-02 acceptance: all three surfaces (timeline, graph, tours) reachable in one dashboard session"
  artifacts:
    - path: "tests/integration/dashboard-dash02-acceptance.test.ts"
      provides: "DASH-02 milestone acceptance E2E"
    - path: "tests/integration/dashboard-multi-repo-e2e.test.ts"
      provides: "MULTI-01/02 federated dashboard E2E"
    - path: "packages/core/src/verify/workspace-integrity.ts"
      provides: "Workspace + multi-repo provenance verifier"
  key_links:
    - from: "tests/integration/dashboard-dash02-acceptance.test.ts"
      to: "GET /api/commits + /api/graph + /api/tours"
      via: "tab surface smoke sequence"
      pattern: "api/commits|api/graph|api/tours"
    - from: "packages/cli/src/commands/validate.ts"
      to: "packages/core/src/verify/workspace-integrity.ts"
      via: "checkWorkspaceIntegrity gate"
      pattern: "checkWorkspaceIntegrity"
---

<objective>
Phase 8 completion gate: golden workspace integrity, multi-repo dashboard E2E, DASH-02 acceptance proving timeline + graph + tour player together, validate CLI extension, and serve workspace auto-detection.

Purpose: Proves SCALE-01, MULTI-01, MULTI-02, and DASH-02 success criteria together; mirrors 06-06 / 07-05 pattern.
Output: Golden tests, integration E2E harness, validate CLI workspace check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hardening-scale-multi-repo/08-RESEARCH.md
@tests/integration/dashboard-drilldown-e2e.test.ts
@tests/integration/dashboard-tour-e2e.test.ts
@packages/cli/src/commands/validate.ts
@packages/core/src/verify/tours-snapshot.ts

<interfaces>
Existing E2E harness pattern:
- Spawns `gitchange serve` on GITCHANGE_PORT
- Uses synthetic repo fixtures
- Asserts API JSON shapes and navigation sequences

From validate.ts — extend with workspace check when workspace.json exists alongside artifact checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace integrity verifier + golden gate</name>
  <files>packages/core/src/verify/workspace-integrity.ts, packages/core/src/verify/workspace-integrity.test.ts, tests/golden/workspace-evidence-integrity.test.ts, packages/core/src/index.ts, packages/cli/src/commands/validate.ts</files>
  <behavior>
    - checkWorkspaceIntegrity: each repo entry path exists; manifest.repoId matches workspace repoId; unified commit list returns repoId for all rows when repos.length > 1
    - Rejects workspace.links referencing unknown repoIds
    - Golden test on two-repo BASIC_SCENARIO workspace fixture
    - gitchange validate exits 1 when workspace repo manifest missing
  </behavior>
  <action>
    Implement `checkWorkspaceIntegrity(workspacePath)` using resolveWorkspaceContext + listCommitsUnified + mergeToursForWorkspace. Verify no evidence ref lacks repoId when multi-repo. Add golden test `workspace-evidence-integrity.test.ts` building two indexed synthetic repos, writing workspace.json with manual link entry, asserting integrity passes. Extend `gitchange validate` to call checkWorkspaceIntegrity when workspace.json found in gitchangeDir (same pattern as checkToursIntegrity). Export verifier from @gitchange/core.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/golden/workspace-evidence-integrity.test.ts packages/core/src/verify/workspace-integrity.test.ts</automated>
  </verify>
  <done>Golden workspace integrity passes; validate CLI workspace gate works</done>
</task>

<task type="auto">
  <name>Task 2: Multi-repo + DASH-02 acceptance E2E</name>
  <files>tests/integration/dashboard-multi-repo-e2e.test.ts, tests/integration/dashboard-dash02-acceptance.test.ts, packages/cli/src/commands/serve.ts, packages/server/src/start.ts</files>
  <action>
    Multi-repo E2E (MULTI-01/02): create two synthetic repos, `workspace add` both, `workspace index`, serve from primary gitchangeDir with workspace auto-detect (extend serve/startServer to pass workspacePath when workspace.json exists beside gitchangeDir). Assert GET /api/workspace lists 2 repos; GET /api/commits returns commits with distinct repoId; filter by repoId returns single-repo slice. DASH-02 acceptance test: same harness loads timeline (commits list), graph tab (/api/graph nodes length > 0), tours tab (/api/tours default tour present) — three surfaces in one test file. Include incremental assertion: re-run workspace index on unchanged HEAD → 0 new commits per repo (SCALE-01 two-phase). Bind fixtures to pre-built semantic + tour artifacts (bindBasicScenario pattern) so no live LLM. Use GITCHANGE_PORT ephemeral binding per Phase 3 precedent.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/integration/dashboard-multi-repo-e2e.test.ts tests/integration/dashboard-dash02-acceptance.test.ts</automated>
  </verify>
  <done>Multi-repo E2E and DASH-02 acceptance pass in CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E harness | Localhost only; synthetic fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | E2E fixture paths | accept | temp dirs only; no real user paths |
| T-08-10 | Tampering | workspace golden | mitigate | integrity verifier runs before E2E assumes valid state |
</threat_model>

<verification>
- Full phase test suite: core workspace + unified reads + graph + scale benchmark
- `pnpm exec vitest run tests/integration/dashboard-dash02-acceptance.test.ts` passes
- Phase 8 success criteria from ROADMAP all have automated coverage
</verification>

<success_criteria>
- SCALE-01 incremental architecture verified in multi-repo E2E
- MULTI-01/02 manual workspace + unified attribution proven
- DASH-02 timeline + graph + tour player all reachable with pre-built index only
</success_criteria>

<output>
Create `.planning/phases/08-hardening-scale-multi-repo/08-05-SUMMARY.md` when done
</output>
