---
phase: 04-era-detection-semantic-pipeline
plan: 05
type: execute
wave: 4
depends_on: [04-02, 04-04]
files_modified:
  - tests/fixtures/semantic/eras-basic-scenario.json
  - tests/golden/semantic.test.ts
  - tests/golden/helpers.ts
  - packages/cli/src/commands/validate.ts
  - packages/cli/src/bin.ts
  - packages/core/src/read/snapshot.ts
  - packages/core/src/read/snapshot.test.ts
  - packages/server/src/routes/snapshot.ts
  - packages/plugin/schemas/snapshot.schema.json
  - packages/plugin/scripts/generate-schemas.ts
  - packages/plugin/schemas/eras-summary.schema.json
autonomous: true
requirements: [ERA-01, ERA-02, ERA-03]
user_setup: []

must_haves:
  truths:
    - "Golden semantic test locks eras + temporal-graph integrity on BASIC_SCENARIO + fixture eras"
    - "User runs `gitchange validate` and sees pass/fail for semantic artifacts"
    - "User sees era names and inflection counts in snapshot API / status (ERA-02 visibility)"
    - "Full pipeline: index → fixture eras → runSemanticPipeline → validate passes"
  artifacts:
    - path: "tests/golden/semantic.test.ts"
      provides: "Golden gate for Phase 4 artifacts"
    - path: "tests/fixtures/semantic/eras-basic-scenario.json"
      provides: "Deterministic eras fixture for tests without live LLM"
    - path: "packages/cli/src/commands/validate.ts"
      provides: "gitchange validate command"
  key_links:
    - from: "packages/core/src/read/snapshot.ts"
      to: ".gitchange/eras.json"
      via: "erasSummary highlights"
      pattern: "readErasArtifact|erasSummary"
    - from: "tests/golden/semantic.test.ts"
      to: "packages/core/src/verify/semantic-integrity.ts"
      via: "checkSemanticIntegrity assertion"
      pattern: "checkSemanticIntegrity"
---

## Phase Goal

**As a** new team member, **I want to** understand how the project evolved through named eras and inflection points backed by evidence, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Close Phase 4 with golden fixtures, CLI validation command, and user-visible era summary in snapshot API — proving ERA-01/02/03 end-to-end without requiring live host LLM in CI.

Purpose: Same trust bar as Phase 1/2 golden tests; ERA-02 requires user can *see* evolution summaries via status/snapshot/chat presentation path.
Output: Golden tests, fixture, gitchange validate, snapshot era slice, schema updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-era-detection-semantic-pipeline/04-RESEARCH.md
@.planning/phases/01-index-foundation/01-08-SUMMARY.md
@.planning/phases/02-repository-intelligence-ownership/02-05-SUMMARY.md
@tests/golden/helpers.ts
@packages/cli/src/commands/index.ts

<interfaces>
```typescript
export function runSemanticPipeline(gitchangeDir: string): Promise<{ ok: boolean; errors: string[] }>;
export function checkSemanticIntegrity(gitchangeDir: string): SemanticIntegrityReport;
export function getRepoSnapshot(gitchangeDir: string): RepoSnapshot;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Golden fixture + semantic golden test</name>
  <files>tests/fixtures/semantic/eras-basic-scenario.json, tests/golden/semantic.test.ts, tests/golden/helpers.ts</files>
  <behavior>
    - Fixture ErasArtifact has 2 named eras, ≥1 inflection per ERA-03 type coverage across fixture, all claims have evidence
    - Golden test: index BASIC_SCENARIO → computeIntelligence → copy/write fixture eras → runSemanticPipeline → checkSemanticIntegrity ok
    - Locked counts: era count, inflection count, graph node count (use verify/semantic-snapshot.ts pattern)
  </behavior>
  <action>
    Author `eras-basic-scenario.json` manually aligned to BASIC_SCENARIO signal IDs (read from intelligence export in test setup). Create `semantic-snapshot.ts` with locked counts similar to intelligence-snapshot.ts. Golden test in tests/golden/semantic.test.ts; extend helpers if needed. Gate dogfood behind env flag if repo-specific like Phase 1.
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/semantic.test.ts</automated>
  </verify>
  <done>Golden semantic test passes on CI; integrity ok; counts locked</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gitchange validate command</name>
  <files>packages/cli/src/commands/validate.ts, packages/cli/src/bin.ts</files>
  <behavior>
    - gitchange validate --repo PATH exits 0 when intelligence + semantic integrity pass
    - Exits non-zero with stderr listing errors when semantic artifacts missing or integrity fails
    - Works without eras.json (reports semantic artifacts missing, exits 1) — does not fake pass
  </behavior>
  <action>
    Add validate subcommand via commander. Call checkIntelligenceIntegrity and checkSemanticIntegrity from @gitchange/core. Register in bin.ts. Integration test optional in cli-index pattern or covered by golden test invoking CLI.
  </action>
  <verify>
    <automated>pnpm vitest run tests/golden/semantic.test.ts 2>/dev/null; pnpm exec gitchange validate --help | grep -q validate</automated>
  </verify>
  <done>validate command registered and reports integrity status</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Snapshot era summary for ERA-02 visibility</name>
  <files>packages/core/src/read/snapshot.ts, packages/core/src/read/snapshot.test.ts, packages/server/src/routes/snapshot.ts, packages/plugin/scripts/generate-schemas.ts, packages/plugin/schemas/snapshot.schema.json, packages/plugin/schemas/eras-summary.schema.json</files>
  <behavior>
    - RepoSnapshot includes erasSummary: { eraCount, inflectionCount, eras: [{ name, summary, inflectionTypes[] }] } when eras.json present
    - GET /api/snapshot returns erasSummary; null when no semantic artifacts
    - snapshot.schema.json updated via generate-schemas
  </behavior>
  <action>
    Extend getRepoSnapshot read path with readErasArtifact; build bounded erasSummary (max 8 eras, truncate summary to 200 chars for API). Update server route to pass through. Regenerate plugin schemas. snapshot.test.ts asserts erasSummary on fixture.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/snapshot.test.ts tests/integration/plugin-schemas.test.ts</automated>
  </verify>
  <done>Snapshot API exposes era names/summaries; schema validates; ERA-02 user visibility path exists</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot API → browser | Read-only bounded excerpts; no full artifact dump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | erasSummary in API | mitigate | Truncate summaries; localhost-only server (P3-D-01) |
| T-04-12 | Tampering | validate command bypass | mitigate | Runs same checkSemanticIntegrity as pipeline gate |
</threat_model>

<verification>
- `pnpm vitest run tests/golden/semantic.test.ts` passes
- `pnpm vitest run tests/integration/plugin-schemas.test.ts` passes
- Phase 4 requirements ERA-01, ERA-02, ERA-03 covered by golden + snapshot tests
</verification>

<success_criteria>
- Golden semantic fixture test green
- gitchange validate reports semantic integrity
- User sees era summaries via snapshot/status path
</success_criteria>

<output>
Create `.planning/phases/04-era-detection-semantic-pipeline/04-05-SUMMARY.md` when done
</output>
