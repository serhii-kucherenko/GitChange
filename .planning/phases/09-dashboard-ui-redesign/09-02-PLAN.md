---
phase: 09-dashboard-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/dashboard/src/components/EraTimeline.tsx
  - packages/dashboard/src/index.css
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "At ≤8 named eras, no era label renders as run-together words from two different items — each item shows either its full name or a clean … ellipsis"
    - "Hovering a truncated era item reveals the full era name via a native tooltip"
    - "The era timeline strip has vertical room (min-h-[8rem]) and readable axis/item text"
    - "vis-timeline behavior, selection logic, and the .open-work-badge injection are unchanged; existing tests green and typecheck passes"
  artifacts:
    - path: "packages/dashboard/src/index.css"
      provides: ".era-timeline .vis-item-content truncation rule (max-width + ellipsis + nowrap)"
      contains: "text-overflow"
    - path: "packages/dashboard/src/components/EraTimeline.tsx"
      provides: "Native title (full era name) on each timeline item + min-h-[8rem] strip"
      contains: "title:"
  key_links:
    - from: "packages/dashboard/src/components/EraTimeline.tsx"
      to: "vis-timeline item.title"
      via: "toTimelineItems adds title: era.name"
      pattern: "title:\\s*era\\.name"
    - from: "packages/dashboard/src/index.css"
      to: ".vis-item-content box"
      via: "CSS max-width/ellipsis override"
      pattern: "\\.vis-item-content"
---

<objective>
Fix the core era-timeline label overlap/truncation defect using CSS + label formatting only: constrain each `.vis-item-content` box with `max-width` + ellipsis + nowrap, add a native `title` (full era name) to each vis-timeline item for hover reveal, and raise the strip to `min-h-[8rem]`. No vis-timeline behavior change, no new dependency, no selection-logic change.

Purpose: Satisfies DASH-02 success criterion "Era timeline renders era labels without overlap or truncation at realistic era counts." Directly resolves the "Mono Git Schema Ba Plugin distribution & onb" run-together defect named in the spec.

Output: Additive `.era-timeline` CSS rule in `index.css` and a `title` field + min-height bump in `EraTimeline.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-dashboard-ui-redesign/09-UI-SPEC.md
@.planning/phases/09-dashboard-ui-redesign/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Constrain era label boxes + add native tooltip + raise strip height</name>
  <files>packages/dashboard/src/components/EraTimeline.tsx, packages/dashboard/src/index.css</files>
  <read_first>
    - packages/dashboard/src/components/EraTimeline.tsx (`formatEraLabel` lines 22-36, `toTimelineItems` lines 38-49, container `className="era-timeline min-h-[7rem] …"` line 225, select/setSelection logic lines 106-175) — the file being modified
    - packages/dashboard/src/index.css (`.era-timeline` block lines 13-61, including `.open-work-badge*` variants lines 34-61) — the file being modified
    - 09-UI-SPEC.md §"Era Timeline Label Strategy" (all six points) and §Color point 4 (axis `slate-400` 12px, item text contrast ≥3:1)
    - 09-RESEARCH.md §"vis-timeline era label + CSS location", Pitfall 4 (do not touch open-work-badge-html.ts or remove .open-work-badge classes)
  </read_first>
  <action>
    In `index.css`, add an additive rule scoped to `.era-timeline .vis-item .vis-item-content`: `max-width` (start at `12rem`; a value that reads clean at ≤8 eras), `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`, `padding: 0 8px`. Keep the existing `.era-timeline` item/axis rules and ALL `.open-work-badge*` classes intact — only add the truncation rule around them. Ensure axis text stays readable at 12px `slate-400` (adjust the existing `.vis-time-axis .vis-text` color to `#94a3b8` if it is currently dimmer) and item text meets ≥3:1 contrast on the existing translucent cyan fill (keep the selected/unselected fill treatment).

    In `EraTimeline.tsx toTimelineItems`, add `title: era.name` to each item object alongside the existing `content: formatEraLabel(...)` so hover reveals the untruncated name (native vis-timeline tooltip — no React portal, no new library). Do NOT change `formatEraLabel`, the open-work badge HTML injection, or the `select`/`setSelection` logic. Bump the container className from `min-h-[7rem]` to `min-h-[8rem]` on line 225.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes (green).
    - `pnpm --dir packages/dashboard test` stays green.
    - `index.css` contains a `.era-timeline .vis-item .vis-item-content` rule with `max-width`, `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`, `padding: 0 8px`; the `.open-work-badge*` classes remain present and unmodified.
    - `EraTimeline.tsx` `toTimelineItems` sets `title: era.name` on each item; `formatEraLabel`, the open-work badge injection, and `select`/`setSelection` are unchanged.
    - Container className is `era-timeline min-h-[8rem] …` (was `min-h-[7rem]`).
    - No new `dangerouslySetInnerHTML` and no change to `open-work-badge-html.ts`.
    - Manual (per 09-VALIDATION.md): at ≤8 eras no run-together labels; each item shows full name or clean `…`; hover reveals full name.
  </acceptance_criteria>
  <done>Era labels truncate cleanly with a working hover tooltip; strip has room; vis-timeline behavior and badge injection unchanged; typecheck + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template HTML → vis-timeline item.content | Existing controlled `open-work-badge-html.ts` template string; NOT user-derived; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Injection (I) | EraTimeline vis-item content/title | mitigate | Preserve the existing controlled template HTML injection; add only text-truncating CSS and a native `title` set to `era.name` (plain text). Do NOT introduce a new `dangerouslySetInnerHTML` or inject user-derived strings as HTML — the one thing to preserve, not weaken (09-RESEARCH §Security Domain). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (no new dependencies permitted); Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `pnpm --dir packages/dashboard typecheck` green; `pnpm --dir packages/dashboard test` green.
- Manual (per 09-VALIDATION.md): era strip full-width `min-h-[8rem]`; labels ellipsis + hover tooltip; no run-together labels at ≤8 eras.
</verification>

<success_criteria>
- `.vis-item-content` truncation rule added; `title: era.name` on every item; strip at `min-h-[8rem]`.
- No vis-timeline behavior/selection change; `.open-work-badge` and its generator untouched.
- Typecheck + existing tests green.
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-ui-redesign/09-02-SUMMARY.md` when done.
</output>
