---
phase: 04-era-detection-semantic-pipeline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/plugin/agents/era-synthesizer.md
  - packages/plugin/agents/gitchange-orchestrator.md
  - packages/plugin/skills/gitchange/SKILL.md
  - packages/plugin/scripts/generate-schemas.ts
  - packages/plugin/schemas/eras.schema.json
  - packages/plugin/schemas/era-synthesis-context.schema.json
  - packages/plugin/package.json
  - tests/integration/plugin-schemas.test.ts
autonomous: true
requirements: [ERA-01, ERA-02, ERA-03]
user_setup: []

must_haves:
  truths:
    - "era-synthesizer agent spec instructs host AI to output ErasArtifact JSON only — no embedded model calls from GitChange"
    - "/gitchange skill runs semantic Phase 2 after index: build context → host synthesizes → writeErasArtifact"
    - "Every inflection in agent output must use InflectionType enum with evidence (ERA-03)"
    - "Plugin JSON schemas validate era synthesis context and eras artifact shapes"
  artifacts:
    - path: "packages/plugin/agents/era-synthesizer.md"
      provides: "Host-LLM era naming + inflection detection spec"
    - path: "packages/plugin/schemas/eras.schema.json"
      provides: "Bounded eras.json schema for host AI"
    - path: "packages/plugin/skills/gitchange/SKILL.md"
      provides: "Extended /gitchange with semantic synthesis step"
  key_links:
    - from: "packages/plugin/skills/gitchange/SKILL.md"
      to: "packages/core buildEraSynthesisContext"
      via: "skill instructs host to call context then writeErasArtifact"
      pattern: "buildEraSynthesisContext|writeErasArtifact|era-synthesizer"
    - from: "packages/plugin/agents/era-synthesizer.md"
      to: "packages/core/src/schema/zod/eras.ts"
      via: "output must match ErasArtifact"
      pattern: "InflectionType|signalIds"
---

## Phase Goal

**As a** new team member, **I want to** understand how the project evolved through named eras and inflection points backed by evidence, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Deliver the host-LLM era synthesis surface: `era-synthesizer` agent spec, extended `/gitchange` orchestration, and JSON schemas for bounded era input/output.

Purpose: ERA-01/02/03 narrative content is produced by host AI following strict contracts; GitChange supplies specs and validation only (PLUG-05).
Output: Agent markdown, updated skills/orchestrator, generated JSON schemas, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-era-detection-semantic-pipeline/04-RESEARCH.md
@.planning/phases/04-era-detection-semantic-pipeline/04-01-PLAN.md
@packages/plugin/agents/gitchange-orchestrator.md
@packages/plugin/skills/gitchange/SKILL.md
@packages/plugin/scripts/generate-schemas.ts

<interfaces>
From Plan 04-01 (implement before this plan executes):
```typescript
export function buildEraSynthesisContext(gitchangeDir: string): EraSynthesisContext;
export function writeErasArtifact(gitchangeDir: string, artifact: ErasArtifact): void;
export const InflectionType = z.enum(["tech_pivot", "scope_steering", "process_shift", "team_ownership_change"]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: era-synthesizer agent spec</name>
  <files>packages/plugin/agents/era-synthesizer.md</files>
  <action>
    Create agent spec per ARCHITECTURE Pattern 3. Input: `EraSynthesisContext` JSON only (never raw git). Output: single `ErasArtifact` JSON matching core Zod schema. Rules per P4-D-02/P4-D-03: (1) Name 3–8 eras anchored to provided signalIds; do not invent commits outside indexed range. (2) Each era has claims[] — every claim sentence individually evidence-linked (ERA-02). (3) Each inflection typed with InflectionType enum and maps from signals: path_churn_pivot→tech_pivot, cc_scope_shift→scope_steering, author_spike→team_ownership_change; process_shift from CI/config/.github churn if present. (4) Use doc evidence for README/ADR citations when docDeltas provided. (5) If attributionConfidence is degraded, use cautious language in summaries. (6) Generate era ids with ulid prefix `era:`. Host AI writes result via `writeErasArtifact` — GitChange code never calls LLM APIs.
  </action>
  <verify>
    <automated>grep -q "InflectionType\|tech_pivot\|signalIds" packages/plugin/agents/era-synthesizer.md && grep -q "writeErasArtifact\|ErasArtifact" packages/plugin/agents/era-synthesizer.md</automated>
  </verify>
  <done>Agent spec covers input contract, output schema, inflection taxonomy, and evidence rules</done>
</task>

<task type="auto">
  <name>Task 2: Extend /gitchange skill and orchestrator</name>
  <files>packages/plugin/skills/gitchange/SKILL.md, packages/plugin/agents/gitchange-orchestrator.md</files>
  <action>
    Add **Phase 2 — Semantic era synthesis** after index step in SKILL.md and orchestrator. Steps: (1) Verify intelligence.json exists (index must have run). (2) Host loads `buildEraSynthesisContext` output — document as Node one-liner or instruct host to run small helper script `packages/plugin/scripts/build-era-context.ts` that prints JSON to stdout if direct import unavailable from skill context. (3) Host executes era-synthesizer agent spec. (4) Host validates output against eras.schema.json. (5) Host persists via writeErasArtifact (script wrapper `packages/plugin/scripts/write-eras.ts` accepting JSON path acceptable). (6) Present era names + inflection count to user (ERA-02 visibility in chat). Do not auto-run on every index if manifest head unchanged and eras.json already valid — offer re-synthesize when intelligence head differs from eras headSha.
  </action>
  <verify>
    <automated>grep -q "era-synthesizer\|semantic" packages/plugin/skills/gitchange/SKILL.md && grep -q "era-synthesizer" packages/plugin/agents/gitchange-orchestrator.md</automated>
  </verify>
  <done>Slash command documents full index → semantic pipeline; PLUG-05 preserved (no LLM SDK in packages)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JSON schemas + integration tests</name>
  <files>packages/plugin/scripts/generate-schemas.ts, packages/plugin/schemas/eras.schema.json, packages/plugin/schemas/era-synthesis-context.schema.json, packages/plugin/package.json, tests/integration/plugin-schemas.test.ts</files>
  <behavior>
    - generate-schemas emits eras.schema.json from ErasArtifact Zod
    - Fixture golden era artifact validates against eras.schema.json
    - PLUG-05 grep gate still passes after changes
  </behavior>
  <action>
    Extend generate-schemas.ts to export ErasArtifact and EraSynthesisContext types from @gitchange/core. Add schemas to plugin package.json schemas list. Add integration test loading `tests/fixtures/semantic/eras-basic-scenario.json` (create minimal valid fixture in this task if 04-05 not run yet — minimal 1-era artifact). Extend plugin-schemas.test.ts; do not weaken PLUG-05 grep patterns.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/plugin-schemas.test.ts</automated>
  </verify>
  <done>eras.schema.json validates fixture; plugin schema tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host AI → eras.json | Agent output crosses trust boundary; validated before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | era-synthesizer output | mitigate | writeErasArtifact Zod gate; schema validation in skill |
| T-04-05 | Tampering | Host invents SHAs | mitigate | Agent spec forbids refs outside context; 04-04 integrity checker |
| T-04-06 | Repudiation | Missing semantic step | accept | User-triggered via /gitchange; manifest checkpoint in 04-04 |
</threat_model>

<verification>
- `pnpm vitest run tests/integration/plugin-schemas.test.ts` passes
- `rg -l "openai|anthropic|@ai-sdk" packages/plugin packages/cli packages/core/src/semantic` returns empty
</verification>

<success_criteria>
- era-synthesizer agent spec committed
- /gitchange documents semantic synthesis phase
- eras JSON schema generated and tested
</success_criteria>

<output>
Create `.planning/phases/04-era-detection-semantic-pipeline/04-02-SUMMARY.md` when done
</output>
