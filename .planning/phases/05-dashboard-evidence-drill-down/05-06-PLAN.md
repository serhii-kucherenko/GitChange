---
phase: 05-dashboard-evidence-drill-down
plan: 06
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04", "05-05"]
files_modified:
  - packages/dashboard/src/components/IndexStatusCard.tsx
  - packages/dashboard/src/components/ConfidenceBadge.tsx
  - packages/dashboard/src/components/EraDetailPanel.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/types.ts
  - packages/cli/src/commands/serve.ts
  - packages/server/src/start.ts
  - packages/server/src/routes/snapshot.ts
  - tests/integration/dashboard-localhost-bind.test.ts
  - tests/integration/dashboard-evidence-e2e.test.ts
autonomous: true
requirements: [DASH-03, EVD-02, PRIV-04, DASH-01]
user_setup: []

must_haves:
  truths:
    - "Dashboard header shows attributionConfidence badge and semantic schema version when present"
    - "Era detail shows per-claim confidence chips derived from evidence count (P5-D-05)"
    - "gitchange serve binds 127.0.0.1 by default; test asserts no 0.0.0.0 without explicit flag"
    - "End-to-end test: era select → commit → file → hunk path completes on fixture"
  artifacts:
    - path: "packages/dashboard/src/components/ConfidenceBadge.tsx"
      provides: "EVD-02 confidence display"
    - path: "tests/integration/dashboard-localhost-bind.test.ts"
      provides: "PRIV-04 bind assertion"
    - path: "tests/integration/dashboard-evidence-e2e.test.ts"
      provides: "Full drill-down gate"
  key_links:
    - from: "ConfidenceBadge"
      to: "intelligence.attributionConfidence"
      via: "snapshot manifest + intelligence"
      pattern: "attributionConfidence"
    - from: "serve command"
      to: "127.0.0.1"
      via: "default host"
      pattern: "127\\.0\\.0\\.1"
---

## Phase Goal

**As a** new team member or maintainer, **I want to** explore project history interactively on localhost with era-aware drill-down to commits, files, and diff hunks, **so that** I can verify every narrative claim against indexed evidence without hunting git log.

<objective>
Polish and gate Phase 5: confidence UI (EVD-02), index freshness/schema display (DASH-03), PRIV-04 localhost hardening, full evidence drill-down E2E integration test.

Purpose: Closes requirement gaps and provides CI gate for Phase 5 success criteria.
Output: ConfidenceBadge, EraDetailPanel with claim confidence, localhost bind test, E2E drill-down test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@packages/dashboard/src/components/IndexStatusCard.tsx
@packages/cli/src/commands/serve.ts
@packages/server/src/start.ts

<interfaces>
Manifest fields: schemaVersion, warnings, intelligenceSchemaVersion, semanticSchemaVersion (if present on manifest from Phase 4).
intelligence.attributionConfidence: "complete" | "degraded".
P5-D-05 claim confidence: high if evidence.length >= 3, medium if 2, low if 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confidence UI + enhanced index status (EVD-02, DASH-03)</name>
  <files>packages/dashboard/src/components/ConfidenceBadge.tsx, packages/dashboard/src/components/EraDetailPanel.tsx, packages/dashboard/src/components/IndexStatusCard.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/types.ts, packages/server/src/routes/snapshot.ts</files>
  <action>
    Create ConfidenceBadge component mapping attributionConfidence to Complete (green) / Degraded (amber) labels; downgrade visually when manifest.warnings non-empty. Extend IndexStatusCard to show semanticSchemaVersion and intelligenceSchemaVersion when on manifest (read from snapshot response — extend types if snapshot already includes manifest fields). Create EraDetailPanel shown when selectedEraId set: fetch /api/eras, show era summary, inflections list, claims with ConfidenceBadge per claim using P5-D-05 heuristic (evidence count). Place EraDetailPanel beside timeline. Extend snapshot types if semantic fields missing from client types.
  </action>
  <verify>
    <automated>pnpm exec biome check packages/dashboard/src/components/ConfidenceBadge.tsx packages/dashboard/src/components/EraDetailPanel.tsx</automated>
  </verify>
  <done>Dashboard shows confidence and full schema/freshness metadata</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRIV-04 bind test + evidence E2E gate</name>
  <files>packages/cli/src/commands/serve.ts, packages/server/src/start.ts, tests/integration/dashboard-localhost-bind.test.ts, tests/integration/dashboard-evidence-e2e.test.ts</files>
  <behavior>
    - runServeCommand without host option passes hostname 127.0.0.1 to startServer (unit-style assertion on options object or spawn test)
    - dashboard-localhost-bind: server address is 127.0.0.1 not 0.0.0.0
    - dashboard-evidence-e2e: index fixture with eras + hunks → GET /api/eras → GET /api/commits with era date filter → GET /api/commits/:sha → response includes file with hunks array length >= 1
    - SCALE-02 grep: server package has zero es-git imports
  </behavior>
  <action>
    Add dashboard-localhost-bind.test.ts spawning serve on ephemeral port, assert listening address 127.0.0.1 (use server.address() or fetch health only on 127.0.0.1). Document PRIV-04 in serve.ts comment if needed. Add dashboard-evidence-e2e.test.ts chaining API calls simulating drill path (no browser required). Ensure --host 0.0.0.0 still works with warning (existing behavior). Run full integration suite for dashboard tests.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/dashboard-localhost-bind.test.ts tests/integration/dashboard-evidence-e2e.test.ts && rg "es-git" packages/server/src -c | rg -q "^0$" || test $(rg -c "es-git" packages/server/src 2>/dev/null || echo 0) -eq 0</automated>
  </verify>
  <done>PRIV-04 tested; full evidence drill-down API path verified in CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network | localhost bind policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | 0.0.0.0 bind | mitigate | Default 127.0.0.1; warn on override; PRIV-04 test |
| T-05-10 | Spoofing | confidence display | accept | Heuristic UI only; not security boundary |
</threat_model>

<verification>
- All dashboard integration tests pass under Node 22
- pnpm turbo test --filter=@gitchange/dashboard --filter=@gitchange/server
- Manual smoke: /gitchange-dashboard flow still works
</verification>

<success_criteria>
- DASH-01, DASH-03, EVD-02, PRIV-04 requirements satisfied
- Phase 5 ROADMAP success criteria 1–5 achievable via implemented features
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-06-SUMMARY.md` when done
</output>
