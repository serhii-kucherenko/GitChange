---
phase: 06-decisions-status-open-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schema/zod/evidence.ts
  - packages/core/src/schema/zod/evidence.test.ts
  - packages/core/src/schema/zod/decisions.ts
  - packages/core/src/schema/zod/decisions.test.ts
  - packages/core/src/schema/zod/open-work.ts
  - packages/core/src/schema/zod/open-work.test.ts
  - packages/core/src/schema/zod/index.ts
  - packages/core/src/decisions/candidates.ts
  - packages/core/src/decisions/candidates.test.ts
  - packages/core/src/decisions/context.ts
  - packages/core/src/decisions/context.test.ts
  - packages/core/src/decisions/threshold.ts
  - packages/core/src/decisions/threshold.test.ts
  - packages/core/src/decisions/decisions-io.ts
  - packages/core/src/decisions/decisions-io.test.ts
  - packages/core/src/decisions/open-work-io.ts
  - packages/core/src/decisions/open-work-io.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [DEC-01, EVD-03]
user_setup: []

must_haves:
  truths:
    - "DecisionsArtifact and OpenWorkArtifact Zod schemas exist with mandatory evidence on narrative fields"
    - "Deterministic mining produces noise-filtered candidates from indexed commits and doc snapshots only"
    - "isBelowEvidenceThreshold returns true when confidence < 0.35 or evidence empty — EVD-03 gate"
    - "buildDecisionMiningContext returns bounded JSON without live git"
    - "writeDecisionsArtifact rejects invalid JSON before atomic write to .gitchange/decisions.json"
  artifacts:
    - path: "packages/core/src/schema/zod/decisions.ts"
      provides: "DecisionsArtifact, DecisionRecord, DecisionStatus, supersession fields"
      exports: ["DecisionsArtifact", "DECISIONS_SCHEMA_VERSION", "DecisionRecord"]
    - path: "packages/core/src/schema/zod/open-work.ts"
      provides: "OpenWorkArtifact, OpenWorkThread, thread event shape for TIME-04"
      exports: ["OpenWorkArtifact", "OPEN_WORK_SCHEMA_VERSION"]
    - path: "packages/core/src/decisions/candidates.ts"
      provides: "extractDecisionCandidates from SQLite"
      exports: ["extractDecisionCandidates"]
    - path: "packages/core/src/decisions/threshold.ts"
      provides: "EVD-03 threshold helpers"
      exports: ["isBelowEvidenceThreshold", "EVD03_GAP_MESSAGE"]
  key_links:
    - from: "packages/core/src/decisions/candidates.ts"
      to: "commits.cc_type / message / doc_snapshots"
      via: "parameterized Drizzle queries"
      pattern: "cc_type|doc_snapshots"
    - from: "packages/core/src/schema/zod/decisions.ts"
      to: "packages/core/src/schema/zod/evidence.ts"
      via: "evidence arrays on DecisionRecord"
      pattern: "Evidence"
---

## Phase Goal

**As a** maintainer or new hire, **I want to** see what decisions were made and what's still in flight with honest confidence when evidence is weak, **so that** I can trust GitChange's answers to the third through fifth core questions.

<objective>
Walking skeleton for Phase 6: decision and open-work artifact contracts, additive `interview` evidence type (P6-D-04), deterministic candidate extraction with PITFALLS #1 noise filters, EVD-03 threshold module, bounded mining context bundler, validated I/O helpers.

Purpose: Locks DEC-01 deterministic layer and EVD-03 before any LLM agent or dashboard work.
Output: Schema files, candidates miner, context bundler, threshold helpers, I/O with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-decisions-status-open-work/06-RESEARCH.md
@.planning/phases/04-era-detection-semantic-pipeline/04-01-SUMMARY.md
@packages/core/src/schema/zod/evidence.ts
@packages/core/src/schema/zod/eras.ts
@packages/core/src/semantic/context.ts
@packages/core/src/semantic/eras-io.ts
@packages/core/src/intelligence/path-filters.ts

<interfaces>
From `packages/core/src/schema/zod/evidence.ts` (extend additively per P6-D-04):
```typescript
export const Evidence = z.discriminatedUnion("type", [
  z.object({ type: z.literal("commit"), sha: z.string().length(40) }),
  z.object({ type: z.literal("file"), path: z.string(), commitSha: z.string().length(40) }),
  z.object({ type: z.literal("doc"), path: z.string(), commitSha: z.string().length(40), excerpt: z.string().max(500) }),
  z.object({ type: z.literal("hunk"), path: z.string(), commitSha: z.string().length(40), startLine: z.number().int().positive(), endLine: z.number().int().positive() }),
  // ADD: interview per P6-D-04
]);
```

From `packages/core/src/schema/drizzle/schema.ts` commits table:
```typescript
ccType: text("cc_type"),
ccScope: text("cc_scope"),
ccBreaking: integer("cc_breaking", { mode: "boolean" }),
```

Pattern from `eras-io.ts`: read → Zod parse; write → Zod parse → tmp file → rename.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decision + open-work schemas and interview evidence type</name>
  <files>packages/core/src/schema/zod/evidence.ts, packages/core/src/schema/zod/evidence.test.ts, packages/core/src/schema/zod/decisions.ts, packages/core/src/schema/zod/decisions.test.ts, packages/core/src/schema/zod/open-work.ts, packages/core/src/schema/zod/open-work.test.ts, packages/core/src/schema/zod/index.ts</files>
  <behavior>
    - Evidence accepts interview type with path, recordedAt (ISO string), excerpt max 500 chars
    - DecisionRecord requires id (decision: prefix), title, summary, status enum (proposed|accepted|rejected|superseded|in_flight|unknown), confidence 0-1, evidence min 1, reviewStatus enum, optional supersededBy and supersedes[]
    - OpenWorkThread requires id (thread: prefix), kind enum (migration|refactor|wip|stale), status enum, events[] with commitSha and paths[], evidence min 1
    - DecisionsArtifact caps decisions array at 40; OpenWorkArtifact caps threads at 20
  </behavior>
  <action>
    Add `interview` variant to Evidence discriminated union per P6-D-04. Create `decisions.ts` with `DECISIONS_SCHEMA_VERSION`, `DecisionStatus`, `DecisionRecord` including optional `attribution` block placeholder for CONT-02 (authorId, name, email, rationale, evidence min 1), `miningSource` enum, and supersession fields. Create `open-work.ts` with `OPEN_WORK_SCHEMA_VERSION` and `OpenWorkThread` including `events[]` for TIME-04 (commitSha, committedAt, summary, paths). Export from `schema/zod/index.ts`. Barrel-export new types from `packages/core/src/index.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/schema/zod/decisions packages/core/src/schema/zod/open-work packages/core/src/schema/zod/evidence</automated>
  </verify>
  <done>Schemas parse valid fixtures; reject empty evidence; interview type round-trips</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic candidates + EVD-03 threshold</name>
  <files>packages/core/src/decisions/candidates.ts, packages/core/src/decisions/candidates.test.ts, packages/core/src/decisions/threshold.ts, packages/core/src/decisions/threshold.test.ts</files>
  <behavior>
    - extractDecisionCandidates skips merge commits, lockfile-only touches, paths matching chore/format noise filters from path-filters.ts
    - Candidates emitted for: cc_breaking true, cc_type feat with migration keywords in message, doc snapshots with ADR/decision frontmatter status fields, WIP/TODO in summary on migration-scoped paths
    - isBelowEvidenceThreshold true when confidence less than 0.35 OR evidence length less than 1
    - EVD03_GAP_MESSAGE exact string: "No recorded decision found"
  </behavior>
  <action>
    Implement `extractDecisionCandidates(gitchangeDir)` reading openDb + doc_snapshots + commits only — no es-git live walk (SCALE-02). Apply PITFALLS #1 noise filters: exclude merge commits, conventional type chore/docs with no structural path change, lockfile-only file_changes. Keyword list for migration/refactor/adr/breaking (case-insensitive) on message and doc frontmatter. Return `{ candidateId, title, seedEvidence[], relatedPaths[], sourceSignals[] }[]` capped at 60 pre-agent. Implement `threshold.ts` with `isBelowEvidenceThreshold(decision)` and exported `EVD03_GAP_MESSAGE` constant per P6-D-03.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/decisions/candidates packages/core/src/decisions/threshold</automated>
  </verify>
  <done>BASIC_SCENARIO yields at least one candidate; noise commit excluded; threshold helper matches EVD-03 rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mining context bundler + artifact I/O</name>
  <files>packages/core/src/decisions/context.ts, packages/core/src/decisions/context.test.ts, packages/core/src/decisions/decisions-io.ts, packages/core/src/decisions/decisions-io.test.ts, packages/core/src/decisions/open-work-io.ts, packages/core/src/decisions/open-work-io.test.ts</files>
  <behavior>
    - buildDecisionMiningContext includes candidates (max 30), erasSummary (max 8), topChurnFiles (max 10), docDeltas (max 5), expertise topics (max 5) — no live git
    - writeDecisionsArtifact validates with Zod before tmp+rename write
    - readDecisionsArtifact returns null when file missing
  </behavior>
  <action>
    Mirror `buildEraSynthesisContext` pattern in `context.ts`: read intelligence.json, eras.json (if present), run extractDecisionCandidates, bound arrays per 06-RESEARCH caps. Implement `readDecisionsArtifact` / `writeDecisionsArtifact` and `readOpenWorkArtifact` / `writeOpenWorkArtifact` following `eras-io.ts` atomic write pattern under `.gitchange/`. Context must include `attributionConfidence` and `manifestWarnings` from manifest for agent caution language.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/decisions/context packages/core/src/decisions/decisions-io packages/core/src/decisions/open-work-io</automated>
  </verify>
  <done>Context bundles from fixture .gitchange; invalid decisions JSON rejected at write boundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite index → candidate miner | Untrusted historical data; miner must not execute message content |
| Artifact write → dashboard read | Tampered JSON must fail Zod at read boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | decisions.json write | mitigate | Zod validate before atomic rename |
| T-06-02 | Spoofing | interview evidence path | mitigate | Resolve path under .gitchange/interviews/ only |
| T-06-03 | Information Disclosure | doc excerpts in context | accept | Same 500-char cap as era synthesis |
| T-06-SC | Tampering | npm installs | accept | No new packages Phase 6 plan 01 |
</threat_model>

<verification>
- All plan 01 unit tests pass
- extractDecisionCandidates runs on BASIC_SCENARIO without network
- EVD03_GAP_MESSAGE exported for downstream API/agent use
</verification>

<success_criteria>
- DecisionsArtifact and OpenWorkArtifact schemas committed with tests
- Deterministic candidate extraction operational on indexed data
- EVD-03 threshold module is single source of truth for gap messaging
</success_criteria>

<output>
Create `.planning/phases/06-decisions-status-open-work/06-01-SUMMARY.md` when done
</output>
