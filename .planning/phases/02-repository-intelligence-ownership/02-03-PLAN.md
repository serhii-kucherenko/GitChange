---
phase: 02-repository-intelligence-ownership
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/core/src/intelligence/cochange.ts
  - packages/core/src/intelligence/era-signals.ts
  - packages/core/src/intelligence/compute.ts
  - packages/core/src/intelligence/export.ts
  - packages/core/src/intelligence/cochange.test.ts
  - packages/core/src/intelligence/era-signals.test.ts
autonomous: true
requirements: [CONT-01]
user_setup: []

must_haves:
  truths:
    - "Co-change pairs are computed from commits that touch multiple human-source files together"
    - "Lockfiles, vendor/, dist/, and generated paths never appear in co_change_edges (P2-D-04)"
    - "Co-change edges include temporal decay weight and disclaimer metadata in export"
    - "Era boundary signals (3–8 windows) are persisted from deterministic heuristics — not LLM names (P2-D-05)"
    - "intelligence.json includes coChange.edges and eraSignals.boundaries for downstream Phase 4"
  artifacts:
    - path: "packages/core/src/intelligence/cochange.ts"
      provides: "Co-change graph computation"
    - path: "packages/core/src/intelligence/era-signals.ts"
      provides: "Pre-LLM era boundary detection"
  key_links:
    - from: "packages/core/src/intelligence/cochange.ts"
      to: "file_changes table"
      via: "per-commit path sets"
      pattern: "commitSha"
    - from: "packages/core/src/intelligence/era-signals.ts"
      to: "era_boundaries table"
      via: "heuristic segmentation"
      pattern: "era_boundaries"
---

## Phase Goal

**As a** maintainer or new team member, **I want to** see who changed what through ownership timelines and contributor expertise derived from git history, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Compute **co-change graph** and **era boundary signals** from the indexed timeline — no live git. Feeds Phase 4 era synthesis and success criterion #4 (intelligence artifacts for downstream agents).

Purpose: Pitfall 6 mitigation (co-change false signals); provides era windows for CONT-01 timelines in Plan 02-04.
Output: `cochange.ts`, `era-signals.ts`, populated `co_change_edges` and `era_boundaries` tables + export sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repository-intelligence-ownership/02-RESEARCH.md
@.planning/phases/02-repository-intelligence-ownership/02-01-SUMMARY.md
@packages/core/src/intelligence/path-filters.ts

<interfaces>
```typescript
// path-filters from 02-01
export function isIntelligenceIgnoredPath(path: string): boolean;

// era_boundaries table (from 02-01 migration)
// id, start_commit_sha, end_commit_sha, start_at, end_at, signal_type, score, evidence_json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing co-change tests — lockfile exclusion + pair detection</name>
  <files>packages/core/src/intelligence/cochange.test.ts</files>
  <behavior>
    - Fixture index where one commit touches src/a.ts and src/b.ts → edge (a,b) with co_occurrence >= 1
    - Commit touching package-lock.json + src/a.ts → no edge involving package-lock.json
    - Edge weight decreases for older co-changes (decay half-life 180 days default)
  </behavior>
  <action>
    Use BASIC_SCENARIO or minimal inline fixture via builder. Test queries co_change_edges after computeCoChange helper (stub until Task 2). Must fail initially.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/cochange.test.ts 2>&1 | grep -q "FAIL\|AssertionError" || test ! -f packages/core/src/intelligence/cochange.ts</automated>
  </verify>
  <done>Co-change tests exist and fail until implementation</done>
</task>

<task type="auto">
  <name>Task 2: Co-change computation with filters and decay</name>
  <files>packages/core/src/intelligence/cochange.ts</files>
  <action>
    `computeCoChange(db, opts?)`: For each commit, collect distinct paths from file_changes excluding isIntelligenceIgnoredPath and content_ignored. For each unordered pair (p1,p2) in commit, upsert co_change_edges with increment count and last_co_change_at = commit.committed_at. Apply exponential decay weight: `weight = count * exp(-ageDays / halfLife)` with halfLife default 180.

    Store canonical pair ordering (lexicographic path_a < path_b) to dedupe. Clear table on full recompute.

    Export helper `getCoChangeEdges(db, limit?)` for export layer.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/cochange.test.ts</automated>
  </verify>
  <done>Co-change tests pass; lockfiles excluded</done>
</task>

<task type="auto">
  <name>Task 3: Era boundary signals + wire into compute/export</name>
  <files>packages/core/src/intelligence/era-signals.ts, packages/core/src/intelligence/era-signals.test.ts, packages/core/src/intelligence/compute.ts, packages/core/src/intelligence/export.ts</files>
  <action>
    `computeEraSignals(db)`: Walk commits chronologically (oldest→newest). Heuristics (combine scores):
    1) **author_spike**: rolling 30-commit window unique author count delta > threshold
    2) **path_churn_pivot**: top-10 path churn set Jaccard distance > 0.5 between adjacent windows
    3) **cc_scope_shift**: new conventional scope appears in >20% of commits in window

    Emit 3–8 boundaries max; each row in era_boundaries with signal_type, score, start/end commit SHAs and timestamps, evidence_json with commit refs for boundary commit.

    Wire into computeIntelligence after churn (parallel-safe with ownership plan — no file conflict). Export `eraSignals.boundaries[]` and `coChange.edges[]` with `relationship: "co_change"` and `disclaimer: "historical correlation, not import dependency"` on each edge.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/era-signals.test.ts packages/core/src/intelligence/cochange.test.ts</automated>
  </verify>
  <done>Era signals and co-change appear in intelligence.json; BASIC_SCENARIO produces >= 1 era boundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file_changes → co-change | Derived metrics only; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | co-change SQL | mitigate | Parameterized Drizzle queries |
| T-02-06 | Spoofing | era signal evidence | mitigate | evidence[] must reference commits in index |
</threat_model>

<verification>
- Co-change excludes lockfiles (automated test)
- Era boundaries have resolvable commit SHAs in evidence
</verification>

<success_criteria>
- intelligence.json coChange and eraSignals sections populated after computeIntelligence
- Downstream Phase 4 can consume eraSignals without live git
</success_criteria>

<output>
Create `.planning/phases/02-repository-intelligence-ownership/02-03-SUMMARY.md` when done
</output>
