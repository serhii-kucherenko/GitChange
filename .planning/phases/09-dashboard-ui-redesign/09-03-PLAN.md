---
phase: 09-dashboard-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/dashboard/src/components/CommitList.tsx
  - packages/dashboard/src/components/DecisionsPanel.tsx
  - packages/dashboard/src/components/MigrationThreadPanel.tsx
  - packages/dashboard/src/components/FileHistoryList.tsx
autonomous: true
requirements: [DASH-03, DASH-04]
must_haves:
  truths:
    - "Each virtualized list shows its full header/toolbar and first row without clipping — the header sits outside the scroll region and nothing is cut at the top edge"
    - "List viewports fill their pane via flex (min-h + flex-1) instead of a fixed h-[min(70vh,40rem)] / max-h-[28rem] cap"
    - "Virtualization still works: rows render, scroll is smooth, no overlapping/gapped rows — ROW_HEIGHT matches the rendered row height after any padding change"
    - "No data-fetch, pagination, or store logic changed; typecheck passes and existing tests stay green"
  artifacts:
    - path: "packages/dashboard/src/components/CommitList.tsx"
      provides: "Flex-fill virtualized commit list; header outside scroll; ROW_HEIGHT audited"
      contains: "flex-1"
    - path: "packages/dashboard/src/components/DecisionsPanel.tsx"
      provides: "Flex-fill decisions list replacing max-h-[28rem]; ROW_HEIGHT audited"
      contains: "overflow-y-auto"
  key_links:
    - from: "list scroll container (parentRef)"
      to: "@tanstack/react-virtual useVirtualizer"
      via: "ref={parentRef} on the single overflow-auto scroll element; estimateSize === ROW_HEIGHT"
      pattern: "estimateSize"
---

<objective>
Fix the clipped-header / cramped-list defect on the four virtualized lists: keep each header/toolbar OUTSIDE the scroll region (already true for most), replace fixed heights (`h-[min(70vh,40rem)]`, `max-h-[28rem]`) with flex-fill (`min-h-[24rem] flex-1 overflow-auto`) so the list grows to fill its pane, add a clear scroll boundary divider, and audit each `ROW_HEIGHT`/`estimateSize` against the rendered row height. Container-class + row-height-constant changes only — no behavior change.

Purpose: Satisfies DASH-04 (large lists stay virtualized and responsive) and DASH-03 presentation (full headers/rows visible, no clipping). The panes now grow with the per-view layouts from Plan 01.

Output: Restyled scroll frames on `CommitList`, `DecisionsPanel`, `MigrationThreadPanel`, `FileHistoryList` with audited row heights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-dashboard-ui-redesign/09-UI-SPEC.md
@.planning/phases/09-dashboard-ui-redesign/09-RESEARCH.md

<interfaces>
<!-- Load-bearing virtualizer constants (grep-verified). If a row's rendered height changes, the matching constant MUST change in the same edit. -->
CommitList.tsx           ROW_HEIGHT = 44   (section: flex h-[min(70vh,40rem)] flex-col … , line 186)
DecisionsPanel.tsx       ROW_HEIGHT = 56   (scroll div: max-h-[28rem] overflow-y-auto, line 357)
MigrationThreadPanel.tsx ROW_HEIGHT = 64   (section: flex h-[min(70vh,40rem)] flex-col … , line 116)
FileHistoryList.tsx      ROW_HEIGHT = 52
Each uses estimateSize: () => ROW_HEIGHT and absolutely-positioned rows via translateY — that positioning stays UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flex-fill CommitList and MigrationThreadPanel scroll frames</name>
  <files>packages/dashboard/src/components/CommitList.tsx, packages/dashboard/src/components/MigrationThreadPanel.tsx</files>
  <read_first>
    - packages/dashboard/src/components/CommitList.tsx (section line 186 `h-[min(70vh,40rem)]`, header lines 185-228, scroll div `flex-1 overflow-auto`, ROW_HEIGHT line 19, estimateSize line 127, row height line 223) — file being modified
    - packages/dashboard/src/components/MigrationThreadPanel.tsx (section line 116 `h-[min(70vh,40rem)]`, ROW_HEIGHT line 9, estimateSize line 86) — file being modified
    - 09-UI-SPEC.md §Component Treatment Contract → "Scroll containers" (header outside scroll, explicit min height that grows via flex, persistent divider)
    - 09-RESEARCH.md Pitfall 1 (ROW_HEIGHT must move with rendered row height; keep ref={parentRef} on the single overflow-auto element)
  </read_first>
  <action>
    On both sections, replace the fixed `h-[min(70vh,40rem)]` with flex-fill: section stays `flex flex-col overflow-hidden rounded-lg border border-slate-700 bg-slate-900`; the scroll region (the `ref={parentRef}` div) becomes `min-h-[24rem] flex-1 overflow-auto`. Keep the header/toolbar OUTSIDE the scroll region with a persistent bottom divider `border-b border-slate-800 px-4 py-3` so the boundary reads clearly; header title at Heading size (`text-lg font-semibold tracking-tight text-slate-100`), meta counts at `text-xs text-slate-400` (raise from `slate-500`). Do NOT change the absolutely-positioned row rendering (translateY), the pagination/"Load more" logic, or the filters prop. If you adjust a row's vertical padding, update `ROW_HEIGHT` (and the inline `style={{ height: ROW_HEIGHT }}`) to match in the same edit; otherwise leave ROW_HEIGHT at 44 (CommitList) / 64 (MigrationThreadPanel). Keep the "Load more commits" CTA copy per §Copywriting.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes.
    - `pnpm --dir packages/dashboard test` stays green.
    - Neither section retains `h-[min(70vh,40rem)]`; the `ref={parentRef}` scroll div carries `min-h-[24rem] flex-1 overflow-auto`.
    - Header sits outside the scroll region with a `border-b` divider; meta text uses `slate-400` (not `slate-500`).
    - `ROW_HEIGHT`, `estimateSize: () => ROW_HEIGHT`, and the row `style={{ height: ROW_HEIGHT }}` remain mutually consistent (constant equals rendered height).
    - No change to pagination logic, `filters` prop handling, or the translateY row positioning.
  </acceptance_criteria>
  <done>CommitList and MigrationThreadPanel grow to fill their pane with headers outside the scroll and consistent row heights; typecheck + tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flex-fill DecisionsPanel and FileHistoryList; audit row heights</name>
  <files>packages/dashboard/src/components/DecisionsPanel.tsx, packages/dashboard/src/components/FileHistoryList.tsx</files>
  <read_first>
    - packages/dashboard/src/components/DecisionsPanel.tsx (scroll div line 357 `max-h-[28rem] overflow-y-auto`, ROW_HEIGHT line 24, estimateSize line 304, row minHeight line 81) — file being modified
    - packages/dashboard/src/components/FileHistoryList.tsx (ROW_HEIGHT line 8, estimateSize line 96, row height lines 34 & 174) — file being modified
    - 09-UI-SPEC.md §Component Treatment Contract → "Scroll containers"; §Copywriting empty-state "No recorded decisions found for this repo."
    - 09-RESEARCH.md Pitfall 1 (DecisionsPanel is where clipping reads worst — fixed max-height, not flex-fill)
  </read_first>
  <action>
    In `DecisionsPanel`, replace the `max-h-[28rem]` fixed cap on the `ref={parentRef}` scroll div with `min-h-[24rem] flex-1 overflow-y-auto`, and ensure the enclosing container is a `flex flex-col` so the header/toolbar stays outside the scroll region with a `border-b border-slate-800` divider. Apply the no-decisions empty-state copy verbatim: "No recorded decisions found for this repo." (honest-gap per EVD-03 — never fabricate). In `FileHistoryList`, give its scroll viewport the same `min-h-[24rem] flex-1 overflow-auto` treatment with the header outside the scroll region. In BOTH files, keep the translateY row positioning and the virtualizer wiring unchanged; if a row's rendered height changes, update `ROW_HEIGHT` (DecisionsPanel 56, FileHistoryList 52) and its `estimateSize`/inline `style` together — otherwise leave them. Raise any `slate-500` metadata to `slate-400`.
  </action>
  <verify>
    <automated>pnpm --dir packages/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir packages/dashboard typecheck` passes.
    - `pnpm --dir packages/dashboard test` stays green.
    - `DecisionsPanel` no longer uses `max-h-[28rem]`; its scroll div is `min-h-[24rem] flex-1 overflow-y-auto` inside a `flex flex-col` container with the header outside the scroll region; no-decisions copy is "No recorded decisions found for this repo."
    - `FileHistoryList` scroll viewport is `min-h-[24rem] flex-1 overflow-auto` with header outside scroll.
    - `ROW_HEIGHT`/`estimateSize`/row-height style stay mutually consistent in both files.
    - No change to decision/file-history data fetching, selection, or the translateY row positioning; metadata uses `slate-400`.
  </acceptance_criteria>
  <done>DecisionsPanel and FileHistoryList fill their pane with no clipped first row, honest empty copy, and consistent row heights; typecheck + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → local /api | Existing read-only paginated fetches; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Virtualized list restyle | accept | No new threat surface — presentational restyle of read-only lists over pre-built artifacts (SCALE-02). No new inputs, network calls, or writes; pagination/fetch/virtualizer logic preserved. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (no new dependencies permitted); Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `pnpm --dir packages/dashboard typecheck` green; `pnpm --dir packages/dashboard test` green.
- Manual (per 09-VALIDATION.md): commit list fills width, full header + first row visible, virtualization scrolls smoothly; Decisions/Open work no clipped rows.
</verification>

<success_criteria>
- All four lists use header-outside-scroll + `min-h-[24rem] flex-1` flex-fill; no fixed `h-[min(70vh,40rem)]`/`max-h-[28rem]` remains.
- ROW_HEIGHT constants audited and consistent with rendered heights.
- No fetch/pagination/store change; typecheck + existing tests green.
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-ui-redesign/09-03-SUMMARY.md` when done.
</output>
