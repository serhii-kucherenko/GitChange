---
phase: 03-cli-plugin-scaffold
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - packages/plugin/scripts/resolve-root.ts
  - packages/plugin/scripts/resolve-root.test.ts
  - packages/cli/src/repo-path.ts
  - scripts/install.sh
  - scripts/install.ps1
  - .github/workflows/validate-plugin.yml
  - packages/plugin/skills/gitchange/SKILL.md
  - packages/plugin/skills/gitchange-dashboard/SKILL.md
  - README.md
autonomous: true
requirements: [PLUG-04, INST-01]
user_setup: []

must_haves:
  truths:
    - "User can install via one-line shell script following UA pattern"
    - "Plugin resolves gitchange root from cloned repo, global install, and monorepo dev layouts"
    - "Marketplace manifest enables Cursor/Claude Code plugin discovery"
  artifacts:
    - path: "scripts/install.sh"
      provides: "One-line installer"
      min_lines: 20
    - path: "packages/plugin/scripts/resolve-root.ts"
      provides: "Plugin root resolver"
      exports: ["resolveGitChangeRoot", "resolveCliBin"]
    - path: ".claude-plugin/marketplace.json"
      provides: "Claude marketplace entry"
  key_links:
    - from: "packages/plugin/skills/gitchange/SKILL.md"
      to: "resolve-root.ts"
      via: "documented resolution order P3-D-04"
      pattern: "resolveGitChangeRoot|GITCHANGE_ROOT"
    - from: "scripts/install.sh"
      to: "pnpm build"
      via: "build CLI after clone"
      pattern: "pnpm.*build"
---

<objective>
Deliver install UX and runtime path resolution so plugin works from marketplace clone, global install, and monorepo dev — mitigating Pitfall 12.

Purpose: INST-01 install path; unblocks real-world slash command usage outside dev monorepo layout.
Output: install.sh, resolve-root helper, README install section, resolver unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-plugin-scaffold/03-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/phases/03-cli-plugin-scaffold/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plugin root resolver with unit tests</name>
  <files>packages/plugin/scripts/resolve-root.ts, packages/plugin/scripts/resolve-root.test.ts, packages/cli/src/repo-path.ts</files>
  <behavior>
    - resolveGitChangeRoot from monorepo packages/plugin cwd → finds repo root containing .cursor-plugin
    - With GITCHANGE_ROOT env set → returns that path
    - resolveCliBin returns path to gitchange executable (node dist/bin.js or pnpm exec fallback)
    - From random /tmp dir without env → throws clear ResolveError
  </behavior>
  <action>
    Implement P3-D-04 precedence in resolve-root.ts:
    1. process.env.GITCHANGE_ROOT if set and valid
    2. Walk up from cwd for directory containing `.cursor-plugin/plugin.json`
    3. Walk up for `node_modules/.bin/gitchange` (global install)
    4. Monorepo dev: from import.meta.url walk to packages/cli/dist/bin.js

    Export typed errors. Unit test each precedence case with temp dirs. Align packages/cli/src/repo-path.ts repo detection with same walk-up-for-.git logic (shared util or duplicate with test parity).

    Update SKILL.md files to reference resolver order explicitly.
  </action>
  <verify>
    <automated>pnpm vitest run packages/plugin/scripts/resolve-root.test.ts</automated>
  </verify>
  <done>Resolver tests pass for monorepo + env + failure cases</done>
</task>

<task type="auto">
  <name>Task 2: Install scripts + README + marketplace polish</name>
  <files>scripts/install.sh, scripts/install.ps1, README.md, .github/workflows/validate-plugin.yml, .claude-plugin/marketplace.json, .cursor-plugin/plugin.json</files>
  <action>
    **install.sh** (UA-style one-liner target):
    - Require Node 22+, pnpm, git on PATH
    - Clone or update GitChange to configurable directory (default ~/.gitchange-plugin)
    - Run pnpm install && pnpm build
    - Print symlink instructions for Cursor (.cursor-plugin) and Claude (.claude-plugin) — or copy skills to ~/.cursor/skills per UA pattern
    - Idempotent on re-run

    **install.ps1**: Windows equivalent with same steps.

    Update README.md from stub: prerequisites, install.sh one-liner, verify with `gitchange --version` and `gitchange status`.

    validate-plugin.yml CI job: JSON parse all plugin manifests; run resolve-root tests; verify skills directory referenced in plugin.json exists.

    Polish marketplace.json: description, keywords, repository URL for GitChange repo (use placeholder github.com if not published yet — document in README).
  </action>
  <verify>
    <automated>bash -n scripts/install.sh && pnpm vitest run packages/plugin/scripts/resolve-root.test.ts && node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json'))"</automated>
  </verify>
  <done>Install script syntax-valid; marketplace JSON valid; README has install section</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh → network | Clones from configured git URL only |
| User → curl\|bash | Supply-chain trust on install script source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | install.sh remote clone | mitigate | Pin to known GitHub repo URL; no arbitrary URL param without warning |
| T-03-11 | Spoofing | Typosquat clone URL | mitigate | Document official repo in README and script comments |
| T-03-12 | Repudiation | install failure | accept | Script prints clear error codes; no silent partial install |
</threat_model>

<verification>
- resolve-root unit tests pass
- install.sh passes bash -n
- Plugin manifest CI validation job defined
</verification>

<success_criteria>
- PLUG-04: UA packaging pattern with multi-platform manifests
- INST-01: one-line installer exists and documented
</success_criteria>

<output>
Create `.planning/phases/03-cli-plugin-scaffold/03-05-SUMMARY.md` when done
</output>
