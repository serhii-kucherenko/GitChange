---
phase: 02-repository-intelligence-ownership
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/core/package.json
  - pnpm-lock.yaml
  - packages/core/src/intelligence/ownership/blame.ts
  - packages/core/src/intelligence/ownership/aggregate.ts
  - packages/core/src/intelligence/ownership/ignore-revs.ts
  - packages/core/src/intelligence/ownership/index.ts
  - packages/core/src/intelligence/compute.ts
  - packages/core/src/intelligence/export.ts
  - packages/core/src/intelligence/ownership/blame.test.ts
  - tests/fixtures/scenarios.ts
  - tests/fixtures/ownership-builder.ts
autonomous: false
requirements: [CONT-04]
user_setup: []

must_haves:
  truths:
    - "Per-file ownership at HEAD is computed from line survival, not last-committer heuristics (P2-D-01)"
    - "Renamed files attribute lines to pre-rename author via blame move/copy tracking (CONT-04)"
    - "When .git-blame-ignore-revs lists a formatting commit, that commit does not receive line credit (P2-D-02)"
    - "Merge commits are skipped when mapping blame hunks to author attribution (P2-D-03)"
    - "file_ownership rows persist line_count and percentage per (path, author_id)"
  artifacts:
    - path: "packages/core/src/intelligence/ownership/blame.ts"
      provides: "HEAD blame with es-git + simple-git fallback"
    - path: "packages/core/src/intelligence/ownership/aggregate.ts"
      provides: "Line counts → ownership percentages"
    - path: "packages/core/src/intelligence/ownership/ignore-revs.ts"
      provides: ".git-blame-ignore-revs loader"
  key_links:
    - from: "packages/core/src/intelligence/compute.ts"
      to: "packages/core/src/intelligence/ownership"
      via: "blame pass after churn"
      pattern: "computeOwnership|blameAtHead"
    - from: "packages/core/src/intelligence/export.ts"
      to: "file_ownership table"
      via: "ownership section in intelligence.json"
      pattern: "ownership"
---

## Phase Goal

**As a** maintainer or new team member, **I want to** see who changed what through ownership timelines and contributor expertise derived from git history, **so that** I can onboard without hunting git log or asking seniors.

<objective>
Implement **line-survival ownership at HEAD** (CONT-04): blame each indexed text file at HEAD, aggregate lines by author, respect renames and ignore-revs, persist `file_ownership`, and export per-file ownership in `intelligence.json`.

Purpose: Fixes Pitfall 3 (ownership conflation). Core answer to "who owns this file right now?"
Output: `intelligence/ownership/*`, OWNSHIP_SCENARIO fixture, ownership section in intelligence.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repository-intelligence-ownership/02-RESEARCH.md
@.planning/phases/02-repository-intelligence-ownership/02-01-SUMMARY.md

<interfaces>
es-git (Context7 verified):
```typescript
repo.blameFile(path, {
  trackLinesMovement: true,
  trackCopiesSameCommitMoves: true,
  trackCopiesAnyCommitCopies: true,
}): Blame;
blame.getHunkByLine(line: number): BlameHunk; // origSignature, origCommitId, finalCommitId
```

From Phase 1 `openRepo` / indexed paths: query distinct paths from file_changes where is_binary=false and content_ignored=false.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify simple-git@3.36.0 package legitimacy</name>
  <files>packages/core/package.json</files>
  <action>
    Human verifies simple-git@3.36.0 on npm (publisher steveukx/simple-git) before Task 2 runs `pnpm add simple-git@3.36.0 -F @gitchange/core`. Executor pauses until resume signal received.
  </action>
  <what-built>Plan requires adding simple-git for git blame --ignore-revs-file fallback per P2-D-02 and STACK.md</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/simple-git
    2. Confirm publisher is steveukx/simple-git, millions of weekly downloads
    3. Confirm version 3.36.0 matches STACK.md
    4. Reply "approved" to proceed with pnpm add
  </how-to-verify>
  <verify>
    <automated>test -f packages/core/package.json</automated>
  </verify>
  <done>Human replied "approved"; executor may install simple-git in Task 2</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: OWNSHIP_SCENARIO fixture + failing blame ownership tests</name>
  <files>tests/fixtures/scenarios.ts, tests/fixtures/ownership-builder.ts, packages/core/src/intelligence/ownership/blame.test.ts</files>
  <behavior>
    - OWNSHIP_SCENARIO: initial author writes src/app.ts; file renamed src/app.ts → src/lib/app.ts; second author edits one line; optional .git-blame-ignore-revs entry for a formatting-only commit
    - After indexFull + computeIntelligence: src/lib/app.ts top owner by line % is original author (not solely last editor)
    - With ignore-revs: formatting commit SHA absent from ownership credit
    - Merge-only attribution commits do not receive ownership credit (P2-D-03)
  </behavior>
  <action>
    Add OWNSHIP_SCENARIO CommitSpec[] to scenarios.ts (3–6 commits). Export helper buildOwnershipRepo() if needed. Write blame.test.ts that indexes repo, runs computeIntelligence, queries file_ownership or intelligence.json ownership section. Tests fail until blame implemented.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/ownership/blame.test.ts 2>&1 | grep -q "FAIL\|AssertionError" || test ! -f packages/core/src/intelligence/ownership/blame.ts</automated>
  </verify>
  <done>Fixture builds; tests fail until blame module exists</done>
</task>

<task type="auto">
  <name>Task 2: Blame module — es-git hot path + simple-git ignore-revs fallback</name>
  <files>packages/core/package.json, pnpm-lock.yaml, packages/core/src/intelligence/ownership/ignore-revs.ts, packages/core/src/intelligence/ownership/blame.ts, packages/core/src/intelligence/ownership/aggregate.ts, packages/core/src/intelligence/ownership/index.ts</files>
  <action>
    After checkpoint approved: `pnpm add simple-git@3.36.0 -F @gitchange/core`.

    `ignore-revs.ts`: load `.git-blame-ignore-revs` from repo root; parse non-comment lines as SHAs; return Set.

    `blame.ts`: `blameFileAtHead(repo, path, opts)` — if ignoreRevs non-empty, use simple-git `raw(['blame', '--ignore-revs-file', '.git-blame-ignore-revs', 'HEAD', '--', path])` and parse porcelain into per-line author email + origCommit; else es-git blameFile with P2-D-01 tracking flags. Skip binary paths. Map email → author_id via authors table (create missing authors only if needed for blame signatures not in index — prefer index authors).

    `aggregate.ts`: count lines per author_id; compute percentage; skip hunks where finalCommitId is merge commit (lookup commits.is_merge). Write file_ownership rows (delete prior rows for repo recompute).

    Do NOT persist raw line content from blame — author aggregation only (security).
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/ownership</automated>
  </verify>
  <done>Blame tests pass including rename and ignore-revs cases</done>
</task>

<task type="auto">
  <name>Task 3: Wire ownership into computeIntelligence + export</name>
  <files>packages/core/src/intelligence/compute.ts, packages/core/src/intelligence/export.ts</files>
  <action>
    Extend computeIntelligence: after churn, enumerate blameable paths from file_changes (distinct path, not ignored, not binary, not INTELLIGENCE_IGNORE_GLOBS); run blame+aggregate; refresh file_ownership table.

    Extend export: intelligence.json `ownership.files[]` with path, authors[{ authorId, name, email, lineCount, percentage }], evidence[{ type:'file', path, commitSha: headSha }].

    Update compute.test.ts to assert ownership section non-empty for BASIC_SCENARIO src paths.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/intelligence/compute.test.ts packages/core/src/intelligence/ownership</automated>
  </verify>
  <done>Full pipeline exports per-file ownership at HEAD with evidence links</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live .git blame | Reads HEAD only; never called from future dashboard — batch compute only |
| simple-git child process | Invokes local git binary; repoPath must be trusted local clone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | blame line text | mitigate | Aggregate counts only; never store blamed lines |
| T-02-04 | Tampering | simple-git args | mitigate | execFile with fixed argv; path from index only |
| T-02-SC | Tampering | simple-git npm install | mitigate | Blocking human-verify checkpoint before install |
</threat_model>

<verification>
- CONT-04: rename + ignore-revs tests green
- No blame line content in SQLite text columns (grep test optional in 02-05)
</verification>

<success_criteria>
- intelligence.json ownership reflects line survival at HEAD
- OWNSHIP_SCENARIO golden assertions pass
</success_criteria>

<output>
Create `.planning/phases/02-repository-intelligence-ownership/02-02-SUMMARY.md` when done
</output>
