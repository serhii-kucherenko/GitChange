---
phase: 05-dashboard-evidence-drill-down
plan: 05
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - packages/core/src/read/files.ts
  - packages/core/src/read/files.test.ts
  - packages/core/src/index.ts
  - packages/server/src/routes/files.ts
  - packages/server/src/app.ts
  - packages/dashboard/src/components/FileScrubber.tsx
  - packages/dashboard/src/components/FileHistoryList.tsx
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/store/drill-down.ts
  - packages/dashboard/src/types.ts
  - tests/integration/dashboard-file-scrubber.test.ts
autonomous: true
requirements: [TIME-03, DASH-04]
user_setup: []

must_haves:
  truths:
    - "User enters or picks a file path and sees chronological history of indexed touches"
    - "Each history event links to commit drill-down (sha, date, change type)"
    - "File history list is virtualized for files with many touches"
  artifacts:
    - path: "packages/core/src/read/files.ts"
      provides: "getFileHistory from file_changes"
      exports: ["getFileHistory", "FileHistoryEvent"]
    - path: "packages/dashboard/src/components/FileScrubber.tsx"
      provides: "Path input + history scrubber"
  key_links:
    - from: "FileScrubber"
      to: "/api/files/:path/history"
      via: "encoded path param"
      pattern: "files/.*/history"
    - from: "FileHistoryList row click"
      to: "drill-down store"
      via: "setSelectedCommitSha + setSelectedFilePath"
      pattern: "selectedCommitSha"
---

## Phase Goal

**As a** new team member or maintainer, **I want to** explore project history interactively on localhost with era-aware drill-down to commits, files, and diff hunks, **so that** I can verify every narrative claim against indexed evidence without hunting git log.

<objective>
File-centric history scrubber (TIME-03): query file_changes by path, virtualized timeline of touches, link into commit/file drill-down.

Purpose: Answers "what happened to this file?" without git log — core onboarding question.
Output: getFileHistory read module, GET /api/files/:path/history, FileScrubber UI, integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-04-SUMMARY.md
@packages/core/src/schema/drizzle/schema.ts
@packages/dashboard/src/components/RepoSnapshot.tsx

<interfaces>
file_changes: commitSha, path, oldPath, changeType, committedAt via join commits.
getCommitDetail from 05-04 for drill-down on row click.
topChurnFiles in snapshot highlights — use as quick-pick suggestions in scrubber.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getFileHistory + API route</name>
  <files>packages/core/src/read/files.ts, packages/core/src/read/files.test.ts, packages/core/src/index.ts, packages/server/src/routes/files.ts, packages/server/src/app.ts, tests/integration/dashboard-file-scrubber.test.ts</files>
  <behavior>
    - getFileHistory returns events ordered by committedAt asc (scrubber chronological)
    - Matches path OR oldPath for rename events
    - Paginated with limit/cursor like commits
    - GET /api/files/:path/history URL-decodes path; 404 empty array for unknown path OK
    - Integration: BASIC_SCENARIO known path returns >= 1 event
  </behavior>
  <action>
    Create read/files.ts with getFileHistory using Drizzle join file_changes + commits + authors. Handle rename: include rows where oldPath matches. Add createFilesRoutes with GET /:path/history (use wildcard or encoded path segment). Wire in app.ts. Unit + integration tests. No es-git in read module.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/src/read/files.test.ts tests/integration/dashboard-file-scrubber.test.ts</automated>
  </verify>
  <done>File history API returns indexed touch events for any path</done>
</task>

<task type="auto">
  <name>Task 2: FileScrubber + virtualized FileHistoryList</name>
  <files>packages/dashboard/src/components/FileScrubber.tsx, packages/dashboard/src/components/FileHistoryList.tsx, packages/dashboard/src/App.tsx, packages/dashboard/src/store/drill-down.ts, packages/dashboard/src/types.ts</files>
  <action>
    FileScrubber panel in App sidebar or below timeline: text input for path with autocomplete from topChurnFiles highlights (datalist). On submit, fetch /api/files/{encodeURIComponent(path)}/history. FileHistoryList virtualized with react-virtual showing date, changeType, commit sha short, author. Row click sets selectedCommitSha + selectedFilePath and opens CommitDetailPanel/DiffHunkView from 05-04. Show empty state for unindexed path. Optional: click churn file in RepoSnapshot pre-fills scrubber path.
  </action>
  <verify>
    <automated>pnpm exec biome check packages/dashboard/src/components/FileScrubber.tsx && pnpm vitest run tests/integration/dashboard-file-scrubber.test.ts</automated>
  </verify>
  <done>User scrubs file history and jumps to commit/file hunk evidence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Path param injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | path traversal in URL | mitigate | Reject paths containing ..; decodeURIComponent once; parameterized SQL |
</threat_model>

<verification>
- File scrubber integration test passes
- Manual: scrub README path shows commit touches
</verification>

<success_criteria>
- TIME-03 file-centric scrubber operational
- DASH-04 virtualization on file history list
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-05-SUMMARY.md` when done
</output>
