---
phase: 03-cli-plugin-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/tsconfig.build.json
  - packages/cli/src/bin.ts
  - packages/cli/src/commands/index.ts
  - packages/cli/src/repo-path.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/dashboard/package.json
  - packages/dashboard/tsconfig.json
  - packages/plugin/package.json
  - packages/core/src/read/snapshot.ts
  - packages/core/src/index.ts
  - tests/integration/cli-index.test.ts
autonomous: false
requirements: [PLUG-03]
user_setup: []

must_haves:
  truths:
    - "User runs `gitchange index` on a local repo and produces `.gitchange/` with manifest.json and intelligence.json"
    - "CLI auto-selects indexFull vs indexIncremental based on existing manifest"
    - "CLI always runs computeIntelligence after successful index (P3-D-02)"
    - "Four new packages exist in monorepo: cli, server, dashboard, plugin"
  artifacts:
    - path: "packages/cli/src/bin.ts"
      provides: "gitchange CLI entry with commander"
      exports: ["program"]
    - path: "packages/cli/src/commands/index.ts"
      provides: "index subcommand implementation"
    - path: "packages/core/src/read/snapshot.ts"
      provides: "Read-only helpers for status/snapshot (used by later plans)"
    - path: "tests/integration/cli-index.test.ts"
      provides: "E2E CLI index test on BASIC_SCENARIO"
  key_links:
    - from: "packages/cli/src/commands/index.ts"
      to: "@gitchange/core"
      via: "indexFull/indexIncremental + computeIntelligence"
      pattern: "indexFull|computeIntelligence"
    - from: "packages/cli/package.json"
      to: "packages/cli/src/bin.ts"
      via: "bin field"
      pattern: "\"bin\""
---

## Phase Goal

**As a** contributor using Cursor, **I want to** install GitChange, complete a first analysis, and open a minimal dashboard in a few steps, **so that** I can understand project history without hunting git log or asking seniors.

<objective>
Scaffold `@gitchange/cli`, `@gitchange/server`, `@gitchange/dashboard`, and `@gitchange/plugin` package shells; deliver the walking skeleton `gitchange index` command that runs the full Phase 1+2 pipeline into `.gitchange/`.

Purpose: Proves terminal indexing works before serve/dashboard/plugin layers. Unblocks all Phase 3 parallel work.
Output: Monorepo package shells, working `gitchange index`, core read helpers, integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cli-plugin-scaffold/03-RESEARCH.md
@.planning/phases/02-repository-intelligence-ownership/02-05-SUMMARY.md
@.planning/phases/01-index-foundation/SKELETON.md

<interfaces>
From `packages/core/src/index.ts`:
```typescript
export function indexFull(opts: IndexOptions): Promise<IndexResult>;
export function indexIncremental(opts: IndexOptions): Promise<IndexResult>;
export function computeIntelligence(repoPath: string, gitchangeDir?: string): Promise<ComputeIntelligenceResult>;
export interface IndexOptions { repoPath: string; gitchangeDir?: string; rebuildIntelligence?: boolean; }
```

From `packages/core/src/schema/manifest.ts`:
```typescript
export function readManifest(gitchangeDir: string): Manifest | null;
export const ManifestSchema: z.ZodObject<...>;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify new npm packages before install</name>
  <files>packages/cli/package.json</files>
  <action>
    Human verifies commander@15.0.0 on npm (publisher tj/commander.js) before Task 2 runs `pnpm add commander@15.0.0 -F @gitchange/cli`. Executor pauses until resume signal received.
  </action>
  <what-built>Package legitimacy audit from 03-RESEARCH.md — commander@15.0.0 for @gitchange/cli</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/commander and confirm publisher is tj/commander.js and version 15.x exists
    2. Confirm no typosquat packages selected (commander not commandor)
    3. Reply "approved" to proceed with pnpm add
  </how-to-verify>
  <verify>
    <automated>test -f packages/cli/package.json</automated>
  </verify>
  <done>Human replied "approved"; executor may install commander in Task 2</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Failing E2E — gitchange index on fixture repo</name>
  <files>tests/integration/cli-index.test.ts, packages/cli/package.json, packages/cli/src/bin.ts, packages/cli/src/commands/index.ts, packages/cli/src/repo-path.ts</files>
  <behavior>
    - Build BASIC_SCENARIO repo via tests/fixtures/builder
    - Spawn `pnpm exec gitchange index --repo <tmpdir>` (or tsx bin directly before build)
    - Expect exit code 0
    - Expect `.gitchange/manifest.json` exists with lastIndexedCommit === repo headSha
    - Expect `.gitchange/intelligence.json` exists with churn.files.length >= 1
  </behavior>
  <action>
    Create `@gitchange/cli` package with commander program stub and `index` subcommand placeholder. Add `repo-path.ts` with `resolveRepoPath(cwd)` — walk up from cwd until `.git` directory found; error with clear message if not in a git repo (P3-D-04 partial). Default `gitchangeDir` to `join(repoPath, '.gitchange')`. Wire root/workspace `bin` via package.json `"bin": { "gitchange": "./dist/bin.js" }` and dev path via tsx for tests.

    Test MUST fail because index command is not implemented or packages don't exist yet.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/cli-index.test.ts 2>&1 | grep -qE "FAIL|AssertionError|Cannot find" || test ! -f packages/cli/src/commands/index.ts</automated>
  </verify>
  <done>Integration test exists and fails for correct reason (missing implementation)</done>
</task>

<task type="auto">
  <name>Task 2: Implement gitchange index + core read helpers + package shells</name>
  <files>packages/cli/src/commands/index.ts, packages/cli/src/bin.ts, packages/cli/tsconfig.build.json, packages/core/src/read/snapshot.ts, packages/core/src/index.ts, packages/server/package.json, packages/server/src/index.ts, packages/dashboard/package.json, packages/plugin/package.json, turbo.json, package.json</files>
  <action>
    Implement `index` command per P3-D-02: resolve repoPath (flag `--repo` or auto-detect); if `readManifest(gitchangeDir)` returns null → `indexFull({ repoPath, gitchangeDir })`, else → `indexIncremental`. On success always call `computeIntelligence(repoPath, gitchangeDir)` regardless of `rebuildIntelligence` library default. Print concise stdout summary: commits indexed, manifest.lastIndexedCommit, intelligence schemaVersion, any manifest.warnings.

    Add `packages/core/src/read/snapshot.ts` with `getRepoSnapshot(gitchangeDir)` returning `{ manifest, stats: { commitCount, fileChangeCount, authorCount }, intelligencePath }` using openDb COUNT queries and optional intelligence.json parse — export from core index.ts for server plan 03-02.

    Scaffold server/dashboard/plugin packages with minimal package.json (name, type module, build/test scripts stub, workspace deps on core where needed). Update turbo.json so `build`/`test` include new packages. Server and dashboard are stubs exporting placeholder only — no implementation yet.

    Run `pnpm install` and `pnpm build --filter=@gitchange/cli` so bin resolves.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/cli-index.test.ts && pnpm build --filter=@gitchange/cli</automated>
  </verify>
  <done>`gitchange index` passes integration test; four package shells exist in workspace</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | Untrusted repo path from `--repo` flag crosses into git read + `.gitchange/` write |
| CLI → @gitchange/core | All git/SQLite operations delegated to tested core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `--repo` path | mitigate | Resolve to absolute path; reject paths without `.git`; no shell interpolation (execFileSync pattern from Phase 1) |
| T-03-02 | Information Disclosure | stdout on index | accept | Manifest summary only; no secret content in CLI output |
| T-03-SC | Tampering | commander npm install | mitigate | Blocking human checkpoint before pnpm add; slopcheck on package name |
</threat_model>

<verification>
- `pnpm vitest run tests/integration/cli-index.test.ts` passes
- `pnpm build --filter=@gitchange/cli` succeeds
- Manual: `gitchange index` in GitChange repo produces `.gitchange/intelligence.json`
</verification>

<success_criteria>
- PLUG-03 partial: `gitchange index` works end-to-end
- Package scaffold ready for server, dashboard, plugin plans
</success_criteria>

<output>
Create `.planning/phases/03-cli-plugin-scaffold/03-01-SUMMARY.md` when done
</output>
