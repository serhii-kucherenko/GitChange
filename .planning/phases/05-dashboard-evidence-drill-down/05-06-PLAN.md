---
phase: 05-dashboard-evidence-drill-down
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - packages/dashboard/src/components/ConfidenceBadge.tsx
  - packages/dashboard/src/components/IndexStatusCard.tsx
  - packages/dashboard/src/components/EraDetailPanel.tsx
  - packages/dashboard/src/layout/DashboardLayout.tsx
  - packages/dashboard/src/utils/confidence.ts
  - packages/dashboard/src/utils/confidence.test.ts
  - packages/cli/src/commands/serve.ts
  - packages/server/src/start.ts
  - tests/integration/dashboard-priv-bind.test.ts
  - tests/integration/dashboard-drilldown-e2e.test.ts
  - tests/integration/scale02-no-esgit-server.test.ts
autonomous: true
requirements: [EVD-02, PRIV-04, DASH-03, SCALE-02]
user_setup: []

must_haves:
  truths:
    - "Header shows attribution confidence Complete/Degraded from intelligence.json"
    - "Era claims and inflections show High/Medium/Low chips from evidence count heuristic (P5-D-05)"
    - "Manifest freshness warnings downgrade displayed confidence"
    - "gitchange serve defaults to 127.0.0.1; 0.0.0.0 logs stderr warning (P5-D-06)"
    - "End-to-end test: era select → commit → file hunk on fixture with semantic artifacts"
    - "Server package has zero es-git imports (SCALE-02 gate)"
  artifacts:
    - path: "packages/dashboard/src/components/ConfidenceBadge.tsx"
      provides: "EVD-02 confidence chips"
    - path: "packages/dashboard/src/utils/confidence.ts"
      provides: "evidenceCountToLevel heuristic"
    - path: "tests/integration/dashboard-drilldown-e2e.test.ts"
      provides: "Phase 5 acceptance gate"
  key_links:
    - from: "ConfidenceBadge"
      to: "intelligence.attributionConfidence"
      via: "snapshot query"
      pattern: "attributionConfidence"
    - from: "dashboard-drilldown-e2e.test.ts"
      to: "full drill path"
      via: "API sequence"
      pattern: "eras|commits|history"
---

## Phase Goal

**As a** new hire or maintainer, **I want to** explore project history interactively with era→commit→file drill-down on localhost, **so that** I can understand how the codebase evolved with linked evidence without hunting git log.

<objective>
Phase 5 completion gate: confidence UI (EVD-02 / P5-D-05), PRIV-04 localhost bind test, DASH-03 semantic freshness badges, SCALE-02 enforcement test, full drill-down E2E integration.

Purpose: Formalizes trust signals and privacy requirements; proves all Phase 5 success criteria together.
Output: ConfidenceBadge, extended IndexStatusCard, integration tests for bind policy and E2E drill path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-evidence-drill-down/05-RESEARCH.md
@packages/server/src/start.ts
@packages/dashboard/src/components/IndexStatusCard.tsx

<interfaces>
evidenceCountToLevel(count: number): "high" | "medium" | "low"
- high: count >= 3
- medium: count === 2
- low: count === 1

Global confidence from intelligence.json attributionConfidence: "complete" | "degraded"

Manifest semantic fields: semanticComputedAt, erasCheckpoint, temporalGraphCheckpoint — show in IndexStatusCard when present (DASH-03).

PRIV-04: serve command default host 127.0.0.1; integration test spawns serve without --host and asserts listening address via health check on 127.0.0.1 only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confidence utilities + badges in dashboard</name>
  <files>packages/dashboard/src/utils/confidence.ts, packages/dashboard/src/utils/confidence.test.ts, packages/dashboard/src/components/ConfidenceBadge.tsx, packages/dashboard/src/components/IndexStatusCard.tsx, packages/dashboard/src/components/EraDetailPanel.tsx, packages/dashboard/src/layout/DashboardLayout.tsx</files>
  <behavior>
    - evidenceCountToLevel(3) → high; 2 → medium; 1 → low
    - IndexStatusCard shows semantic schema version / eras checkpoint when manifest has fields
    - Header AttributionBadge shows Complete vs Degraded from snapshot intelligence
    - EraDetailPanel claims show ConfidenceBadge per P5-D-05
    - manifest.warnings.length > 0 applies visual downgrade on global badge
  </behavior>
  <action>
    Implement confidence.ts pure functions + vitest unit tests in dashboard package (add vitest devDep if needed, or place tests in tests/unit/).

    ConfidenceBadge: small chip with accessible label (High confidence / etc.), color tokens consistent with warning amber pattern.

    Extend IndexStatusCard for DASH-03: show CORE_SCHEMA_VERSION, SEMANTIC_SCHEMA_VERSION from manifest/snapshot when available; semantic computed timestamp.

    Wire EraDetailPanel inflections + claims with per-item ConfidenceBadge from evidence.length.

    Do NOT implement Phase 6 decision-confidence model — heuristic only per P5-D-05.
  </action>
  <verify>
    <automated>pnpm vitest run packages/dashboard/src/utils/confidence.test.ts 2>&1 | tail -3 | grep -q "passed"</automated>
  </verify>
  <done>EVD-02 Phase 5 scope confidence visible in dashboard header and era detail</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRIV-04 bind test + SCALE-02 gate + drill-down E2E</name>
  <files>tests/integration/dashboard-priv-bind.test.ts, tests/integration/scale02-no-esgit-server.test.ts, tests/integration/dashboard-drilldown-e2e.test.ts, packages/cli/src/commands/serve.ts, packages/server/src/start.ts</files>
  <behavior>
    - serve without --host listens on 127.0.0.1 (connection refused on 0.0.0.0 probe if applicable)
    - serve --host 0.0.0.0 stderr contains warning substring
    - scale02 test: ripgrep @gitchange/server for es-git import fails (exit 1)
    - E2E: index + semantic fixture → GET eras → filter commits by era window → commit detail with hunks → file history for same path — all 200, no es-git in server
  </behavior>
  <action>
    dashboard-priv-bind.test.ts: spawn gitchange serve on ephemeral port; assert health via 127.0.0.1; optionally parse serve log for bound address.

    scale02-no-esgit-server.test.ts: static grep packages/server/src for es-git / from "es-git" — must be zero matches.

    dashboard-drilldown-e2e.test.ts: orchestrate full Phase 5 happy path using API only (mirrors user drill). Use fixture with eras + hunks (re-index after 05-04 migration). Document in test header that semantic step required for era portion.

    Verify P5-D-06 warning string already in start.ts; add CLI test for 0.0.0.0 flag if missing from cli-serve-status.test.ts coverage.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/dashboard-priv-bind.test.ts tests/integration/scale02-no-esgit-server.test.ts tests/integration/dashboard-drilldown-e2e.test.ts 2>&1 | tail -5 | grep -q "Tests.*passed"</automated>
  </verify>
  <done>PRIV-04 and SCALE-02 enforced by CI; Phase 5 E2E drill path green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost bind | 0.0.0.0 exposes index to LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | I | serve --host 0.0.0.0 | mitigate | Default 127.0.0.1; stderr warning; test documents unsafe opt-in |
| T-05-11 | E | Misleading confidence | mitigate | Heuristic labeled in UI; degraded when manifest warnings present |
</threat_model>

<verification>
- pnpm vitest run tests/integration/dashboard-priv-bind.test.ts tests/integration/scale02-no-esgit-server.test.ts tests/integration/dashboard-drilldown-e2e.test.ts packages/dashboard/src/utils/confidence.test.ts
- All Phase 5 integration tests pass together
</verification>

<success_criteria>
- EVD-02, PRIV-04, DASH-03, SCALE-02 requirements satisfied
- ROADMAP Phase 5 success criteria 1–5 achievable after this plan
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-evidence-drill-down/05-06-SUMMARY.md` when done
</output>
